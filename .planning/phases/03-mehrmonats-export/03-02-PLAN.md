---
phase: 03-mehrmonats-export
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - frontend/src/App.js
autonomous: true
requirements:
  - MULTI-01
  - MULTI-02
  - MULTI-03

must_haves:
  truths:
    - "Nutzer sieht Von-Monat und Bis-Monat Dropdowns im Export-Bereich"
    - "Von und Bis defaulten auf den aktuell ausgewaehlten Monat"
    - "Klick auf Zeitraum-Export-Button loest Download ueber die neue API-Route aus"
    - "Bestehende Einzelmonats-Export-Buttons funktionieren weiterhin unveraendert"
    - "Dropdowns sind auf Mobile untereinander, auf Desktop nebeneinander"
  artifacts:
    - path: "frontend/src/App.js"
      provides: "Von/Bis-Dropdowns, handleExportToExcelRange Handler"
      contains: "handleExportToExcelRange"
  key_links:
    - from: "frontend/src/App.js"
      to: "/api/fahrten/export-range/:type/:startYear/:startMonth/:endYear/:endMonth"
      via: "axios.get in handleExportToExcelRange"
      pattern: "export-range"
---

<objective>
Frontend-UI fuer Mehrmonats-Export mit Von/Bis-Dropdowns und neuem Export-Handler.

Purpose: Nutzer koennen im Export-Bereich einen Zeitraum ueber mehrere Monate waehlen und den Export ausloesen (per D-01, D-02, D-03). Der bestehende Einzelmonats-Export-Button bleibt unveraendert (per D-10, D-11).
Output: Neue UI-Elemente und Handler in App.js.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-mehrmonats-export/3-CONTEXT.md
@.planning/phases/03-mehrmonats-export/03-01-SUMMARY.md

@frontend/src/App.js

<interfaces>
<!-- Neue Backend-Route aus Plan 01 -->
GET /api/fahrten/export-range/:type/:startYear/:startMonth/:endYear/:endMonth
- Liefert Excel-Datei (blob) oder ZIP bei >22 Fahrten
- Content-Type: application/vnd.openxmlformats-officedocument.spreadsheetml.sheet ODER application/zip
- Setzt automatisch Eingereicht-Status fuer jeden Monat im Zeitraum
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Von/Bis-Dropdowns und Zeitraum-Export-Handler im Frontend</name>
  <files>frontend/src/App.js</files>
  <read_first>frontend/src/App.js (Zeilen 507-520 fuer FahrtenListe State, Zeilen 620-660 fuer handleExportToExcel, Zeilen 1009-1020 fuer Export-Buttons)</read_first>
  <action>
1. **Neue State-Variablen** in der `FahrtenListe` Komponente (nach den bestehenden State-Deklarationen, ca. Zeile 516):

```javascript
const [exportVonYear, setExportVonYear] = useState(new Date().getFullYear().toString());
const [exportVonMonth, setExportVonMonth] = useState((new Date().getMonth() + 1).toString());
const [exportBisYear, setExportBisYear] = useState(new Date().getFullYear().toString());
const [exportBisMonth, setExportBisMonth] = useState((new Date().getMonth() + 1).toString());
```

2. **Neuer Handler `handleExportToExcelRange`** (nach `handleExportToExcel`, ca. Zeile 670). Dieser Handler ist analog zu `handleExportToExcel`, ruft aber die neue Route auf:

```javascript
const handleExportToExcelRange = async (type) => {
  try {
    // Validierung: Bis >= Von
    const vonDate = new Date(parseInt(exportVonYear), parseInt(exportVonMonth) - 1);
    const bisDate = new Date(parseInt(exportBisYear), parseInt(exportBisMonth) - 1);
    if (bisDate < vonDate) {
      showNotification("Fehler", "Der Bis-Monat muss gleich oder nach dem Von-Monat liegen.");
      return;
    }

    const startMonth = exportVonMonth.padStart(2, '0');
    const endMonth = exportBisMonth.padStart(2, '0');
    const response = await axios.get(
      `/api/fahrten/export-range/${type}/${exportVonYear}/${startMonth}/${exportBisYear}/${endMonth}`,
      { responseType: 'blob' }
    );

    const contentType = response.headers['content-type'];
    const contentDisposition = response.headers['content-disposition'];
    const filenameMatch = contentDisposition && contentDisposition.match(/filename="?(.+)"?/i);
    let filename = filenameMatch ? filenameMatch[1] : `fahrtenabrechnung_${type}_${exportVonYear}_${startMonth}_bis_${exportBisYear}_${endMonth}`;

    if (contentType === 'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet') {
      filename = filename.endsWith('.xlsx') ? filename : `${filename}.xlsx`;
    } else if (contentType === 'application/zip') {
      filename = filename.endsWith('.zip') ? filename : `${filename}.zip`;
    }

    const blob = new Blob([response.data], { type: contentType });
    const url = window.URL.createObjectURL(blob);
    const link = document.createElement('a');
    link.href = url;
    link.setAttribute('download', filename);
    document.body.appendChild(link);
    link.click();
    link.parentNode.removeChild(link);
    window.URL.revokeObjectURL(url);

    showNotification("Erfolg", "Export wurde heruntergeladen");
  } catch (error) {
    console.error('Fehler beim Mehrmonats-Export:', error);
    if (error.response && error.response.status === 404) {
      showNotification("Hinweis", "Keine Daten fuer den ausgewaehlten Zeitraum gefunden.");
    } else {
      showNotification("Fehler", "Export konnte nicht erstellt werden.");
    }
  }
};
```

3. **UI: Zeitraum-Export-Bereich** direkt NACH den bestehenden Export-Buttons (nach Zeile ~1020, innerhalb des Summary-Views). Per D-11 ein separater Bereich:

```jsx
{/* Zeitraum Export (per D-11) */}
<div className="mt-6 p-4 border border-gray-200 dark:border-gray-700 rounded-lg">
  <h4 className="text-sm font-medium mb-3">Zeitraum exportieren</h4>
  <div className="flex flex-col sm:flex-row gap-2 mb-3">
    {/* Von-Monat (per D-01) */}
    <div className="flex items-center gap-1">
      <label className="text-xs text-gray-500">Von:</label>
      <select
        value={exportVonMonth}
        onChange={(e) => setExportVonMonth(e.target.value)}
        className="input-field text-sm py-1 px-2"
      >
        {Array.from({length: 12}, (_, i) => (
          <option key={i+1} value={(i+1).toString()}>
            {new Date(2000, i, 1).toLocaleString('de-DE', { month: 'long' })}
          </option>
        ))}
      </select>
      <select
        value={exportVonYear}
        onChange={(e) => setExportVonYear(e.target.value)}
        className="input-field text-sm py-1 px-2 w-20"
      >
        {Array.from({length: 5}, (_, i) => {
          const y = new Date().getFullYear() - 2 + i;
          return <option key={y} value={y.toString()}>{y}</option>;
        })}
      </select>
    </div>
    {/* Bis-Monat (per D-01) */}
    <div className="flex items-center gap-1">
      <label className="text-xs text-gray-500">Bis:</label>
      <select
        value={exportBisMonth}
        onChange={(e) => setExportBisMonth(e.target.value)}
        className="input-field text-sm py-1 px-2"
      >
        {Array.from({length: 12}, (_, i) => (
          <option key={i+1} value={(i+1).toString()}>
            {new Date(2000, i, 1).toLocaleString('de-DE', { month: 'long' })}
          </option>
        ))}
      </select>
      <select
        value={exportBisYear}
        onChange={(e) => setExportBisYear(e.target.value)}
        className="input-field text-sm py-1 px-2 w-20"
      >
        {Array.from({length: 5}, (_, i) => {
          const y = new Date().getFullYear() - 2 + i;
          return <option key={y} value={y.toString()}>{y}</option>;
        })}
      </select>
    </div>
  </div>
  <div className="flex flex-col sm:flex-row gap-2">
    {getKategorienMitErstattung().map(([key, displayName]) => (
      <button
        key={`range-${key}`}
        onClick={() => handleExportToExcelRange(key.toLowerCase())}
        className="btn-primary text-sm"
      >
        Zeitraum Export {displayName}
      </button>
    ))}
  </div>
</div>
```

Die CSS-Klassen `flex-col sm:flex-row` sorgen fuer responsive Darstellung (per D-03): auf Mobile untereinander, auf Desktop nebeneinander.

WICHTIG: Die bestehenden Export-Buttons (Zeile 1010-1018) NICHT aendern (per D-10).
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Fahrtenbuch && grep -n "handleExportToExcelRange" frontend/src/App.js && grep -n "exportVonMonth" frontend/src/App.js && grep -n "export-range" frontend/src/App.js && grep -n "Zeitraum exportieren" frontend/src/App.js</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/App.js` enthaelt `handleExportToExcelRange`
    - `frontend/src/App.js` enthaelt `exportVonMonth` und `exportBisMonth` State-Variablen
    - `frontend/src/App.js` enthaelt `exportVonYear` und `exportBisYear` State-Variablen
    - `frontend/src/App.js` enthaelt `export-range` in der axios URL
    - `frontend/src/App.js` enthaelt "Zeitraum exportieren" als UI-Label
    - `frontend/src/App.js` enthaelt `flex-col sm:flex-row` fuer responsive Dropdowns (per D-03)
    - Bestehende `handleExportToExcel` Funktion ist UNVERAENDERT
    - Bestehende Export-Buttons (mit `handleExportToExcel`) sind UNVERAENDERT
    - Bis >= Von Validierung ist implementiert
  </acceptance_criteria>
  <done>Von/Bis-Dropdowns sichtbar, Zeitraum-Export-Button loest Download ueber neue Route aus, bestehender Export unveraendert</done>
</task>

</tasks>

<verification>
- Frontend kompiliert ohne Fehler: `cd frontend && npx react-scripts build 2>&1 | tail -5`
- Beide Export-Handler existieren: `grep -c "handleExportToExcel\b" frontend/src/App.js` (sollte weiterhin existieren)
- Neuer Handler existiert: `grep -c "handleExportToExcelRange" frontend/src/App.js`
- Responsive Klassen: `grep "flex-col sm:flex-row" frontend/src/App.js`
</verification>

<success_criteria>
- Von-Monat und Bis-Monat Dropdowns sind im Export-Bereich sichtbar (per D-01)
- Defaults sind aktueller Monat (per D-02)
- Responsive Layout: Mobile untereinander, Desktop nebeneinander (per D-03)
- Zeitraum-Export-Button pro Abrechnungstraeger vorhanden (per D-11)
- Bestehende Einzelmonats-Export-Buttons funktionieren weiterhin (per D-10)
- Validierung: Bis-Monat >= Von-Monat
</success_criteria>

<output>
After completion, create `.planning/phases/03-mehrmonats-export/03-02-SUMMARY.md`
</output>
