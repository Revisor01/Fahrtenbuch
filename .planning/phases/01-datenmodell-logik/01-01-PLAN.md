---
phase: 01-datenmodell-logik
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/migrations/0006_add_kostenstelle.sql
  - backend/models/AbrechnungsTraeger.js
  - backend/controllers/abrechnungstraegerController.js
  - frontend/src/components/AbrechnungstraegerForm.js
autonomous: true
requirements: [ABRTR-01]

must_haves:
  truths:
    - "Nutzer kann beim Abrechnungstraeger ein optionales Kostenstelle-Feld ausfuellen und speichern"
    - "Kostenstelle erscheint in der Abrechnungstraeger-Uebersicht neben dem Namen"
    - "Wenn Kostenstelle leer ist, wird nur der Name angezeigt (kein 'Kst.:'-Prefix)"
    - "Kostenstelle erscheint im Bearbeitungsdialog und kann geaendert werden"
  artifacts:
    - path: "backend/migrations/0006_add_kostenstelle.sql"
      provides: "ALTER TABLE fuer kostenstelle Spalte"
      contains: "ALTER TABLE abrechnungstraeger ADD COLUMN kostenstelle"
    - path: "backend/models/AbrechnungsTraeger.js"
      provides: "kostenstelle in allen CRUD-Queries"
      contains: "kostenstelle"
    - path: "backend/controllers/abrechnungstraegerController.js"
      provides: "kostenstelle in create/update/getById Endpoints"
      contains: "kostenstelle"
    - path: "frontend/src/components/AbrechnungstraegerForm.js"
      provides: "Kostenstelle Input-Feld und Anzeige"
      contains: "kostenstelle"
  key_links:
    - from: "frontend/src/components/AbrechnungstraegerForm.js"
      to: "/api/abrechnungstraeger"
      via: "axios POST/PUT mit kostenstelle Feld"
      pattern: "kostenstelle"
    - from: "backend/controllers/abrechnungstraegerController.js"
      to: "backend/models/AbrechnungsTraeger.js"
      via: "create/update mit kostenstelle Parameter"
      pattern: "kostenstelle"
---

<objective>
Kostenstelle als optionales Freitextfeld am Abrechnungstraeger hinzufuegen — von der Datenbank ueber die API bis zur UI.

Purpose: Nutzer muessen Kostenstellen pflegen koennen, damit diese spaeter (Phase 2) im Excel-Export erscheinen.
Output: Migration, erweitertes Model/Controller, erweitertes Frontend-Formular.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-datenmodell-logik/1-CONTEXT.md

<interfaces>
<!-- Existing code contracts the executor needs -->

From backend/models/AbrechnungsTraeger.js:
```javascript
class AbrechnungsTraeger {
    static async findAllForUser(userId)    // SELECT at.* FROM abrechnungstraeger at WHERE at.user_id = ?
    static async findById(id, userId)      // SELECT id, name, active FROM abrechnungstraeger
    static async create(userData)           // INSERT INTO abrechnungstraeger (user_id, name, sort_order)
    static async update(id, userId, updateData) // UPDATE abrechnungstraeger SET name = ?, active = ?
    static async delete(id, userId)
}
```

From backend/controllers/abrechnungstraegerController.js:
```javascript
exports.createAbrechnungstraeger  // POST: extracts { name } from req.body
exports.updateAbrechnungstraeger  // PUT: extracts { name, active } from req.body
exports.getById                   // GET: SELECT id, name, active
exports.getSimpleList             // GET: SELECT id, name, active (active only)
exports.getAllAbrechnungstraeger   // GET: calls findAllForUser
```

DB schema (abrechnungstraeger):
```sql
CREATE TABLE abrechnungstraeger (
    id INT AUTO_INCREMENT PRIMARY KEY,
    user_id INT NOT NULL,
    name VARCHAR(255) NOT NULL,
    sort_order INT NOT NULL DEFAULT 0,
    active TINYINT(1) DEFAULT 1,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE
);
```

Frontend state in AbrechnungstraegerForm.js:
```javascript
const [newEntry, setNewEntry] = useState({ name: '' });
const [editingTraeger, setEditingTraeger] = useState(null);
// editingTraeger comes from GET /api/abrechnungstraeger/:id which returns { id, name, active }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DB-Migration und Backend (Model + Controller) fuer Kostenstelle</name>
  <read_first>
    - backend/migrations/0005_add_standard_orte.sql (letztes Migrations-File, fuer Nummern-Konvention)
    - backend/models/AbrechnungsTraeger.js (aktueller Stand)
    - backend/controllers/abrechnungstraegerController.js (aktueller Stand)
  </read_first>
  <files>
    backend/migrations/0006_add_kostenstelle.sql,
    backend/models/AbrechnungsTraeger.js,
    backend/controllers/abrechnungstraegerController.js
  </files>
  <action>
    1. Migration `backend/migrations/0006_add_kostenstelle.sql` erstellen:
       ```sql
       ALTER TABLE abrechnungstraeger ADD COLUMN kostenstelle VARCHAR(255) DEFAULT NULL AFTER name;
       ```

    2. `backend/models/AbrechnungsTraeger.js` anpassen:
       - `findAllForUser()`: SELECT bleibt `at.*` — liefert kostenstelle automatisch mit, keine Aenderung noetig.
       - `findById()`: SELECT erweitern von `id, name, active` auf `id, name, kostenstelle, active` (per D-01).
       - `create()`: INSERT erweitern um kostenstelle:
         `INSERT INTO abrechnungstraeger (user_id, name, kostenstelle, sort_order) VALUES (?, ?, ?, ?)`
         mit `[userData.userId, userData.name, userData.kostenstelle || null, userData.sortOrder]`
       - `update()`: UPDATE erweitern:
         `UPDATE abrechnungstraeger SET name = ?, kostenstelle = ?, active = ? WHERE id = ? AND user_id = ?`
         mit `[updateData.name, updateData.kostenstelle !== undefined ? updateData.kostenstelle : null, updateData.active, id, userId]`

    3. `backend/controllers/abrechnungstraegerController.js` anpassen:
       - `createAbrechnungstraeger`: `const { name, kostenstelle } = req.body;` und an `AbrechnungsTraeger.create()` weitergeben: `{ userId: req.user.id, name, kostenstelle, sortOrder: nextSortOrder }` (per D-01, D-02 keine Validierung).
       - `updateAbrechnungstraeger`: `const { name, active, kostenstelle } = req.body;` — beim vollstaendigen Update (name !== undefined Block) auch kostenstelle mitschicken. Den direkten SQL-Query `UPDATE abrechnungstraeger SET name = ?` durch `AbrechnungsTraeger.update(id, req.user.id, { name, kostenstelle, active })` ersetzen (per D-03 VARCHAR(255)).
       - `getById`: Die direkte SQL-Query `SELECT id, name, active` auf `SELECT id, name, kostenstelle, active` erweitern.
       - `getSimpleList`: SQL erweitern auf `SELECT id, name, kostenstelle, active`.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Fahrtenbuch && grep -n "kostenstelle" backend/migrations/0006_add_kostenstelle.sql backend/models/AbrechnungsTraeger.js backend/controllers/abrechnungstraegerController.js</automated>
  </verify>
  <acceptance_criteria>
    - Datei backend/migrations/0006_add_kostenstelle.sql existiert und enthaelt "ALTER TABLE abrechnungstraeger ADD COLUMN kostenstelle VARCHAR(255)"
    - backend/models/AbrechnungsTraeger.js: findById enthaelt "kostenstelle" im SELECT
    - backend/models/AbrechnungsTraeger.js: create() INSERT-Statement enthaelt "kostenstelle"
    - backend/models/AbrechnungsTraeger.js: update() UPDATE-Statement enthaelt "kostenstelle"
    - backend/controllers/abrechnungstraegerController.js: createAbrechnungstraeger destrukturiert "kostenstelle" aus req.body
    - backend/controllers/abrechnungstraegerController.js: getById SELECT enthaelt "kostenstelle"
    - backend/controllers/abrechnungstraegerController.js: getSimpleList SELECT enthaelt "kostenstelle"
  </acceptance_criteria>
  <done>Migration-Datei existiert, alle Model- und Controller-Methoden lesen und schreiben kostenstelle</done>
</task>

<task type="auto">
  <name>Task 2: Frontend-Formular um Kostenstelle erweitern</name>
  <read_first>
    - frontend/src/components/AbrechnungstraegerForm.js (aktueller Stand)
  </read_first>
  <files>frontend/src/components/AbrechnungstraegerForm.js</files>
  <action>
    1. State fuer neuen Eintrag erweitern:
       `const [newEntry, setNewEntry] = useState({ name: '', kostenstelle: '' });`

    2. Im "Hinzufuegen"-Formular (handleAddNew / form JSX) nach dem Name-Input ein weiteres Input-Feld einfuegen:
       ```jsx
       <div className="w-full">
           <label className="form-label">Kostenstelle (optional)</label>
           <input
               type="text"
               value={newEntry.kostenstelle}
               onChange={(e) => setNewEntry({ ...newEntry, kostenstelle: e.target.value })}
               className="form-input"
               placeholder="z.B. 760130"
           />
       </div>
       ```
       Im handleAddNew nach dem POST-Aufruf den Reset erweitern: `setNewEntry({ name: '', kostenstelle: '' })`

    3. In der Uebersichtsliste (das `traeger.map` JSX) die Anzeige aendern — per D-04 und D-05:
       Statt `<div className="font-medium text-value">{traeger.name}</div>` verwende:
       ```jsx
       <div className="font-medium text-value">
           {traeger.name}{traeger.kostenstelle ? ` — Kst.: ${traeger.kostenstelle}` : ''}
       </div>
       ```

    4. Im Bearbeitungsdialog (editingTraeger !== null Block) nach dem Name-Input ein Kostenstelle-Input einfuegen:
       ```jsx
       <div className="w-full">
           <label className="form-label">Kostenstelle (optional)</label>
           <input
               type="text"
               value={editingTraeger.kostenstelle || ''}
               onChange={(e) => setEditingTraeger({ ...editingTraeger, kostenstelle: e.target.value })}
               className="form-input w-full"
               placeholder="z.B. 760130"
           />
       </div>
       ```

    5. Im handleUpdate den PUT-Request erweitern:
       `await axios.put(\`/api/abrechnungstraeger/${editingTraeger.id}\`, { name: editingTraeger.name, kostenstelle: editingTraeger.kostenstelle });`

    6. Responsive per D-06: Die flex-col/sm:flex-row Klassen sind bereits vorhanden, das neue Feld fuegt sich in das bestehende responsive Layout ein.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Fahrtenbuch && grep -c "kostenstelle" frontend/src/components/AbrechnungstraegerForm.js</automated>
  </verify>
  <acceptance_criteria>
    - "kostenstelle" kommt mindestens 8 Mal in AbrechnungstraegerForm.js vor (State-Init, Add-Input, Reset, Anzeige, Edit-Input-Value, Edit-onChange, handleUpdate)
    - String "Kst.:" kommt in AbrechnungstraegerForm.js vor (per D-04)
    - String "Kostenstelle (optional)" kommt in AbrechnungstraegerForm.js vor (Label fuer das Feld)
    - handleUpdate sendet kostenstelle im PUT-Request
  </acceptance_criteria>
  <done>Kostenstelle-Feld ist im Hinzufuegen-Formular, in der Uebersichtsliste und im Bearbeitungsdialog sichtbar</done>
</task>

</tasks>

<verification>
- Migration-Datei 0006 existiert mit ALTER TABLE Statement
- Backend-Model und Controller verarbeiten kostenstelle in allen CRUD-Operationen
- Frontend zeigt Kostenstelle in Uebersicht (Format "Name — Kst.: Wert") und Bearbeitungsdialog
- Leere Kostenstelle wird ohne "Kst.:"-Prefix angezeigt (per D-05)
</verification>

<success_criteria>
- `grep -r "kostenstelle" backend/migrations/ backend/models/AbrechnungsTraeger.js backend/controllers/abrechnungstraegerController.js frontend/src/components/AbrechnungstraegerForm.js` zeigt Treffer in allen 4 Dateien
- Migration enthaelt VARCHAR(255) DEFAULT NULL
- Anzeige-Format in Frontend: "Name — Kst.: Wert" (mit ternary fuer leere Kostenstelle)
</success_criteria>

<output>
After completion, create `.planning/phases/01-datenmodell-logik/01-01-SUMMARY.md`
</output>
