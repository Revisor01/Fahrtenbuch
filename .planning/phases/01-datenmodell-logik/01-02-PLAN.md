---
phase: 01-datenmodell-logik
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/controllers/distanzController.js
  - backend/models/Distanz.js
autonomous: true
requirements: [DATA-01]

must_haves:
  truths:
    - "Wenn ein Nutzer eine gespeicherte Distanz aendert, werden alle bestehenden Fahrten mit dieser Strecke automatisch auf den neuen Kilometerwert aktualisiert"
    - "Die Distanz-Aktualisierung laeuft atomar in einer Transaktion"
    - "Beide Richtungen werden aktualisiert (A nach B und B nach A)"
    - "Stille Aktualisierung ohne Bestaetigungsdialog"
  artifacts:
    - path: "backend/controllers/distanzController.js"
      provides: "Aufruf von Fahrt.updateFahrtenByDistanz nach Distanz-Update"
      contains: "updateFahrtenByDistanz"
    - path: "backend/models/Distanz.js"
      provides: "Transaktionsbasiertes Update mit Fahrten-Aktualisierung"
      contains: "beginTransaction"
  key_links:
    - from: "backend/controllers/distanzController.js"
      to: "backend/models/Fahrt.js"
      via: "Fahrt.updateFahrtenByDistanz() Aufruf"
      pattern: "Fahrt\\.updateFahrtenByDistanz"
    - from: "backend/models/Distanz.js"
      to: "backend/models/Fahrt.js"
      via: "Import und Aufruf in transaktionalem update/createOrUpdate"
      pattern: "require.*Fahrt"
---

<objective>
Distanz-Aenderungen sollen automatisch und atomar alle bestehenden Fahrten mit dieser Strecke rueckwirkend aktualisieren.

Purpose: Nutzer muessen Distanzen korrigieren koennen, ohne alle betroffenen Fahrten manuell anzupassen (Joergs Tellingstedt-Albersdorf Problem).
Output: Erweiterte Distanz-Model-Methoden mit Transaktionen und automatischem Fahrten-Update.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-datenmodell-logik/1-CONTEXT.md

<interfaces>
<!-- Existing code contracts the executor needs -->

From backend/models/Fahrt.js (Zeile 118-126):
```javascript
static async updateFahrtenByDistanz(vonOrtId, nachOrtId, neueDistanz) {
    const [result] = await db.execute(
      `UPDATE fahrten
      SET kilometer = ?
      WHERE (von_ort_id = ? AND nach_ort_id = ?) OR (von_ort_id = ? AND nach_ort_id = ?)`,
      [neueDistanz, vonOrtId, nachOrtId, nachOrtId, vonOrtId]
    );
    return result.affectedRows;
}
```
WICHTIG: Diese Methode existiert bereits, nutzt aber db.execute() (keine Transaktion). Sie wird bisher nirgends aufgerufen. Fuer atomares Update muss sie eine Connection akzeptieren oder intern eine Transaktion verwenden.

From backend/models/Distanz.js:
```javascript
static async update(id, vonOrtId, nachOrtId, distanz, userId) {
    const [result] = await db.execute(
      'UPDATE distanzen SET von_ort_id = ?, nach_ort_id = ?, distanz = ? WHERE id = ? AND user_id = ?',
      [vonOrtId, nachOrtId, distanz, id, userId]
    );
    return result.affectedRows > 0;
}

static async createOrUpdate(vonOrtId, nachOrtId, distanz, userId) {
    // Prueft ob Distanz existiert, dann UPDATE oder INSERT
    // Kein transaktionales Fahrten-Update
}
```

From backend/controllers/distanzController.js:
```javascript
exports.updateDistanz = async (req, res) => {
    const { id } = req.params;
    const { vonOrtId, nachOrtId, distanz } = req.body;
    const result = await Distanz.update(id, vonOrtId, nachOrtId, distanz, userId);
    // Kein Aufruf von Fahrt.updateFahrtenByDistanz
};

exports.createOrUpdateDistanz = async (req, res) => {
    const result = await Distanz.createOrUpdate(vonOrtId, nachOrtId, distanz, userId);
    // Kein Aufruf von Fahrt.updateFahrtenByDistanz
};
```

Transaktions-Pattern im Projekt (z.B. AbrechnungsTraeger.create):
```javascript
const connection = await db.getConnection();
try {
    await connection.beginTransaction();
    // ... queries mit connection.execute() ...
    await connection.commit();
} catch (error) {
    await connection.rollback();
    throw error;
} finally {
    connection.release();
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Distanz-Model mit Transaktion und automatischem Fahrten-Update</name>
  <read_first>
    - backend/models/Distanz.js (aktueller Stand)
    - backend/models/Fahrt.js (updateFahrtenByDistanz Methode, Zeile 118-126)
  </read_first>
  <files>backend/models/Distanz.js</files>
  <action>
    1. Am Anfang der Datei den Import hinzufuegen (nach `const db = require('../config/database');`):
       ```javascript
       const Fahrt = require('./Fahrt');
       ```

    2. `Distanz.update()` (Zeile 48-54) umbauen auf Transaktions-Pattern per D-09:
       ```javascript
       static async update(id, vonOrtId, nachOrtId, distanz, userId) {
           const connection = await db.getConnection();
           try {
               await connection.beginTransaction();

               const [result] = await connection.execute(
                   'UPDATE distanzen SET von_ort_id = ?, nach_ort_id = ?, distanz = ? WHERE id = ? AND user_id = ?',
                   [vonOrtId, nachOrtId, distanz, id, userId]
               );

               if (result.affectedRows > 0) {
                   // Per D-08: Beide Richtungen werden durch die OR-Bedingung in updateFahrtenByDistanz abgedeckt
                   await connection.execute(
                       `UPDATE fahrten SET kilometer = ? WHERE (von_ort_id = ? AND nach_ort_id = ?) OR (von_ort_id = ? AND nach_ort_id = ?)`,
                       [distanz, vonOrtId, nachOrtId, nachOrtId, vonOrtId]
                   );
               }

               await connection.commit();
               return result.affectedRows > 0;
           } catch (error) {
               await connection.rollback();
               console.error('Fehler beim Aktualisieren der Distanz:', error);
               throw error;
           } finally {
               connection.release();
           }
       }
       ```

    3. `Distanz.createOrUpdate()` ebenfalls auf Transaktion umbauen — der UPDATE-Zweig (existingDistanz.length > 0) muss auch die Fahrten aktualisieren:
       ```javascript
       static async createOrUpdate(vonOrtId, nachOrtId, distanz, userId) {
           const connection = await db.getConnection();
           try {
               await connection.beginTransaction();

               vonOrtId = vonOrtId || null;
               nachOrtId = nachOrtId || null;
               distanz = distanz || null;

               if (vonOrtId === null || nachOrtId === null || distanz === null) {
                   throw new Error('Ungueltige Parameter fuer createOrUpdate');
               }

               const [existingDistanz] = await connection.execute(
                   'SELECT * FROM distanzen WHERE (von_ort_id = ? AND nach_ort_id = ?) OR (von_ort_id = ? AND nach_ort_id = ?)',
                   [vonOrtId, nachOrtId, nachOrtId, vonOrtId]
               );

               let resultId;
               if (existingDistanz.length > 0) {
                   await connection.execute(
                       'UPDATE distanzen SET distanz = ? WHERE ((von_ort_id = ? AND nach_ort_id = ?) OR (von_ort_id = ? AND nach_ort_id = ?)) AND user_id = ?',
                       [distanz, vonOrtId, nachOrtId, nachOrtId, vonOrtId, userId]
                   );
                   // Fahrten rueckwirkend aktualisieren (per D-08 bidirektional)
                   await connection.execute(
                       `UPDATE fahrten SET kilometer = ? WHERE (von_ort_id = ? AND nach_ort_id = ?) OR (von_ort_id = ? AND nach_ort_id = ?)`,
                       [distanz, vonOrtId, nachOrtId, nachOrtId, vonOrtId]
                   );
                   resultId = existingDistanz[0].id;
               } else {
                   const [result] = await connection.execute(
                       'INSERT INTO distanzen (von_ort_id, nach_ort_id, distanz, user_id) VALUES (?, ?, ?, ?)',
                       [vonOrtId, nachOrtId, distanz, userId]
                   );
                   resultId = result.insertId;
               }

               await connection.commit();
               return resultId;
           } catch (error) {
               await connection.rollback();
               console.error('Fehler in Distanz.createOrUpdate:', error);
               throw error;
           } finally {
               connection.release();
           }
       }
       ```

    HINWEIS: Per D-07 keine Frontend-Aenderungen noetig — stille Aktualisierung ohne Toast/Dialog. Der Controller muss NICHT geaendert werden, da die Logik komplett im Model liegt.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Fahrtenbuch && grep -n "beginTransaction\|UPDATE fahrten\|connection.execute\|connection.rollback\|connection.commit" backend/models/Distanz.js</automated>
  </verify>
  <acceptance_criteria>
    - backend/models/Distanz.js enthaelt "beginTransaction" (mindestens 2 Mal — einmal in update, einmal in createOrUpdate)
    - backend/models/Distanz.js enthaelt "UPDATE fahrten SET kilometer" (mindestens 2 Mal — einmal in update, einmal in createOrUpdate)
    - backend/models/Distanz.js enthaelt "connection.rollback()" (fuer atomares Verhalten)
    - backend/models/Distanz.js enthaelt "connection.release()" in finally-Block
    - Alle db.execute() Aufrufe in update() und createOrUpdate() sind durch connection.execute() ersetzt
    - Es gibt KEINEN Import oder Aufruf von Fahrt.updateFahrtenByDistanz (da wir die SQL-Queries inline ausfuehren mit connection.execute fuer die Transaktion)
  </acceptance_criteria>
  <done>Distanz-Aenderungen aktualisieren automatisch und atomar alle betroffenen Fahrten in beiden Richtungen, ohne Frontend-Feedback</done>
</task>

</tasks>

<verification>
- Distanz.update() und Distanz.createOrUpdate() verwenden Transaktionen (beginTransaction/commit/rollback)
- Beide Methoden fuehren nach dem Distanz-UPDATE ein Fahrten-UPDATE aus
- Die Fahrten-UPDATE Query deckt beide Richtungen ab (OR-Bedingung)
- Kein Bestaetigungsdialog oder Toast im Frontend (per D-07)
- Error-Handling: Bei Fehler wird die Transaktion zurueckgerollt
</verification>

<success_criteria>
- `grep -c "beginTransaction" backend/models/Distanz.js` gibt mindestens 2 zurueck
- `grep -c "UPDATE fahrten" backend/models/Distanz.js` gibt mindestens 2 zurueck
- `grep "connection.rollback" backend/models/Distanz.js` zeigt rollback in beiden Methoden
- Kein neuer Code in frontend/ (stille Aktualisierung per D-07)
</success_criteria>

<output>
After completion, create `.planning/phases/01-datenmodell-logik/01-02-SUMMARY.md`
</output>
