---
phase: 18-einstellungen-login-polish
plan: 03
type: execute
wave: 2
depends_on: [18-01, 18-02]
files_modified:
  - frontend/src/index.css
  - frontend/src/components/AppContent.js
autonomous: true
requirements: [ANI-01]

must_haves:
  truths:
    - "Tab-Wechsel in AppContent hat eine sanfte Fade-Transition statt abruptem Umschalten"
    - "Settings Sub-Tab-Wechsel hat eine sanfte Fade-Transition"
    - "Card-Hover-Effekte sind mit Transitions versehen"
  artifacts:
    - path: "frontend/src/index.css"
      provides: "CSS-Klassen fuer Tab-Content-Transitions"
      contains: "tab-content-fade"
    - path: "frontend/src/components/AppContent.js"
      provides: "Transition-Wrapper um Tab-Inhalte"
      contains: "tab-content-fade"
  key_links:
    - from: "frontend/src/components/AppContent.js"
      to: "frontend/src/index.css"
      via: "tab-content-fade CSS-Klasse"
      pattern: "tab-content-fade"
    - from: "frontend/src/components/Settings.js"
      to: "frontend/src/index.css"
      via: "tab-content-fade CSS-Klasse"
      pattern: "tab-content-fade"
---

<objective>
Sanfte CSS-Transitions fuer Tab-Wechsel und Card-Interaktionen hinzufuegen — rein CSS-basiert, keine JavaScript-Animationen. Der letzte visuelle Schliff fuer v2.0.

Purpose: Abruptes Umschalten zwischen Tabs und Sektionen wirkt unpoliert. Sanfte Transitions geben der App ein professionelles Gefuehl.
Output: CSS-Transitions in index.css und Transition-Wrapper in AppContent.js und Settings.js.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/src/index.css
@frontend/src/components/AppContent.js
@frontend/src/components/Settings.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: CSS-Transition-Klassen erstellen</name>
  <files>frontend/src/index.css</files>
  <action>
In index.css im @layer components Block neue Utility-Klassen hinzufuegen (per ANI-01):

1. Tab-Content Fade-Animation mit CSS @keyframes:
   Vor dem schliessenden } von @layer components einfuegen:

   /* Tab Content Transitions */
   .tab-content-fade {
     animation: tabFadeIn 200ms ease-out;
   }

   Ausserhalb von @layer components (am Ende der Datei) den @keyframes Block:

   @keyframes tabFadeIn {
     from {
       opacity: 0;
       transform: translateY(4px);
     }
     to {
       opacity: 1;
       transform: translateY(0);
     }
   }

2. Card-Container Hover-Verstaerkung — bereits vorhanden via base-container transition-all duration-200 und card-interactive hover:shadow-card-hover. Ergaenze in card-container (nicht card-interactive) einen subtilen Hover:
   .card-container aendern zu:
   @apply base-container p-6 hover:shadow-card-hover;

   ACHTUNG: Nur card-container bekommt hover:shadow-card-hover. card-container-highlight und card-container-flush NICHT aendern, da diese in Formularbereichen genutzt werden wo Hover-Effekte stoerend waeren.

3. Settings Sub-Tab-Buttons: transition-colors duration-200 ist bereits inline im JSX — keine CSS-Aenderung noetig.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Fahrtenbuch/frontend && npx react-scripts build 2>&1 | tail -5</automated>
  </verify>
  <done>tab-content-fade Klasse und @keyframes tabFadeIn existieren in index.css. card-container hat hover:shadow-card-hover. Build erfolgreich.</done>
</task>

<task type="auto">
  <name>Task 2: Transitions in AppContent und Settings anwenden</name>
  <files>frontend/src/components/AppContent.js, frontend/src/components/Settings.js</files>
  <action>
Tab-Content-Bereiche mit der neuen tab-content-fade Klasse versehen:

1. AppContent.js — Die 5 Tab-Content-Zeilen (Zeilen 122-126) jeweils in einen Wrapper mit key und Transition wrappen:

   Statt:
   {activeTab === 'dashboard' && <Dashboard onNavigate={setActiveTab} />}

   Verwende:
   {activeTab === 'dashboard' && <div key="dashboard" className="tab-content-fade"><Dashboard onNavigate={setActiveTab} /></div>}
   {activeTab === 'fahrten' && <div key="fahrten" className="tab-content-fade"><FahrtenListe /></div>}
   {activeTab === 'monatsuebersicht' && <div key="monatsuebersicht" className="tab-content-fade"><MonthlyOverview /></div>}
   {activeTab === 'einstellungen' && <div key="einstellungen" className="tab-content-fade"><Settings /></div>}
   {activeTab === 'verwaltung' && <div key="verwaltung" className="tab-content-fade"><UserManagement /></div>}

   Das key-Attribut sorgt dafuer, dass React bei jedem Tab-Wechsel das Element neu mounted und die CSS-Animation erneut abspielt.

2. Settings.js — Im Content-Bereich (div.flex-1) jeden Tab-Content-Block in einen Wrapper mit key und tab-content-fade wrappen:

   Statt:
   {activeTab === 'profile' && (<div className="space-y-4">...)}

   Verwende:
   {activeTab === 'profile' && (<div key="profile" className="tab-content-fade"><div className="space-y-4">...</div></div>)}

   Gleiches Muster fuer alle 8 Sub-Tabs: profile, orte, distanzen, abrechnungen, favoriten, erstattungssaetze, security, api.
   Jeder bekommt ein aeusseres div mit key={tabId} className="tab-content-fade".
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Fahrtenbuch/frontend && npx react-scripts build 2>&1 | tail -5</automated>
  </verify>
  <done>Alle Haupt-Tab-Wechsel und Settings-Sub-Tab-Wechsel haben sanfte Fade-In-Animation. Build erfolgreich.</done>
</task>

</tasks>

<verification>
- Build kompiliert ohne Fehler
- Tab-Wechsel in der Haupt-Navigation zeigt Fade-In-Effekt
- Sub-Tab-Wechsel in Einstellungen zeigt Fade-In-Effekt
- card-container Elemente zeigen Hover-Shadow-Verstaerkung
- Keine JavaScript-Animationsbibliotheken — rein CSS
</verification>

<success_criteria>
Tab-Wechsel und Card-Interaktionen fuehlen sich smooth und professionell an. Keine abrupten UI-Sprünge mehr.
</success_criteria>

<output>
After completion, create `.planning/phases/18-einstellungen-login-polish/18-03-SUMMARY.md`
</output>
