---
phase: 17-listen-uebersichten
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/FahrtenListe.js
autonomous: true
requirements:
  - FE-01
  - FE-02
  - FE-03

must_haves:
  truths:
    - "Die Fahrtenliste zeigt nur die Liste der Fahrten (kein Eingabeformular) in modernem Card-Layout"
    - "Filter- und Export-Bereich sind visuell als eigene Cards abgegrenzt und modernisiert"
    - "Auf Mobile ist die Fahrtenliste kompakt dargestellt und per Touch bedienbar"
  artifacts:
    - path: "frontend/src/components/FahrtenListe.js"
      provides: "Modernisierte Fahrtenliste mit Card-Layout"
      contains: "card-container"
  key_links:
    - from: "frontend/src/components/FahrtenListe.js"
      to: "frontend/src/index.css"
      via: "CSS-Klassen card-container, card-container-highlight, mobile-card, table-container"
      pattern: "card-container|mobile-card|table-container"
---

<objective>
FahrtenListe visuell modernisieren: Filter/Zeitraum-Bereich und Export-Buttons in eigene Cards wrappen, Tabellen-Bereich als Card, Mobile-Darstellung mit touch-freundlichen Targets (min 44px). FE-01 ist teilweise erledigt (Formular in Phase 16 entfernt), hier geht es um das Card-Layout und die visuelle Modernisierung.

Purpose: Die Fahrtenliste soll konsistent mit dem Phase-15-Designsystem aussehen und auf Mobile gut bedienbar sein.
Output: Modernisierte FahrtenListe.js mit Card-Layout fuer alle Sektionen.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-listen-uebersichten/17-CONTEXT.md
@frontend/src/components/FahrtenListe.js
@frontend/src/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Filter/Zeitraum-Card und Export-Card visuell modernisieren</name>
  <files>frontend/src/components/FahrtenListe.js</files>
  <action>
In der `renderAbrechnungsStatus`-Funktion (ca. Zeile 570-794):

1. **Aeusserer Container bleibt card-container-highlight** — das ist bereits korrekt fuer den Header-Bereich.

2. **Export-Bereich als eigene Card abgrenzen (per FE-02):** Der Export-Abschnitt (Zeile ~768-790, `flex flex-col sm:flex-row justify-end gap-2 mt-6`) soll in eine eigene `card-container` gewrappt werden:
   - Wrap die Export-Buttons in `<div className="card-container">` mit einem kleinen Titel `<h3 className="text-sm font-medium text-label mb-3">Export</h3>` darueber
   - Export-Buttons: statt nur `btn-primary` nutze ein Grid-Layout `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-2` damit die Buttons gleichmaessig verteilt sind
   - Jeder Button bekommt `min-h-[44px]` fuer touch-freundliche Targets (per FE-03)

3. **Zeitraum-Auswahl modernisieren:** Die Von/Bis-Dropdowns (Zeile ~601-669):
   - Select-Felder bekommen `min-h-[44px]` auf Mobile via `className="form-select min-w-0 flex-1 sm:min-h-0 min-h-[44px]"`
   - "Aktueller Monat"-Button bekommt ebenfalls `min-h-[44px]` auf Mobile

4. **Outer wrapper der gesamten Komponente:** Der return-Wert (Zeile ~1186) hat `<div>` ohne Klassen. Aendere zu `<div className="space-y-6">` fuer konsistentes Spacing zwischen den Sektionen.

WICHTIG: Keine Logik aendern, nur CSS-Klassen und Wrapper-Divs. Alle bestehenden onClick-Handler und State-Management bleiben exakt gleich.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Fahrtenbuch && grep -c "card-container" frontend/src/components/FahrtenListe.js</automated>
  </verify>
  <done>
- Export-Bereich ist in eigener card-container mit Titel "Export"
- Export-Buttons haben min-h-[44px] fuer Touch-Targets
- Select-Felder haben min-h-[44px] auf Mobile
- Outer wrapper hat space-y-6
- Alle bestehende Logik ist unveraendert
  </done>
</task>

<task type="auto">
  <name>Task 2: Fahrten-Tabelle und Mobile-Cards modernisieren</name>
  <files>frontend/src/components/FahrtenListe.js</files>
  <action>
Im return-Block der Komponente (ab Zeile ~1186):

1. **Desktop-Tabelle in Card wrappen (per FE-01):** Die Desktop-Tabelle (Zeile ~1189-1250) ist bereits in `table-container` — das erbt von `base-container` und hat damit Card-Styling. Hier nur eine Ueberschrift hinzufuegen:
   - Vor dem `<table>` ein `<div className="px-6 py-4 border-b border-primary-100 dark:border-primary-700"><h3 className="text-lg font-medium text-value">Fahrten</h3><p className="text-sm text-label mt-1">{sortedFahrten.length} Eintraege</p></div>` einfuegen
   - Das gibt der Tabelle einen Header wie die anderen Cards

2. **Mobile-View modernisieren (per FE-03):** Die Mobile-Cards (Zeile ~1253ff) nutzen bereits `mobile-card`. Modernisierung:
   - Vor den Mobile-Cards eine Ueberschrift: `<h3 className="text-lg font-medium text-value">Fahrten <span className="text-sm font-normal text-label">({sortedFahrten.length})</span></h3>`
   - Alle Aktions-Buttons in der Mobile-View (Edit, Delete, Nochmal, Rueckfahrt) muessen mindestens `min-h-[44px] min-w-[44px]` haben fuer Touch-Targets
   - Die `mobile-icon-button-primary` und `mobile-icon-button-secondary` Buttons (Zeile ~1444-1455): fuege `min-h-[44px] min-w-[44px]` hinzu via className-Ergaenzung
   - Die Nochmal/Rueckfahrt-Buttons (Zeile ~1429-1442): fuege `min-h-[44px] min-w-[44px]` hinzu

3. **Empty State:** Falls `sortedFahrten.length === 0`, zeige in beiden Views (Desktop und Mobile) statt leerer Tabelle/Liste:
   ```jsx
   <div className="card-container text-center py-12">
     <p className="text-label">Keine Fahrten im ausgewaehlten Zeitraum</p>
   </div>
   ```

WICHTIG: Nur CSS-Klassen und Wrapper-Divs aendern. Alle bestehenden onClick-Handler, Edit-Mode-Logik und State-Management bleiben exakt gleich.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Fahrtenbuch && grep -c "min-h-\[44px\]" frontend/src/components/FahrtenListe.js</automated>
  </verify>
  <done>
- Desktop-Tabelle hat Card-Header mit Titel "Fahrten" und Anzahl
- Mobile-Buttons haben min 44px Touch-Targets
- Empty State wird bei 0 Fahrten angezeigt
- Mobile-Cards nutzen mobile-card Klassen (unveraendert)
- Alle bestehende Edit/Delete/Copy-Logik funktioniert unveraendert
  </done>
</task>

</tasks>

<verification>
- Die FahrtenListe hat kein Eingabeformular (bereits in Phase 16 entfernt, FE-01 bestaetigt)
- Filter/Zeitraum-Bereich ist in card-container-highlight
- Export-Bereich ist in eigener card-container mit Titel
- Desktop-Tabelle hat Card-Header
- Mobile-Buttons haben min 44px Touch-Targets
- Empty State bei 0 Fahrten
</verification>

<success_criteria>
- grep "card-container" FahrtenListe.js findet mindestens 5 Treffer (bestehende + neue)
- grep "min-h-\[44px\]" FahrtenListe.js findet mindestens 4 Treffer (Buttons)
- grep "Keine Fahrten" FahrtenListe.js findet 1 Treffer (Empty State)
- Kein FahrtForm-Import in FahrtenListe.js
</success_criteria>

<output>
After completion, create `.planning/phases/17-listen-uebersichten/17-01-SUMMARY.md`
</output>
