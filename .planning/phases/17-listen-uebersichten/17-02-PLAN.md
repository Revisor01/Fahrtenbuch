---
phase: 17-listen-uebersichten
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/MonthlyOverview.js
autonomous: true
requirements:
  - MO-01
  - MO-02

must_haves:
  truths:
    - "Die Monatsuebersicht ist in modernem Card-Layout dargestellt"
    - "Tabellen und Zusammenfassungen sind uebersichtlich und visuell ansprechend"
  artifacts:
    - path: "frontend/src/components/MonthlyOverview.js"
      provides: "Modernisierte Monatsuebersicht mit Card-Layout"
      contains: "card-container"
  key_links:
    - from: "frontend/src/components/MonthlyOverview.js"
      to: "frontend/src/index.css"
      via: "CSS-Klassen card-container, card-container-highlight, mobile-card"
      pattern: "card-container|mobile-card"
---

<objective>
MonthlyOverview visuell modernisieren: Header/Filter-Bereich, Jahres-Summary-Cards und Monats-Detail-Cards konsistent mit Phase-15-Designsystem gestalten. Touch-Targets auf Mobile vergroessern, Schnellaktionen-Dropdown modernisieren.

Purpose: Die Monatsuebersicht soll visuell auf dem gleichen Niveau wie das Dashboard sein.
Output: Modernisierte MonthlyOverview.js mit konsistentem Card-Layout.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-listen-uebersichten/17-CONTEXT.md
@frontend/src/components/MonthlyOverview.js
@frontend/src/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Header/Filter-Bereich und Jahres-Summary modernisieren</name>
  <files>frontend/src/components/MonthlyOverview.js</files>
  <action>
Die MonthlyOverview hat bereits card-container-highlight fuer den Header (Zeile 322) und card-container fuer die Kategorie-Cards (Zeile 421). Folgende Modernisierungen:

1. **Header-Titel verbessern (per MO-02):** Der Titel "Jahresuebersicht" (Zeile 330) bekommt ein Untertitel:
   - Unter dem h2 ein `<p className="text-sm text-label mt-1">{selectedYear === 'all' ? 'Alle Jahre' : selectedYear}</p>` einfuegen

2. **Filter-Controls modernisieren:** Die Select-Felder (Zeile 356-367 und Zeile 383-394) und Checkbox (Zeile 347-354, 374-381):
   - Alle Select-Felder: fuege `min-h-[44px]` als Klasse hinzu fuer Mobile-Touch-Targets
   - "Aktuelles Jahr"-Button (Zeile 332, 342): fuege `min-h-[44px]` hinzu
   - Checkbox-Label: fuege `min-h-[44px] flex items-center` hinzu

3. **Schnellaktionen-Dropdown modernisieren (per MO-02):** Das QuickActions-Dropdown (Zeile 119-230):
   - Der Dropdown-Container (Zeile 212) bekommt `rounded-card shadow-card` statt `rounded-lg shadow-lg`
   - Jede Dropdown-Option (Zeile 213-224) bekommt `min-h-[44px] flex items-center` statt nur `py-2`
   - Der Button selbst (Zeile 202) bekommt `min-h-[44px]`

4. **Jahres-Summary-Cards spacing (per MO-01):** Das Cards-Grid (Zeile 411-452) ist gut. Fuege dem Gesamt-Card (Zeile 437-452) eine visuelle Hervorhebung hinzu:
   - Aendere `className="card-container col-span-1 sm:col-span-2 lg:col-span-full"` zu `className="card-container-highlight col-span-1 sm:col-span-2 lg:col-span-full"` — damit hebt sich die Gesamtsumme farblich ab

5. **Empty State:** Nach dem filteredData-Mapping (nach Zeile 525), fuege einen Empty State hinzu falls `filteredData.length === 0`:
   ```jsx
   {filteredData.length === 0 && (
     <div className="card-container text-center py-12">
       <p className="text-label">Keine offenen Abrechnungen im ausgewaehlten Zeitraum</p>
     </div>
   )}
   ```

WICHTIG: Keine Logik aendern, nur CSS-Klassen und Wrapper-Divs. Alle bestehenden Funktionen (calculateYearTotal, getKategorienMitErstattung, etc.) bleiben exakt gleich.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Fahrtenbuch && grep -c "min-h-\[44px\]" frontend/src/components/MonthlyOverview.js</automated>
  </verify>
  <done>
- Header hat Untertitel mit aktuellem Jahr
- Alle interaktiven Elemente haben min 44px Touch-Targets
- Schnellaktionen-Dropdown nutzt Design-Tokens (rounded-card, shadow-card)
- Gesamt-Card nutzt card-container-highlight
- Empty State bei 0 gefilterten Monaten
  </done>
</task>

<task type="auto">
  <name>Task 2: Monats-Detail-Cards und Mobile-View modernisieren</name>
  <files>frontend/src/components/MonthlyOverview.js</files>
  <action>
Desktop-Monats-Cards (Zeile 455-526) und Mobile-View (Zeile 529-597):

1. **Desktop-Monats-Cards verbessern (per MO-01):** Die einzelnen Monats-Cards (Zeile 468) nutzen bereits `card-container`. Verbesserungen:
   - Die inneren Abrechnungstraeger-Cards (Zeile 495) nutzen ebenfalls `card-container`. Aendere zu einem leichteren Stil: `className="bg-primary-25 dark:bg-primary-900/30 rounded-lg p-4"` — gleich wie die Mitfahrer-Card (Zeile 510) damit es konsistent ist. Das verhindert verschachtelte Schatten.
   - Die Mitfahrer-Card (Zeile 510) bleibt wie sie ist (bereits korrekt gestylt)

2. **Desktop Status-Cells touch-freundlicher:** Die renderStatusCell-Funktion (Zeile 232-316):
   - Alle klickbaren status-badge Elemente (Zeile 256, 283, 302): fuege `min-h-[44px]` zu den klickbaren span-Elementen hinzu via className-Ergaenzung: z.B. `className="status-badge-primary cursor-pointer min-h-[44px] flex items-center"`

3. **Mobile-Monats-Cards verbessern (per MO-02):** Die Mobile-Cards (Zeile 541) nutzen `mobile-card`. Verbesserungen:
   - Der Header-Bereich (Zeile 542-550): Die Monatsnamen sollen groesser sein: aendere `mobile-card-title` (Zeile 544) zu `className="text-lg font-semibold text-value"`
   - Die Abrechnungstraeger-Divs (Zeile 566): fuege `border-t border-primary-100 dark:border-primary-700` hinzu fuer visuelle Trennung (statt nur `pt-4`)
   - Die Mitfahrer-Section (Zeile 580): gleiche Border-Behandlung

4. **Gesamter Outer Container:** Der aeussere Container (Zeile 321) hat `className="w-full max-w-full space-y-6"`. Das ist gut. Keine Aenderung noetig.

WICHTIG: Keine Logik aendern, nur CSS-Klassen. Alle onClick-Handler, Status-Updates und Modal-Logik bleiben exakt gleich.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Fahrtenbuch && grep -c "border-primary-100" frontend/src/components/MonthlyOverview.js</automated>
  </verify>
  <done>
- Desktop-Monats-Cards haben keine verschachtelten Schatten mehr (innere Cards nutzen bg-primary-25 statt card-container)
- Klickbare Status-Badges haben min 44px Touch-Targets
- Mobile-Monats-Cards haben groessere Monatsnamen und visuelle Trennlinien
- Alle Status-Update-Logik funktioniert unveraendert
  </done>
</task>

</tasks>

<verification>
- MonthlyOverview nutzt card-container und card-container-highlight konsistent
- Keine verschachtelten card-container (innere Cards nutzen bg-primary-25)
- Alle interaktiven Elemente haben min 44px Touch-Targets
- Empty State bei leerer Filterung
- Schnellaktionen-Dropdown nutzt Design-Tokens
</verification>

<success_criteria>
- grep "min-h-\[44px\]" MonthlyOverview.js findet mindestens 5 Treffer
- grep "card-container-highlight" MonthlyOverview.js findet mindestens 2 Treffer (Header + Gesamt-Card)
- grep "Keine offenen" MonthlyOverview.js findet 1 Treffer (Empty State)
- grep "rounded-card" MonthlyOverview.js findet mindestens 1 Treffer (Dropdown)
</success_criteria>

<output>
After completion, create `.planning/phases/17-listen-uebersichten/17-02-SUMMARY.md`
</output>
