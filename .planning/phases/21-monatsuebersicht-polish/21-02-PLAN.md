---
phase: 21-monatsuebersicht-polish
plan: 02
type: execute
wave: 2
depends_on: [21-01]
files_modified:
  - frontend/src/components/MonthlyOverview.js
autonomous: true
requirements: [D-01, D-02, D-03, D-06, D-13, D-14, D-15, D-16]

must_haves:
  truths:
    - "Keine Icons (Building2, Wallet, Banknote, Users) in den Erstattungs-KPI-Cards"
    - "Farben der Cards kommen aus dem Abrechnungstraeger-Objekt (farbe Feld), nicht aus hardcoded kategorieStyles"
    - "Jahresuebersicht und Jahreszahl haben ein Leerzeichen dazwischen"
    - "Trennstriche nach Monatsnamen sind durch Kalender-Icons ersetzt"
    - "Hintergrundfarbe der Cards passt sich dynamisch an die konfigurierte Farbe an"
  artifacts:
    - path: "frontend/src/components/MonthlyOverview.js"
      provides: "Dynamische Farben, keine Icons, Layout-Fixes"
      min_lines: 400
  key_links:
    - from: "frontend/src/components/MonthlyOverview.js"
      to: "AppContext abrechnungstraeger"
      via: "traeger.farbe fuer Hintergrundfarbe"
      pattern: "traeger\\.farbe"
---

<objective>
MonthlyOverview: Icons entfernen, Farben dynamisch aus Abrechnungstraeger laden, Layout-Fixes (Leerzeichen, Trennstriche durch Kalender-Icons).

Purpose: Die Monatsuebersicht nutzt konfigurierbare Farben statt hardcoded Werte und wird visuell aufgeraeumt (per D-01 bis D-03, D-06, D-13 bis D-16).
Output: Ueberarbeitete MonthlyOverview.js ohne Icons, mit dynamischen Farben und Layout-Fixes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-monatsuebersicht-polish/21-CONTEXT.md
@.planning/phases/21-monatsuebersicht-polish/21-01-SUMMARY.md

<interfaces>
<!-- After Plan 01: abrechnungstraeger objects now have a `farbe` field -->
<!-- farbe is a hex string like '#10b981' or null (fallback needed) -->
<!-- Available via AppContext: abrechnungstraeger array, each item has .farbe -->

From MonthlyOverview.js current state:
```javascript
// Lines 14-20 - to be replaced:
const kategorieStyles = [
  { bg: 'bg-emerald-50 dark:bg-emerald-900/20', icon: Building2, iconColor: 'text-emerald-500' },
  { bg: 'bg-blue-50 dark:bg-blue-900/20', icon: Wallet, iconColor: 'text-blue-500' },
  { bg: 'bg-purple-50 dark:bg-purple-900/20', icon: Banknote, iconColor: 'text-purple-500' },
  { bg: 'bg-amber-50 dark:bg-amber-900/20', icon: Building2, iconColor: 'text-amber-500' },
];
const mitfahrerStyle = { bg: 'bg-indigo-50 dark:bg-indigo-900/20', icon: Users, iconColor: 'text-indigo-500' };

// Line 2 - current imports:
import { AlertCircle, Circle, CheckCircle2, Banknote, Users, Building2, Wallet } from 'lucide-react';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Icons entfernen und Farben dynamisch machen</name>
  <files>frontend/src/components/MonthlyOverview.js</files>
  <read_first>
    frontend/src/components/MonthlyOverview.js,
    .planning/phases/21-monatsuebersicht-polish/21-01-SUMMARY.md
  </read_first>
  <action>
    **D-01: Icons aus KPI-Cards entfernen (Jahresuebersicht + Monats-Cards)**
    **D-02: kategorieStyles und mitfahrerStyle vereinfachen**
    **D-03: Nicht mehr benoetigte Lucide-Imports entfernen**
    **D-06: Farbwert aus Abrechnungstraeger-Objekt lesen**
    **D-07: Fallback-Farbe wenn keine Farbe gesetzt**

    1. **Import-Zeile aendern (Zeile 2):**
       - Entferne: `Building2, Wallet, Banknote, Users` aus dem Import
       - Behalte: `AlertCircle, Circle, CheckCircle2` (werden noch in renderStatusCell gebraucht)
       - Fuege hinzu: `CalendarDays` (fuer D-15/D-16, wird in Task 2 benutzt)
       ```javascript
       import { AlertCircle, Circle, CheckCircle2, CalendarDays } from 'lucide-react';
       ```

    2. **kategorieStyles und mitfahrerStyle ersetzen (Zeilen 14-20):**
       Ersetze das gesamte kategorieStyles Array und mitfahrerStyle durch eine Helper-Funktion die aus der Hex-Farbe des Traegers einen Hintergrundfarbstil generiert:
       ```javascript
       const DEFAULT_FARBE = '#6b7280'; // neutral grau als Fallback (per D-07)
       const MITFAHRER_FARBE = '#6366f1'; // indigo fuer Mitfahrer

       const getCardStyle = (hexColor) => {
         const color = hexColor || DEFAULT_FARBE;
         return {
           backgroundColor: `${color}15`, // 15 = ~8% Opacity fuer hellen Hintergrund
         };
       };

       const getCardStyleDark = (hexColor) => {
         const color = hexColor || DEFAULT_FARBE;
         return {
           backgroundColor: `${color}20`, // 20 = ~12% Opacity fuer Dark Mode
         };
       };
       ```

       Da Tailwind Dark Mode ueber Klassen funktioniert und wir inline styles brauchen, nutze einen einfacheren Ansatz mit einer einzigen Funktion:
       ```javascript
       const DEFAULT_FARBE = '#6b7280';
       const MITFAHRER_FARBE = '#6366f1';

       // Erzeugt inline style fuer Card-Hintergrund mit niedriger Opacity
       const getCardBg = (hexColor) => ({
         backgroundColor: `${hexColor || DEFAULT_FARBE}14`,
       });
       ```

    3. **Jahresuebersicht KPI-Cards (ca. Zeile 429-449) — Icons entfernen:**
       Im `getKategorienMitErstattung().map(...)` Block:
       - Entferne die `const style = ...` und `const IconComponent = ...` Zeilen
       - Ersetze `${style.bg}` im className durch leeren String
       - Fuege `style={getCardBg(traegerFarbe)}` als inline style hinzu
       - Finde die Farbe: Die Funktion gibt `[key, displayName, data]` zurueck. Hole den Traeger:
         ```javascript
         const traeger = abrechnungstraeger.find(t => t.id.toString() === key);
         const farbe = key === 'mitfahrer' ? MITFAHRER_FARBE : (traeger?.farbe || DEFAULT_FARBE);
         ```
       - Entferne die `<IconComponent size={22} ... />` Zeile komplett
       - Das Ergebnis fuer jede Card:
         ```jsx
         <div key={key} className="rounded-card p-4 shadow-card border border-card" style={getCardBg(farbe)}>
           <div className="min-w-0">
             <p className="text-xs text-muted mb-1">{displayName}</p>
             <p className="text-xl font-semibold text-value truncate">
               {(data.ausstehend || 0).toFixed(2).replace('.', ',')} &euro;
             </p>
             {data.original !== data.ausstehend && (
               <p className="text-xs text-muted">
                 Urspruenglich: {(data.original || 0).toFixed(2).replace('.', ',')} &euro;
               </p>
             )}
           </div>
         </div>
         ```

    4. **Gesamt Card (ca. Zeile 453-469) — Banknote-Icon entfernen:**
       - Entferne `<Banknote size={22} className="text-primary-500 shrink-0" />`
       - Entferne das `flex items-center justify-between gap-2` Wrapper-div (nicht mehr noetig ohne Icon)

    5. **Desktop Monats-Cards (ca. Zeile 507-531) — Icons entfernen, Farben dynamisch:**
       Im `.map((traeger, index) => ...)` Block:
       - Entferne `const style = kategorieStyles[index % kategorieStyles.length];` und `const IconComponent = style.icon;`
       - Ersetze `${style.bg}` durch leeren String, fuege `style={getCardBg(traeger.farbe)}` hinzu
       - Entferne `<IconComponent size={22} className={...} />`
       - Ergebnis:
         ```jsx
         <div key={traeger.id} className="rounded-card p-4 shadow-card border border-card" style={getCardBg(traeger.farbe)}>
           <div className="min-w-0">
             <p className="text-xs text-muted mb-1">{traeger.name}</p>
             <p className="text-xl font-semibold text-value truncate">
               {Number(month.erstattungen?.[traeger.id] || 0).toFixed(2).replace('.', ',')} &euro;
             </p>
           </div>
           <div className="mt-2">
             {renderStatusCell(month, traeger.id)}
           </div>
         </div>
         ```

    6. **Mitfahrer-Card Desktop (ca. Zeile 536-550) — Icon entfernen, Farbe dynamisch:**
       - Ersetze `${mitfahrerStyle.bg}` durch leeren String
       - Fuege `style={getCardBg(MITFAHRER_FARBE)}` hinzu
       - Entferne `<Users size={22} className={...} />`
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Fahrtenbuch && ! grep -q "Building2\|Wallet\b" frontend/src/components/MonthlyOverview.js && grep -q "getCardBg" frontend/src/components/MonthlyOverview.js && grep -q "DEFAULT_FARBE" frontend/src/components/MonthlyOverview.js && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - MonthlyOverview.js importiert NICHT mehr Building2, Wallet, Banknote, Users
    - MonthlyOverview.js importiert CalendarDays
    - kategorieStyles Array existiert NICHT mehr
    - mitfahrerStyle Objekt existiert NICHT mehr
    - getCardBg Funktion existiert mit DEFAULT_FARBE Fallback
    - MITFAHRER_FARBE Konstante existiert
    - Keine IconComponent-Referenzen in KPI-Cards
    - Keine Banknote-Referenz in der Gesamt-Card
    - traeger.farbe wird in getCardBg aufgerufen fuer Monats-Cards
  </acceptance_criteria>
  <done>Alle Icons aus KPI-Cards entfernt. Farben kommen dynamisch aus dem Abrechnungstraeger-Objekt mit grauem Fallback.</done>
</task>

<task type="auto">
  <name>Task 2: Layout-Fixes (Leerzeichen, Trennstriche durch Kalender-Icons)</name>
  <files>frontend/src/components/MonthlyOverview.js</files>
  <read_first>frontend/src/components/MonthlyOverview.js</read_first>
  <action>
    **D-13: Leerzeichen zwischen "Jahresuebersicht" und Jahr**
    **D-15: Trennstriche nach Monatsnamen durch Kalender-Icons ersetzen**
    **D-16: Kalender-Icon (CalendarDays) im Monats-Header**

    1. **Leerzeichen-Fix (D-13):**
       Finde den Header-Bereich (ca. Zeile 337-339):
       ```jsx
       <h2 className="text-lg font-medium text-value">Jahresuebersicht</h2>
       <p className="text-sm text-label mt-1">{selectedYear === 'all' ? 'Alle Jahre' : selectedYear}</p>
       ```
       Ersetze durch ein einzelnes Element mit Leerzeichen:
       ```jsx
       <h2 className="text-lg font-medium text-value">
         Jahresuebersicht {selectedYear === 'all' ? '— Alle Jahre' : selectedYear}
       </h2>
       ```
       Entferne das separate `<p>` Element.

    2. **Desktop Monats-Header: Trennstrich durch Kalender-Icon ersetzen (D-15, D-16):**
       Finde den Desktop Monats-Header (ca. Zeile 489):
       ```jsx
       <div className="flex justify-between items-center mb-4 pb-4 border-b border-primary-100 dark:border-primary-700">
       ```
       Ersetze durch:
       ```jsx
       <div className="flex justify-between items-center mb-4 pb-2">
         <div className="flex items-center gap-2">
           <CalendarDays size={20} className="text-primary-400 dark:text-primary-500" />
           <h3 className="text-lg font-medium text-value">
             {month.monthName} {month.year}
           </h3>
         </div>
         <div className="text-right">
           <div className="text-value font-medium">
             {gesamtAusstehend.toFixed(2)} &euro;
           </div>
           {gesamtAusstehend !== originalGesamt && (
             <div className="text-xs text-muted">
               Urspruenglich: {originalGesamt.toFixed(2)} &euro;
             </div>
           )}
         </div>
       </div>
       ```
       Wichtig: `border-b border-primary-100 dark:border-primary-700` wird entfernt (kein Trennstrich mehr).

    3. **Mobile Monats-Header: Ebenfalls Kalender-Icon statt Trennstrich:**
       Im Mobile-View (ca. Zeile 570-580), aendere den Monats-Header:
       ```jsx
       <div className="mobile-card-header mb-4">
         <div className="flex justify-between items-center w-full">
           <div className="flex items-center gap-2">
             <CalendarDays size={18} className="text-primary-400 dark:text-primary-500" />
             <span className="text-lg font-semibold text-value">
               {month.monthName} {month.year}
             </span>
           </div>
           <div className="text-value font-medium">
             {gesamtAusstehend.toFixed(2)} &euro;
           </div>
         </div>
       </div>
       ```
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Fahrtenbuch && grep -q "CalendarDays" frontend/src/components/MonthlyOverview.js && grep -q "Jahresuebersicht.*selectedYear\|Jahresübersicht.*selectedYear" frontend/src/components/MonthlyOverview.js && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - "Jahresuebersicht" und Jahr stehen in einem einzigen h2 Element mit Leerzeichen dazwischen
    - Das separate p-Element fuer selectedYear existiert nicht mehr
    - CalendarDays Icon erscheint im Desktop-Monats-Header (size={20})
    - CalendarDays Icon erscheint im Mobile-Monats-Header (size={18})
    - "border-b border-primary-100" existiert NICHT mehr im Desktop-Monats-Header
  </acceptance_criteria>
  <done>Leerzeichen zwischen Jahresuebersicht und Jahr eingefuegt. Trennstriche in Monats-Headern durch CalendarDays-Icons ersetzt.</done>
</task>

</tasks>

<verification>
- Keine Building2, Wallet, Banknote, Users Icons mehr in MonthlyOverview
- getCardBg nutzt traeger.farbe dynamisch
- Jahresuebersicht + Jahr in einem Element mit Leerzeichen
- CalendarDays Icons in Desktop + Mobile Monats-Headern
- Keine border-b Trennstriche in Monats-Headern
</verification>

<success_criteria>
- MonthlyOverview zeigt keine Icons in KPI-Cards
- Card-Hintergrundfarben werden dynamisch aus Abrechnungstraeger.farbe gelesen
- Layout-Fixes (Leerzeichen, Kalender-Icons) sind umgesetzt
</success_criteria>

<output>
After completion, create `.planning/phases/21-monatsuebersicht-polish/21-02-SUMMARY.md`
</output>
