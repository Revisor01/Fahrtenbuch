---
phase: 21-monatsuebersicht-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/migrations/0008_add_farbe_to_abrechnungstraeger.sql
  - backend/models/AbrechnungsTraeger.js
  - backend/controllers/abrechnungstraegerController.js
  - frontend/src/components/AbrechnungstraegerForm.js
autonomous: true
requirements: [D-04, D-05, D-06, D-07]

must_haves:
  truths:
    - "Abrechnungstraeger hat ein farbe-Feld in der Datenbank"
    - "API gibt farbe-Wert bei GET /api/abrechnungstraeger zurueck"
    - "Farbe kann beim Erstellen und Bearbeiten eines Abrechnungstraegers gesetzt werden"
    - "Wenn keine Farbe gesetzt ist, wird ein Fallback verwendet"
  artifacts:
    - path: "backend/migrations/0008_add_farbe_to_abrechnungstraeger.sql"
      provides: "farbe Spalte in abrechnungstraeger Tabelle"
      contains: "ADD COLUMN farbe"
    - path: "backend/models/AbrechnungsTraeger.js"
      provides: "farbe in create und update Queries"
      contains: "farbe"
    - path: "frontend/src/components/AbrechnungstraegerForm.js"
      provides: "Farbwahl-UI im Formular"
      contains: "farbe"
  key_links:
    - from: "frontend/src/components/AbrechnungstraegerForm.js"
      to: "/api/abrechnungstraeger"
      via: "axios POST/PUT mit farbe Feld"
      pattern: "farbe"
    - from: "backend/controllers/abrechnungstraegerController.js"
      to: "AbrechnungsTraeger model"
      via: "farbe in create/update"
      pattern: "farbe"
---

<objective>
Abrechnungstraeger um ein konfigurierbares Farbe-Feld erweitern (Backend + Einstellungen-UI).

Purpose: Ermoeglicht es Nutzern, jedem Abrechnungstraeger eine individuelle Farbe zuzuweisen, die in der Monatsuebersicht verwendet wird (per D-04).
Output: DB-Migration, erweiterte API, Farbwahl in AbrechnungstraegerForm.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-monatsuebersicht-polish/21-CONTEXT.md

<interfaces>
<!-- Backend Model - current AbrechnungsTraeger -->
From backend/models/AbrechnungsTraeger.js:
```javascript
static async create(userData)
// INSERT INTO abrechnungstraeger (user_id, name, kostenstelle, sort_order) VALUES (?, ?, ?, ?)

static async update(id, userId, updateData)
// UPDATE abrechnungstraeger SET name = ?, kostenstelle = ?, active = ? WHERE id = ? AND user_id = ?
```

From backend/controllers/abrechnungstraegerController.js:
```javascript
exports.createAbrechnungstraeger // reads { name, kostenstelle } from req.body
exports.updateAbrechnungstraeger // reads { name, active, kostenstelle } from req.body
exports.getById // SELECT id, name, kostenstelle, active
```

From backend/migrations/0006_add_kostenstelle.sql (pattern):
```sql
ALTER TABLE abrechnungstraeger ADD COLUMN kostenstelle VARCHAR(255) DEFAULT NULL AFTER name;
```

<!-- Current DB schema for abrechnungstraeger -->
Columns: id, user_id, name, kostenstelle, sort_order, active, created_at
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DB-Migration und Backend-API fuer Farbe-Feld</name>
  <files>
    backend/migrations/0008_add_farbe_to_abrechnungstraeger.sql,
    backend/models/AbrechnungsTraeger.js,
    backend/controllers/abrechnungstraegerController.js
  </files>
  <read_first>
    backend/migrations/0006_add_kostenstelle.sql,
    backend/models/AbrechnungsTraeger.js,
    backend/controllers/abrechnungstraegerController.js
  </read_first>
  <action>
    1. Erstelle Migration `backend/migrations/0008_add_farbe_to_abrechnungstraeger.sql`:
       ```sql
       ALTER TABLE abrechnungstraeger ADD COLUMN farbe VARCHAR(7) DEFAULT NULL AFTER kostenstelle;
       ```
       VARCHAR(7) fuer Hex-Werte wie #10b981. DEFAULT NULL = kein Fallback in DB, Fallback im Frontend (per D-07).

    2. In `backend/models/AbrechnungsTraeger.js`:
       - `findAllForUser()`: Die Query `SELECT at.*` holt bereits alle Spalten, daher keine Aenderung noetig — farbe kommt automatisch mit.
       - `create()`: INSERT erweitern um farbe:
         ```javascript
         'INSERT INTO abrechnungstraeger (user_id, name, kostenstelle, farbe, sort_order) VALUES (?, ?, ?, ?, ?)',
         [userData.userId, userData.name, userData.kostenstelle || null, userData.farbe || null, userData.sortOrder]
         ```
       - `update()`: UPDATE erweitern um farbe:
         ```javascript
         'UPDATE abrechnungstraeger SET name = ?, kostenstelle = ?, farbe = ?, active = ? WHERE id = ? AND user_id = ?',
         [updateData.name, updateData.kostenstelle !== undefined ? updateData.kostenstelle : null, updateData.farbe || null, updateData.active, id, userId]
         ```

    3. In `backend/controllers/abrechnungstraegerController.js`:
       - `createAbrechnungstraeger`: Destructure `farbe` aus `req.body`, uebergebe an `AbrechnungsTraeger.create()`:
         ```javascript
         const { name, kostenstelle, farbe } = req.body;
         // ... im create-Aufruf:
         const id = await AbrechnungsTraeger.create({ userId: req.user.id, name, kostenstelle, farbe, sortOrder: nextSortOrder });
         ```
       - `updateAbrechnungstraeger`: Im vollstaendigen Update-Block `farbe` aus `req.body` lesen und an `update()` uebergeben:
         ```javascript
         const { name, active, kostenstelle, farbe } = req.body;
         // ... im update-Aufruf:
         await AbrechnungsTraeger.update(id, req.user.id, { name, kostenstelle, farbe, active: active !== undefined ? active : 1 });
         ```
       - `getById`: Query erweitern um farbe:
         ```javascript
         'SELECT id, name, kostenstelle, farbe, active FROM abrechnungstraeger WHERE id = ? AND user_id = ?'
         ```
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Fahrtenbuch && grep -q "farbe" backend/migrations/0008_add_farbe_to_abrechnungstraeger.sql && grep -q "farbe" backend/models/AbrechnungsTraeger.js && grep -q "farbe" backend/controllers/abrechnungstraegerController.js && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - backend/migrations/0008_add_farbe_to_abrechnungstraeger.sql enthaelt "ADD COLUMN farbe VARCHAR(7)"
    - AbrechnungsTraeger.js create() enthaelt "userData.farbe"
    - AbrechnungsTraeger.js update() enthaelt "updateData.farbe"
    - abrechnungstraegerController.js createAbrechnungstraeger enthaelt "farbe" in req.body destructuring
    - abrechnungstraegerController.js getById Query enthaelt "farbe"
  </acceptance_criteria>
  <done>farbe-Feld ist in DB-Migration, Model und Controller integriert. API gibt farbe zurueck und akzeptiert farbe bei create/update.</done>
</task>

<task type="auto">
  <name>Task 2: Farbwahl-Widget in AbrechnungstraegerForm</name>
  <files>frontend/src/components/AbrechnungstraegerForm.js</files>
  <read_first>frontend/src/components/AbrechnungstraegerForm.js</read_first>
  <action>
    Per D-04: Farbe pro Abrechnungstraeger in den Einstellungen definierbar machen.

    1. Vordefinierte Farbpalette als Konstante (Claude's Discretion: vordefinierte Palette statt freier Hex-Input):
       ```javascript
       const FARB_PALETTE = [
         { hex: '#10b981', name: 'Gruen' },
         { hex: '#3b82f6', name: 'Blau' },
         { hex: '#8b5cf6', name: 'Violett' },
         { hex: '#f59e0b', name: 'Gelb' },
         { hex: '#ef4444', name: 'Rot' },
         { hex: '#ec4899', name: 'Pink' },
         { hex: '#06b6d4', name: 'Cyan' },
         { hex: '#6366f1', name: 'Indigo' },
       ];
       ```

    2. Im "Hinzufuegen"-Formular (`handleAddNew` Bereich):
       - `newEntry` State erweitern: `{ name: '', kostenstelle: '', farbe: '#10b981' }`
       - Nach dem Kostenstelle-Input ein Farbwahl-Widget einfuegen:
         ```jsx
         <div className="w-full">
           <label className="form-label">Farbe</label>
           <div className="flex flex-wrap gap-2">
             {FARB_PALETTE.map(f => (
               <button
                 key={f.hex}
                 type="button"
                 onClick={() => setNewEntry({ ...newEntry, farbe: f.hex })}
                 className={`w-8 h-8 rounded-full border-2 transition-all duration-150 ${
                   newEntry.farbe === f.hex ? 'border-gray-800 dark:border-white scale-110' : 'border-transparent'
                 }`}
                 style={{ backgroundColor: f.hex }}
                 title={f.name}
               />
             ))}
           </div>
         </div>
         ```
       - Im `handleAddNew` axios-Call `farbe` mitsenden:
         ```javascript
         await axios.post('/api/abrechnungstraeger', {
           ...newEntry,
           sort_order: abrechnungstraeger.length + 1
         });
         ```
         (newEntry enthaelt bereits farbe, wird also mitgesendet)

    3. Im Edit-Formular (editingTraeger Block):
       - Nach dem Kostenstelle-Input-Feld das gleiche Farbwahl-Widget einfuegen, aber mit `editingTraeger.farbe` als Wert:
         ```jsx
         <div className="w-full">
           <label className="form-label">Farbe</label>
           <div className="flex flex-wrap gap-2">
             {FARB_PALETTE.map(f => (
               <button
                 key={f.hex}
                 type="button"
                 onClick={() => setEditingTraeger({ ...editingTraeger, farbe: f.hex })}
                 className={`w-8 h-8 rounded-full border-2 transition-all duration-150 ${
                   editingTraeger.farbe === f.hex ? 'border-gray-800 dark:border-white scale-110' : 'border-transparent'
                 }`}
                 style={{ backgroundColor: f.hex }}
                 title={f.name}
               />
             ))}
           </div>
         </div>
         ```
       - Im `handleUpdate` axios-Call `farbe` mitsenden:
         ```javascript
         await axios.put(`/api/abrechnungstraeger/${editingTraeger.id}`, {
           name: editingTraeger.name,
           kostenstelle: editingTraeger.kostenstelle,
           farbe: editingTraeger.farbe,
         });
         ```

    4. In der Listenansicht (nicht-Edit-Modus): Einen kleinen Farbpunkt vor dem Namen anzeigen:
       ```jsx
       <div className="flex items-center gap-2">
         {traeger.farbe && (
           <span className="w-3 h-3 rounded-full shrink-0" style={{ backgroundColor: traeger.farbe }} />
         )}
         <span className="font-medium text-value">
           {traeger.name}{traeger.kostenstelle ? ` — Kst.: ${traeger.kostenstelle}` : ''}
         </span>
       </div>
       ```
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Fahrtenbuch && grep -q "FARB_PALETTE" frontend/src/components/AbrechnungstraegerForm.js && grep -q "farbe" frontend/src/components/AbrechnungstraegerForm.js && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - AbrechnungstraegerForm.js enthaelt FARB_PALETTE Array mit mindestens 6 Farben
    - newEntry State enthaelt farbe Property
    - Farbwahl-Buttons mit rounded-full und style={{ backgroundColor: f.hex }} vorhanden
    - handleUpdate sendet farbe im axios.put Body
    - Listenansicht zeigt Farbpunkt (w-3 h-3 rounded-full) vor Traeger-Name
  </acceptance_criteria>
  <done>Nutzer koennen beim Erstellen und Bearbeiten eines Abrechnungstraegers eine Farbe aus einer Palette waehlen. Die Farbe wird als Farbpunkt in der Liste angezeigt.</done>
</task>

</tasks>

<verification>
- Migration-Datei existiert mit korrektem ALTER TABLE
- Backend Model und Controller verarbeiten farbe-Feld
- AbrechnungstraegerForm zeigt Farbwahl-Widget
- Farbe wird bei Create und Update mitgesendet
</verification>

<success_criteria>
- farbe VARCHAR(7) Spalte in abrechnungstraeger Tabelle definiert
- API-Endpoints akzeptieren und liefern farbe
- Einstellungen-UI hat funktionale Farbwahl mit 8 Farben
</success_criteria>

<output>
After completion, create `.planning/phases/21-monatsuebersicht-polish/21-01-SUMMARY.md`
</output>
