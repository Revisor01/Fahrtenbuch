---
phase: 21-monatsuebersicht-polish
plan: 03
type: execute
wave: 2
depends_on: [21-01]
files_modified:
  - frontend/src/index.css
  - frontend/src/components/MonthlyOverview.js
autonomous: true
requirements: [D-08, D-09, D-10, D-11, D-12]

must_haves:
  truths:
    - "Status-Buttons (Nicht eingereicht, Eingereicht am, Erhalten am) sehen wie echte Buttons aus mit Hover-State, Padding und Border"
    - "Farbliche Unterscheidung: Nicht-eingereicht=Warnung, Eingereicht=Info, Erhalten=Erfolg"
    - "Abbrechen-Button im AbrechnungsStatusModal erscheint als vollwertiger Button mit Background und Border"
  artifacts:
    - path: "frontend/src/index.css"
      provides: "Ueberarbeitete status-badge und btn-secondary Klassen"
      contains: "status-badge"
    - path: "frontend/src/components/MonthlyOverview.js"
      provides: "Ueberarbeitete renderStatusCell mit Button-Styling"
      contains: "renderStatusCell"
  key_links:
    - from: "frontend/src/components/MonthlyOverview.js renderStatusCell"
      to: "frontend/src/index.css status-badge-*"
      via: "CSS-Klassen"
      pattern: "status-badge"
---

<objective>
Status-Buttons in der Monatsuebersicht und Abbrechen-Button im Modal als richtige, gestylte Buttons umgestalten.

Purpose: Die Status-Badges sehen wie echte interaktive Buttons aus (per D-08 bis D-10), und der Abbrechen-Button im Modal ist ein vollwertiger Button (per D-11, D-12).
Output: Ueberarbeitete CSS-Klassen und renderStatusCell in MonthlyOverview.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-monatsuebersicht-polish/21-CONTEXT.md

<interfaces>
<!-- Current CSS classes in index.css -->
From frontend/src/index.css:
```css
.status-badge {
  @apply inline-flex items-center gap-2
         px-2 py-0.5 rounded-full 
         text-xs font-medium;
}

.status-badge-primary {
  @apply status-badge
         bg-primary-100 dark:bg-primary-900
         text-primary-700 dark:text-primary-300;
}

.status-badge-secondary {
  @apply status-badge
         bg-secondary-100 dark:bg-secondary-900
         text-secondary-700 dark:text-secondary-300;
}

.btn-secondary {
  @apply bg-primary-100 dark:bg-primary-800
         text-primary-700 dark:text-primary-200
         px-4 h-8 rounded-card
         hover:bg-primary-200 dark:hover:bg-primary-700
         transition-all duration-200
         text-sm whitespace-nowrap
         disabled:opacity-50 disabled:cursor-not-allowed;
}
```

<!-- renderStatusCell uses these classes: -->
- "Keine Abrechnung": status-badge-secondary + CheckCircle2 icon
- "Erhalten am": status-badge-primary + CheckCircle2 icon, cursor-pointer
- "Eingereicht am": status-badge-secondary + Circle icon, cursor-pointer
- "Nicht eingereicht": status-badge-secondary + AlertCircle icon, cursor-pointer
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Status-Badge CSS zu echten Buttons umgestalten</name>
  <files>frontend/src/index.css</files>
  <read_first>frontend/src/index.css</read_first>
  <action>
    **D-08: Status-Badges an Card-Design anpassen**
    **D-09: Wie echte Buttons mit Hover-State, Padding, Border**
    **D-10: Farbliche Unterscheidung beibehalten**
    **D-11, D-12: btn-secondary als vollwertiger Button**

    1. **Status-Badge Klassen ueberarbeiten:**
       Ersetze die drei bestehenden status-badge Klassen durch button-artige Varianten:

       ```css
       .status-badge {
         @apply inline-flex items-center gap-2
                px-3 py-1.5 rounded-card
                text-xs font-medium
                border
                transition-all duration-200;
       }

       /* Erhalten = Erfolg (gruen) */
       .status-badge-primary {
         @apply status-badge
                bg-emerald-50 dark:bg-emerald-900/30
                text-emerald-700 dark:text-emerald-300
                border-emerald-200 dark:border-emerald-700
                hover:bg-emerald-100 dark:hover:bg-emerald-800/40
                hover:shadow-sm;
       }

       /* Eingereicht = Info (blau) */
       .status-badge-info {
         @apply status-badge
                bg-blue-50 dark:bg-blue-900/30
                text-blue-700 dark:text-blue-300
                border-blue-200 dark:border-blue-700
                hover:bg-blue-100 dark:hover:bg-blue-800/40
                hover:shadow-sm;
       }

       /* Nicht eingereicht = Warnung (amber/gelb) */
       .status-badge-warning {
         @apply status-badge
                bg-amber-50 dark:bg-amber-900/30
                text-amber-700 dark:text-amber-300
                border-amber-200 dark:border-amber-700
                hover:bg-amber-100 dark:hover:bg-amber-800/40
                hover:shadow-sm;
       }

       /* Keine Abrechnung = neutral */
       .status-badge-secondary {
         @apply status-badge
                bg-gray-50 dark:bg-gray-800
                text-gray-500 dark:text-gray-400
                border-gray-200 dark:border-gray-700;
       }
       ```

    2. **btn-secondary aufwerten (per D-11, D-12):**
       Ersetze die bestehende btn-secondary Klasse durch eine Version mit klarem Border und Background:

       ```css
       .btn-secondary {
         @apply bg-white dark:bg-primary-800
                text-primary-700 dark:text-primary-200
                px-4 h-10 rounded-card
                border border-primary-200 dark:border-primary-600
                hover:bg-primary-50 dark:hover:bg-primary-700
                hover:border-primary-300 dark:hover:border-primary-500
                transition-all duration-200
                text-sm whitespace-nowrap font-medium
                disabled:opacity-50 disabled:cursor-not-allowed;
       }
       ```
       Aenderungen gegenueber aktuell:
       - h-8 -> h-10 (groessere Touch-Target)
       - bg-primary-100 -> bg-white (klarer sichtbar als Button)
       - border hinzugefuegt (border-primary-200)
       - hover:border-primary-300 fuer besseren Hover-Feedback
       - font-medium hinzugefuegt
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Fahrtenbuch && grep -q "status-badge-warning" frontend/src/index.css && grep -q "status-badge-info" frontend/src/index.css && grep -q "border border-primary-200" frontend/src/index.css && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - index.css enthaelt status-badge-primary mit bg-emerald und border-emerald
    - index.css enthaelt status-badge-info mit bg-blue und border-blue
    - index.css enthaelt status-badge-warning mit bg-amber und border-amber
    - index.css enthaelt status-badge-secondary mit bg-gray und border-gray
    - Alle status-badge Varianten haben hover:shadow-sm oder hover-Effekte (ausser secondary)
    - Base .status-badge hat px-3 py-1.5 rounded-card border
    - btn-secondary hat border border-primary-200 und bg-white
    - btn-secondary hat h-10 (nicht h-8)
  </acceptance_criteria>
  <done>Status-Badges sehen wie richtige Buttons aus mit Border, Padding, Hover-State und farblicher Unterscheidung. btn-secondary ist ein vollwertiger Button mit Border und Background.</done>
</task>

<task type="auto">
  <name>Task 2: renderStatusCell auf neue Badge-Klassen umstellen</name>
  <files>frontend/src/components/MonthlyOverview.js</files>
  <read_first>frontend/src/components/MonthlyOverview.js</read_first>
  <action>
    **D-08, D-09, D-10: Status-Buttons in renderStatusCell aktualisieren**

    In `renderStatusCell()` (ca. Zeile 240-324) die CSS-Klassen auf die neuen Varianten umstellen:

    1. **"Keine Abrechnung" (betrag === 0):**
       Aendere `status-badge-secondary` — bleibt, da neutral korrekt.

    2. **"Erhalten am" (status?.erhalten_am):**
       Bleibt `status-badge-primary` — jetzt gruen (Erfolg) per D-10.

    3. **"Eingereicht am" (status?.eingereicht_am):**
       Aendere `status-badge-secondary` zu `status-badge-info`:
       ```jsx
       <span
         className="status-badge-info cursor-pointer min-h-[44px] flex items-center"
         onClick={() => setAbrechnungsStatusModal({...})}
       >
       ```

    4. **"Nicht eingereicht" (betrag > 0):**
       Aendere `status-badge-secondary` zu `status-badge-warning`:
       ```jsx
       <span
         className="status-badge-warning cursor-pointer min-h-[44px] flex items-center"
         onClick={() => setAbrechnungsStatusModal({...})}
       >
       ```

    Zusammenfassung der Klassen-Zuordnung:
    - Keine Abrechnung: `status-badge-secondary` (grau, nicht klickbar)
    - Erhalten am: `status-badge-primary` (gruen, klickbar)
    - Eingereicht am: `status-badge-info` (blau, klickbar)
    - Nicht eingereicht: `status-badge-warning` (amber, klickbar)
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Fahrtenbuch && grep -q "status-badge-warning" frontend/src/components/MonthlyOverview.js && grep -q "status-badge-info" frontend/src/components/MonthlyOverview.js && grep -q "status-badge-primary" frontend/src/components/MonthlyOverview.js && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - renderStatusCell verwendet status-badge-warning fuer "Nicht eingereicht"
    - renderStatusCell verwendet status-badge-info fuer "Eingereicht am"
    - renderStatusCell verwendet status-badge-primary fuer "Erhalten am"
    - renderStatusCell verwendet status-badge-secondary fuer "Keine Abrechnung"
    - Alle klickbaren Badges haben cursor-pointer und min-h-[44px]
  </acceptance_criteria>
  <done>renderStatusCell nutzt die neuen farblich differenzierten Badge-Klassen: Warnung=amber, Info=blau, Erfolg=gruen, Neutral=grau.</done>
</task>

</tasks>

<verification>
- Status-Badges haben Button-Styling mit Border, Padding und Hover
- Farbliche Unterscheidung: Warnung(amber), Info(blau), Erfolg(gruen)
- btn-secondary hat sichtbaren Border und Background
- renderStatusCell nutzt die korrekten neuen Klassen
</verification>

<success_criteria>
- Alle Status-Badges in der Monatsuebersicht sehen wie echte Buttons aus
- Farbliche Differenzierung ist klar erkennbar
- Abbrechen-Button im Modal ist ein vollwertiger Button (via ueberarbeiteter btn-secondary)
</success_criteria>

<output>
After completion, create `.planning/phases/21-monatsuebersicht-polish/21-03-SUMMARY.md`
</output>
