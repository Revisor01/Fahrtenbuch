---
phase: 07-code-qualitaet
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/package.json
  - backend/package-lock.json
  - backend/middleware/validate.js
  - backend/schemas/authSchemas.js
  - backend/schemas/fahrtSchemas.js
  - backend/schemas/ortSchemas.js
  - backend/schemas/distanzSchemas.js
  - backend/schemas/abrechnungstraegerSchemas.js
  - backend/schemas/userSchemas.js
  - backend/schemas/profileSchemas.js
  - backend/schemas/apiKeySchemas.js
  - backend/schemas/mitfahrerErstattungSchemas.js
  - backend/routes/auth.js
  - backend/routes/fahrten.js
  - backend/routes/orte.js
  - backend/routes/distanzen.js
  - backend/routes/abrechnungstraeger.js
  - backend/routes/users.js
  - backend/routes/profile.js
  - backend/routes/apiKeys.js
  - backend/routes/mitfahrerErstattung.js
autonomous: true
requirements:
  - QUAL-01

must_haves:
  truths:
    - "Alle POST/PUT-Endpoints validieren req.body mit Zod-Schema bevor Controller-Logik laeuft"
    - "Ungueltige Eingaben liefern 400 mit konkreter Fehlerbeschreibung (Feldname + Erwartung)"
    - "Gueltige Eingaben passieren die Validierung und werden normal verarbeitet"
  artifacts:
    - path: "backend/middleware/validate.js"
      provides: "Zod-Validierungs-Middleware"
      exports: ["validate"]
    - path: "backend/schemas/fahrtSchemas.js"
      provides: "Zod-Schemas fuer Fahrten-Endpoints"
    - path: "backend/schemas/authSchemas.js"
      provides: "Zod-Schemas fuer Auth-Endpoints"
  key_links:
    - from: "backend/routes/*.js"
      to: "backend/middleware/validate.js"
      via: "validate(schema) Middleware vor Controller"
      pattern: "validate\\(.*Schema\\)"
    - from: "backend/middleware/validate.js"
      to: "zod"
      via: "schema.parse(req.body)"
      pattern: "schema\\.parse"
---

<objective>
Input-Validierung mit Zod fuer alle Backend-Endpoints einfuehren.

Purpose: Alle Benutzereingaben werden vor der Controller-Logik validiert — ungueltige Daten erreichen nie die Datenbank.
Output: Zod-Schemas fuer alle Endpoints, validate-Middleware, Routes mit Validierung.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-code-qualitaet/07-CONTEXT.md
@backend/routes/fahrten.js
@backend/routes/auth.js
@backend/routes/orte.js
@backend/routes/distanzen.js
@backend/routes/abrechnungstraeger.js
@backend/routes/users.js
@backend/routes/profile.js
@backend/routes/apiKeys.js
@backend/routes/mitfahrerErstattung.js
@backend/controllers/fahrtController.js
@backend/controllers/authController.js
@backend/controllers/ortController.js
@backend/controllers/distanzController.js
@backend/controllers/abrechnungstraegerController.js
@backend/controllers/userController.js
@backend/controllers/profileController.js
@backend/controllers/apiKeyController.js
@backend/controllers/mitfahrerErstattungController.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Zod installieren, validate-Middleware und alle Schemas erstellen</name>
  <files>
    backend/package.json
    backend/middleware/validate.js
    backend/schemas/authSchemas.js
    backend/schemas/fahrtSchemas.js
    backend/schemas/ortSchemas.js
    backend/schemas/distanzSchemas.js
    backend/schemas/abrechnungstraegerSchemas.js
    backend/schemas/userSchemas.js
    backend/schemas/profileSchemas.js
    backend/schemas/apiKeySchemas.js
    backend/schemas/mitfahrerErstattungSchemas.js
  </files>
  <read_first>
    backend/controllers/fahrtController.js
    backend/controllers/authController.js
    backend/controllers/ortController.js
    backend/controllers/distanzController.js
    backend/controllers/abrechnungstraegerController.js
    backend/controllers/userController.js
    backend/controllers/profileController.js
    backend/controllers/apiKeyController.js
    backend/controllers/mitfahrerErstattungController.js
  </read_first>
  <action>
    1. `cd backend && npm install zod`

    2. Erstelle `backend/middleware/validate.js`:
       - Exportiere `validate(schema)` — eine Express-Middleware-Factory
       - Ruft `schema.parse(req.body)` auf
       - Bei ZodError: 400 mit `{ message: "Validierungsfehler", errors: formatted }` zurueckgeben
       - `errors` ist ein Array mit `{ field, message }` Objekten (aus `error.errors` gemappt)
       - Bei Erfolg: `req.body` mit dem geparsten (und ggf. transformierten) Wert ueberschreiben, `next()` aufrufen

    3. Erstelle Schema-Dateien in `backend/schemas/`. Fuer jeden Controller die `req.body`-Felder aus dem destructuring lesen und als Zod-Schema abbilden.

    Konkrete Schemas (aus den Controllern abgeleitet):

    **authSchemas.js:**
    - `loginSchema`: { username: z.string().min(1), password: z.string().min(1) }
    - `registerSchema`: { username: z.string().min(3).max(50), password: z.string().min(6), email: z.string().email(), fullName: z.string().min(1), registrationCode: z.string().optional() }
    - `forgotPasswordSchema`: { email: z.string().email() }
    - `resetPasswordSchema`: { token: z.string().min(1), password: z.string().min(6) }
    - `changePasswordSchema`: { currentPassword: z.string().min(1), newPassword: z.string().min(6) }

    **fahrtSchemas.js:**
    - `createFahrtSchema`: { vonOrtId: z.number().int().positive().nullable().optional(), nachOrtId: z.number().int().positive().nullable().optional(), datum: z.string().min(1), anlass: z.string().min(1), kilometer: z.number().positive().optional().nullable(), abrechnung: z.number().int().positive(), einmaligerVonOrt: z.string().optional().nullable(), einmaligerNachOrt: z.string().optional().nullable(), mitfahrer: z.array(z.object({ name: z.string().min(1), pipirichtung: z.enum(["hin", "rueck", "hin_rueck"]).optional() })).optional() }
    - `updateFahrtSchema`: gleiche Felder wie create
    - `addMitfahrerSchema`: { name: z.string().min(1), pipirichtung: z.enum(["hin","rueck","hin_rueck"]).optional() }
    - `updateMitfahrerSchema`: gleiche Felder wie addMitfahrer
    - `abrechnungsStatusSchema`: { fahrtIds: z.array(z.number().int().positive()), abrechnpiungsstatus: z.string() }

    **ortSchemas.js:**
    - `createOrtSchema`: { name: z.string().min(1).max(200), adresse: z.string().optional(), istWohnort: z.boolean().optional().default(false), istDienstort: z.boolean().optional().default(false) }
    - `updateOrtSchema`: gleiche Felder

    **distanzSchemas.js:**
    - `createDistanzSchema`: { vonOrtId: z.number().int().positive(), nachOrtId: z.number().int().positive(), kilometer: z.number().positive() }
    - `updateDistanzSchema`: { kilometer: z.number().positive() }

    **abrechnungstraegerSchemas.js:**
    - `createAbrechnungstraegerSchema`: { name: z.string().min(1).max(200), kostenstelle: z.string().optional(), erstattungsSatz: z.number().positive().optional() }
    - `updateAbrechnungstraegerSchema`: gleiche Felder
    - `updateErstattungSchema`: { erstattungsSatz: z.number().min(0) }

    **userSchemas.js:**
    - `updateUserSchema`: { username: z.string().min(3).max(50).optional(), role: z.enum(["admin","user"]).optional(), isActive: z.boolean().optional() }

    **profileSchemas.js:**
    - `updateProfileSchema`: { fullName: z.string().min(1).optional(), email: z.string().email().optional(), kennzeichen: z.string().optional(), dienstbezeichnung: z.string().optional(), dienststelle: z.string().optional(), abteilung: z.string().optional(), behoerde: z.string().optional(), anschrift: z.string().optional(), telefon: z.string().optional() }

    **apiKeySchemas.js:**
    - `createApiKeySchema`: { name: z.string().min(1).max(100) }

    **mitfahrerErstattungSchemas.js:**
    - `updateErstattungSchema`: { erstattungsSatz: z.number().min(0) }

    WICHTIG: Bei allen Schemas `.strict()` NICHT verwenden — die Controller nutzen teilweise Felder die nicht im Body kommen (z.B. userId aus req.user). Stattdessen einfach die Body-Felder validieren.

    Fuer Number-Felder die als String aus dem Frontend kommen koennten: `z.coerce.number()` statt `z.number()` verwenden.
  </action>
  <verify>
    <automated>cd backend && node -e "const v = require('./middleware/validate'); const z = require('zod'); const s = z.object({x: z.string()}); const m = v.validate(s); console.log(typeof m === 'function' ? 'OK' : 'FAIL')"</automated>
  </verify>
  <acceptance_criteria>
    - Zod ist in backend/package.json als Dependency
    - validate.js exportiert validate(schema)-Funktion
    - Schema-Dateien existieren fuer alle 9 Controller
    - Alle Schemas spiegeln die tatsaechlichen req.body-Felder der Controller wider
  </acceptance_criteria>
  <done>Zod installiert, validate-Middleware erstellt, alle Schema-Dateien vorhanden</done>
</task>

<task type="auto">
  <name>Task 2: Validierungs-Middleware in alle Routes einbauen</name>
  <files>
    backend/routes/auth.js
    backend/routes/fahrten.js
    backend/routes/orte.js
    backend/routes/distanzen.js
    backend/routes/abrechnungstraeger.js
    backend/routes/users.js
    backend/routes/profile.js
    backend/routes/apiKeys.js
    backend/routes/mitfahrerErstattung.js
  </files>
  <read_first>
    backend/middleware/validate.js
    backend/schemas/authSchemas.js
    backend/schemas/fahrtSchemas.js
    backend/schemas/ortSchemas.js
    backend/schemas/distanzSchemas.js
    backend/schemas/abrechnungstraegerSchemas.js
    backend/schemas/userSchemas.js
    backend/schemas/profileSchemas.js
    backend/schemas/apiKeySchemas.js
    backend/schemas/mitfahrerErstattungSchemas.js
    backend/routes/auth.js
    backend/routes/fahrten.js
    backend/routes/orte.js
    backend/routes/distanzen.js
    backend/routes/abrechnungstraeger.js
    backend/routes/users.js
    backend/routes/profile.js
    backend/routes/apiKeys.js
    backend/routes/mitfahrerErstattung.js
  </read_first>
  <action>
    In jeder Route-Datei:
    1. Schema-Datei und validate-Middleware importieren
    2. Bei POST- und PUT-Routen: `validate(schema)` als Middleware VOR dem Controller einfuegen

    Beispiel fuer fahrten.js:
    ```javascript
    const { validate } = require('../middleware/validate');
    const { createFahrtSchema, updateFahrtSchema, addMitfahrerSchema, updateMitfahrerSchema, abrechnungsStatusSchema } = require('../schemas/fahrtSchemas');

    router.post('/', validate(createFahrtSchema), fahrtController.createFahrt);
    router.put('/:id', validate(updateFahrtSchema), fahrtController.updateFahrt);
    router.post('/:fahrtId/mitfahrer', validate(addMitfahrerSchema), fahrtController.addMitfahrer);
    router.put('/:fahrtId/mitfahrer/:mitfahrerId', validate(updateMitfahrerSchema), fahrtController.updateMitfahrer);
    router.post('/abrechnungsstatus', validate(abrechnungsStatusSchema), fahrtController.updateAbrechnungsStatus);
    ```

    GET- und DELETE-Routen brauchen KEINE Body-Validierung (haben keinen req.body).

    Dieses Muster auf alle 9 Route-Dateien anwenden. Nur Routen mit POST oder PUT bekommen validate().
  </action>
  <verify>
    <automated>cd backend && node -e "require('./routes/fahrten'); require('./routes/auth'); require('./routes/orte'); require('./routes/distanzen'); require('./routes/abrechnungstraeger'); require('./routes/users'); require('./routes/profile'); require('./routes/apiKeys'); require('./routes/mitfahrerErstattung'); console.log('All routes load OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Alle POST/PUT-Routen haben validate(schema) Middleware
    - GET/DELETE-Routen bleiben unveraendert
    - Alle Route-Dateien laden ohne Fehler
    - Ungueltige Bodies liefern 400 mit Feldbeschreibung
  </acceptance_criteria>
  <done>Alle POST/PUT-Endpoints validieren Eingaben mit Zod-Schema via Middleware</done>
</task>

</tasks>

<verification>
- `cd backend && npm start` startet ohne Fehler
- POST an /api/fahrten mit leerem Body liefert 400 mit Validierungsfehler
- POST an /api/auth/login mit { username: "", password: "" } liefert 400
- Normaler Login mit gueltigen Daten funktioniert weiterhin
</verification>

<success_criteria>
Alle Backend POST/PUT-Endpoints validieren Eingaben mit Zod-Schemas und geben bei Fehlern 400 mit konkreter Fehlerbeschreibung zurueck. Gueltige Requests werden normal verarbeitet.
</success_criteria>

<output>
After completion, create `.planning/phases/07-code-qualitaet/07-01-SUMMARY.md`
</output>
