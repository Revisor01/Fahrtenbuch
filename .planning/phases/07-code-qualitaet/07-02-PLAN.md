---
phase: 07-code-qualitaet
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/controllers/authController.js
  - backend/app.js
  - backend/initDb.js
  - backend/scripts/update-template.js
  - backend/utils/Migrator.js
  - frontend/src/App.js
  - backend/package.json
  - backend/package-lock.json
  - frontend/package.json
  - frontend/package-lock.json
autonomous: true
requirements:
  - QUAL-02
  - QUAL-03

must_haves:
  truths:
    - "Backend-Code enthaelt keine console.log-Statements ausser console.error fuer echte Fehler"
    - "Frontend-Code enthaelt keine debug console.log-Statements"
    - "npm audit zeigt keine fixbaren Vulnerabilities mehr an (ausser react-scripts transitive deps)"
  artifacts:
    - path: "backend/controllers/authController.js"
      provides: "Auth-Controller ohne Debug-Logs"
    - path: "backend/package.json"
      provides: "Aktualisierte Backend-Dependencies"
    - path: "frontend/package.json"
      provides: "Aktualisierte Frontend-Dependencies"
  key_links: []
---

<objective>
Debug-Logs entfernen und Dependencies aktualisieren.

Purpose: Production-Code ist frei von Debug-Output, bekannte Vulnerabilities sind gefixt.
Output: Bereinigte Logfiles, aktualisierte package.json und package-lock.json.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-code-qualitaet/07-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Debug-Logs aus Backend und Frontend entfernen</name>
  <files>
    backend/controllers/authController.js
    backend/app.js
    backend/initDb.js
    backend/scripts/update-template.js
    backend/utils/Migrator.js
    frontend/src/App.js
  </files>
  <read_first>
    backend/controllers/authController.js
    backend/app.js
    backend/initDb.js
    backend/scripts/update-template.js
    backend/utils/Migrator.js
    frontend/src/App.js
  </read_first>
  <action>
    Durchsuche alle Dateien mit `grep -rn "console.log" backend/ frontend/src/` und entferne/ersetze:

    **Entfernen (Debug-Output):**
    - `console.log('Login attempt:', ...)` in authController.js
    - `console.log('User not found')` in authController.js
    - `console.log('Password match:', ...)` in authController.js
    - Alle weiteren `console.log` die Debug-Informationen ausgeben (Variablenwerte, Flow-Marker)
    - Frontend `console.log` in App.js (2 Stellen)

    **Behalten:**
    - `console.error(...)` — das sind echte Fehler-Logs, bleiben
    - `console.log('Server laeuft auf Port...')` in app.js — Startup-Info, behalten

    **Migrator.js / initDb.js / update-template.js:**
    - `console.log` die Migrations-Fortschritt oder DB-Init-Status anzeigen: Diese in `console.info` umwandeln (semantisch korrekt fuer Info-Output)
    - Debug-Logs (Variablenwerte, "entering function X") entfernen

    Regel: Im Zweifel entfernen. Wenn ein Log fuer Production-Monitoring genuinely noetig ist, `console.info` verwenden.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Fahrtenbuch && grep -rn "console\.log" backend/controllers/ frontend/src/ --include="*.js" | grep -v node_modules | grep -v "console\.log('Server" || echo "No debug logs found - OK"</automated>
  </verify>
  <acceptance_criteria>
    - Keine console.log in backend/controllers/
    - Keine console.log in frontend/src/ (ausser genuines Info-Logging)
    - console.error Statements bleiben erhalten
    - App startet und funktioniert weiterhin
  </acceptance_criteria>
  <done>Alle Debug-Logs entfernt, nur console.error und console.info fuer echte Fehler/Info behalten</done>
</task>

<task type="auto">
  <name>Task 2: Dependency-Updates und npm audit fix</name>
  <files>
    backend/package.json
    backend/package-lock.json
    frontend/package.json
    frontend/package-lock.json
  </files>
  <read_first>
    backend/package.json
    frontend/package.json
  </read_first>
  <action>
    WICHTIG: CRA → Vite Migration ist OUT OF SCOPE. Viele Dependabot-Alerts kommen von react-scripts transitive Dependencies — diese sind nur durch CRA-Entfernung fixbar und werden ignoriert.

    1. **Backend Dependencies:**
       ```bash
       cd backend
       npm audit
       npm audit fix
       ```
       Falls `npm audit fix` breaking changes vorschlaegt: NUR non-breaking Updates durchfuehren.
       Nach jedem Update: `node -e "require('./app')"` testen dass der Import-Graph intakt ist.

    2. **Frontend Dependencies:**
       ```bash
       cd frontend
       npm audit
       npm audit fix
       ```
       Gleiche Strategie: nur non-breaking. NICHT `npm audit fix --force` ausfuehren — das kann react-scripts zerstoeren.

    3. **Verbleibende Alerts dokumentieren:**
       Nach den Fixes: `npm audit` in beiden Verzeichnissen ausfuehren.
       Alerts die von react-scripts kommen notieren — diese werden in v1.2 (CRA → Vite) gefixt.

    4. **Build testen:**
       ```bash
       cd frontend && npm run build
       ```
       Build muss durchlaufen. Falls Build bricht: letztes Update rueckgaengig machen.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Fahrtenbuch/backend && npm audit --omit=dev 2>&1 | tail -5 && cd ../frontend && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - npm audit fix wurde in backend und frontend ausgefuehrt
    - Backend startet ohne Fehler
    - Frontend Build laeuft durch
    - Verbleibende Alerts sind nur react-scripts transitive deps (CRA, out of scope)
  </acceptance_criteria>
  <done>Fixbare Vulnerabilities behoben, Frontend baut erfolgreich, verbleibende Alerts sind react-scripts-bedingt und fuer v1.2 vorgemerkt</done>
</task>

</tasks>

<verification>
- `cd backend && node -e "require('./app')"` laeuft ohne Fehler
- `cd frontend && npm run build` laeuft durch
- `grep -rn "console.log" backend/controllers/` liefert 0 Treffer
- `cd backend && npm audit --omit=dev` zeigt 0 oder nur nicht-fixbare Alerts
</verification>

<success_criteria>
Production-Code ist frei von Debug-Logs. Dependencies sind auf aktuellem Stand, npm audit zeigt keine fixbaren Vulnerabilities. Frontend baut erfolgreich.
</success_criteria>

<output>
After completion, create `.planning/phases/07-code-qualitaet/07-02-SUMMARY.md`
</output>
