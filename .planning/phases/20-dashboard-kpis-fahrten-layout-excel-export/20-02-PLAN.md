---
phase: 20-dashboard-kpis-fahrten-layout-excel-export
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/MonthlyOverview.js
  - backend/utils/excelExport.js
autonomous: false
requirements:
  - SC-05
  - SC-07
  - SC-08
must_haves:
  truths:
    - "Monatsuebersicht Erstattungs-Zusammenfassung sieht aus wie farbige KPI-Cards mit Icons (wie Dashboard)"
    - "Bestehende Funktionalitaet (Abrechnungsstatus, Jahresfilter, Hide-Completed, Quick-Actions) bleibt erhalten"
    - "Excel-Export bewahrt vollstaendig das Layout der 2026-Vorlage (Spaltenbreiten, Borders, Fonts, Merges)"
    - "Excel-Export nutzt korrekt das passende Quartals-Sheet basierend auf dem Export-Zeitraum"
  artifacts:
    - path: "frontend/src/components/MonthlyOverview.js"
      provides: "KPI-Card-Darstellung der Erstattungen"
      contains: "bg-emerald"
    - path: "backend/utils/excelExport.js"
      provides: "Template-treuer Excel-Export"
      contains: "fillQuartalSheet"
  key_links:
    - from: "MonthlyOverview.js getKategorienMitErstattung()"
      to: "KPI-Card JSX"
      via: "map ueber Kategorien mit farbigen Backgrounds und Icons"
      pattern: "bg-emerald|bg-blue|bg-purple"
    - from: "excelExport.js fillQuartalSheet()"
      to: "Template-Worksheet"
      via: "Nur Werte setzen, Styles aus Template bewahren"
      pattern: "getCell.*value"
---

<objective>
Monatsuebersicht-Erstattungen als farbige KPI-Cards darstellen und Excel-Export-Formatierung sicherstellen.

Purpose: Monatsuebersicht bekommt einheitliches Dashboard-Feeling. Excel-Export bewahrt die offizielle 2026-Vorlage korrekt.
Output: MonthlyOverview.js mit KPI-Card-Layout, excelExport.js mit korrekter Template-Nutzung.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-dashboard-kpis-fahrten-layout-excel-export/20-CONTEXT.md

<interfaces>
<!-- Dashboard KPI-Card Pattern fuer Referenz: -->
```jsx
<div className="rounded-card p-4 shadow-card border border-card bg-emerald-50 dark:bg-emerald-900/20">
  <div className="flex items-center justify-between gap-2">
    <div className="min-w-0">
      <p className="text-xs text-muted mb-1">Offene Erstattungen</p>
      <p className="text-xl font-semibold text-value truncate">245,00 &euro;</p>
    </div>
    <Banknote size={22} className="text-emerald-500 shrink-0" />
  </div>
</div>
```

<!-- MonthlyOverview getKategorienMitErstattung() gibt Array von [key, displayName, data] zurueck -->
<!-- data hat {original, ausstehend} fuer Jahresuebersicht und Number fuer Monatsuebersicht -->

<!-- MonthlyOverview Cards aktuell (Zeile 421-435): schlichte card-container ohne Farbe/Icons -->
```jsx
<div key={key} className="card-container">
  <div className="flex justify-between items-center mb-2">
    <span className="text-sm text-label">{displayName}</span>
    <span className="text-value font-medium">{(data.ausstehend || 0).toFixed(2)} €</span>
  </div>
</div>
```

<!-- excelExport.js fillQuartalSheet() setzt nur Werte, keine Styles (Zeile 65-90) -->
<!-- Template-Pfad: backend/templates/fahrtenabrechnung_vorlage.xlsx -->
<!-- QUARTAL_SHEETS: ['Januar-März', 'April-Juni', 'Juli-September', 'Oktober-Dezember'] -->
<!-- getQuartalSheet(month) mappt Monatsnummer auf Quartals-Sheet-Name -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Monatsuebersicht Erstattungen als KPI-Cards mit Farben und Icons</name>
  <files>frontend/src/components/MonthlyOverview.js</files>
  <read_first>
    - frontend/src/components/MonthlyOverview.js
    - frontend/src/components/Dashboard.js (Zeile 192-239 fuer KPI-Card-Pattern)
  </read_first>
  <action>
Aenderungen in MonthlyOverview.js:

1. **Icons importieren (per D-07):**
   Lucide-Imports erweitern um `Banknote, Users, Building2, Wallet`:
   ```js
   import { AlertCircle, Circle, CheckCircle2, Banknote, Users, Building2, Wallet } from 'lucide-react';
   ```

2. **Farb-Mapping fuer Kategorien erstellen:**
   Oben in der Komponente (nach den State-Deklarationen) ein Mapping anlegen:
   ```js
   const kategorieStyles = [
     { bg: 'bg-emerald-50 dark:bg-emerald-900/20', icon: Building2, iconColor: 'text-emerald-500' },
     { bg: 'bg-blue-50 dark:bg-blue-900/20', icon: Wallet, iconColor: 'text-blue-500' },
     { bg: 'bg-purple-50 dark:bg-purple-900/20', icon: Banknote, iconColor: 'text-purple-500' },
     { bg: 'bg-amber-50 dark:bg-amber-900/20', icon: Building2, iconColor: 'text-amber-500' },
   ];
   const mitfahrerStyle = { bg: 'bg-indigo-50 dark:bg-indigo-900/20', icon: Users, iconColor: 'text-indigo-500' };
   ```

3. **Jahresuebersicht Cards (Zeile 411-453) in KPI-Cards umwandeln:**
   Ersetze die schlichten `card-container` Cards durch farbige KPI-Cards im Dashboard-Stil:
   ```jsx
   {getKategorienMitErstattung().map(([key, displayName, data], index) => {
     const style = key === 'mitfahrer' ? mitfahrerStyle : kategorieStyles[index % kategorieStyles.length];
     const IconComponent = style.icon;
     return (
       <div key={key} className={`rounded-card p-4 shadow-card border border-card ${style.bg}`}>
         <div className="flex items-center justify-between gap-2">
           <div className="min-w-0">
             <p className="text-xs text-muted mb-1">{displayName}</p>
             <p className="text-xl font-semibold text-value truncate">
               {(data.ausstehend || 0).toFixed(2).replace('.', ',')} &euro;
             </p>
             {data.original !== data.ausstehend && (
               <p className="text-xs text-muted">
                 Ursprünglich: {(data.original || 0).toFixed(2).replace('.', ',')} &euro;
               </p>
             )}
           </div>
           <IconComponent size={22} className={`${style.iconColor} shrink-0`} />
         </div>
       </div>
     );
   })}
   ```

4. **Monats-Erstattungen Cards (FahrtenListe renderAbrechnungsStatus, Zeile ~486-546) ebenfalls in KPI-Cards:**
   Die Cards in der FahrtenListe-Monatsuebersicht (renderAbrechnungsStatus) muessen das gleiche Pattern bekommen. Hier wird `getKategorienMitErstattung()` der FahrtenListe verwendet, die `[key, displayName, value]` zurueckgibt (value ist Number, nicht Object). Umwandeln:
   ```jsx
   {getKategorienMitErstattung().map(([key, displayName, value], index) => {
     const style = key === 'mitfahrer' ? mitfahrerStyle : kategorieStyles[index % kategorieStyles.length];
     const IconComponent = style.icon;
     return (
       <div key={key} className={`rounded-card p-4 shadow-card border border-card ${style.bg}`}>
         <div className="flex items-center justify-between gap-2">
           <div className="min-w-0">
             <p className="text-xs text-muted mb-1">{displayName}</p>
             <p className="text-xl font-semibold text-value truncate">
               {Number(value).toFixed(2).replace('.', ',')} &euro;
             </p>
           </div>
           <IconComponent size={22} className={`${style.iconColor} shrink-0`} />
         </div>
         {/* Status-Anzeige bleibt wie gehabt */}
         {value > 0 && (
           <div className="text-xs mt-2">...</div>
         )}
       </div>
     );
   })}
   ```
   HINWEIS: Die Status-Logik (Nicht eingereicht / Eingereicht am / Erhalten am) MUSS erhalten bleiben — nur die Card-Optik aendert sich.

5. **Gesamt-Card ebenfalls aufwerten:**
   Die Gesamt-Card bekommt `card-container-highlight` mit einem Icon:
   ```jsx
   <div className="card-container-highlight col-span-1 sm:col-span-2 lg:col-span-full">
     <div className="flex items-center justify-between gap-2">
       <div className="min-w-0">
         <p className="text-xs text-muted mb-1">Gesamt</p>
         <p className="text-xl font-semibold text-value truncate">...</p>
       </div>
       <Banknote size={22} className="text-primary-500 shrink-0" />
     </div>
   </div>
   ```

6. **Bestehende Funktionalitaet bewahren (per D-08):**
   NICHT aendern: `hideCompleted`, `selectedYear`, `QuickActions`, `renderStatusCell`, `setAbrechnungsStatusModal`, Filter-Logik. Nur die Card-Darstellung der Erstattungs-Zusammenfassung wird optisch aufgewertet.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Fahrtenbuch/frontend && grep -n "bg-emerald" src/components/MonthlyOverview.js | head -5 && grep -n "Building2\|Wallet\|Banknote" src/components/MonthlyOverview.js | head -5 && grep -n "hideCompleted" src/components/MonthlyOverview.js | head -3</automated>
  </verify>
  <acceptance_criteria>
    - MonthlyOverview.js enthaelt `bg-emerald-50` (farbige KPI-Cards)
    - MonthlyOverview.js importiert `Building2` oder `Wallet` oder `Banknote` von lucide-react
    - MonthlyOverview.js enthaelt weiterhin `hideCompleted` State-Variable (Funktionalitaet bewahrt)
    - MonthlyOverview.js enthaelt weiterhin `QuickActions` Komponente
    - MonthlyOverview.js enthaelt weiterhin `renderStatusCell` Funktion
    - MonthlyOverview.js enthaelt `kategorieStyles` Array mit bg/icon/iconColor
    - MonthlyOverview.js enthaelt `rounded-card p-4 shadow-card` (KPI-Card-Klassen)
  </acceptance_criteria>
  <done>Monatsuebersicht Erstattungen werden als farbige KPI-Cards mit Icons dargestellt, bestehende Funktionalitaet (Filter, Status, QuickActions) bleibt vollstaendig erhalten.</done>
</task>

<task type="auto">
  <name>Task 2: Excel-Export Template-Formatierung sicherstellen</name>
  <files>backend/utils/excelExport.js</files>
  <read_first>
    - backend/utils/excelExport.js
    - backend/templates/fahrtenabrechnung_vorlage.xlsx (existiert als Template)
  </read_first>
  <action>
Aenderungen in excelExport.js:

1. **fillQuartalSheet: Nur Werte setzen, KEINE Styles ueberschreiben (per D-09):**
   Pruefen dass `fillQuartalSheet()` (Zeile 65-90) KEINE Style-Properties setzt. Aktuell setzt es:
   - `excelRow.getCell('A').numFmt = 'DD.MM.YYYY'` — das ist OK (Zahlenformat)
   - Sonst nur `.value` — das ist korrekt

   ABER: In der Mitfahrer-Sektion (Zeile 171-174) werden Fonts ueberschrieben:
   ```js
   cell.font = { name: 'Arial', size: 10 };
   ```
   Das ueberschreibt die Template-Fonts. Entferne diese Zeilen:
   ```js
   // ENTFERNEN - Template-Styles bewahren:
   // ['A', 'B', 'C', 'D', 'E', 'F', 'G'].forEach(col => {
   //   const cell = row.getCell(col);
   //   cell.font = { name: 'Arial', size: 10 };
   // });
   ```
   Diese Entfernung muss SOWOHL in `exportToExcel` (Zeile 171-174) ALS AUCH in `exportToExcelRange` (Zeile 354-357) erfolgen.

2. **Datum als Date-Objekt statt formatiertem String (per D-09):**
   In `fillQuartalSheet()` Zeile 75-76:
   ```js
   excelRow.getCell('A').value = row.datum;
   excelRow.getCell('A').numFmt = 'DD.MM.YYYY';
   ```
   Hier wird `row.datum` gesetzt. In `formattedData` (Zeile 186-209) gibt es `datum: new Date(fahrt.datum)` und `formattedDatum: formatDate(fahrt.datum)`. In `fillQuartalSheet` wird `row.datum` verwendet — das ist das `formattedDatum` (String "01. Jan") aus der Daten-Vorbereitung. Pruefen: In `fillQuartalSheet` Zeile 75 wird `row.datum` gesetzt. Die Daten die reinkommen haben das Feld `datum` als Date-Objekt UND `formattedDatum` als String. Aber `fillQuartalSheet` ruft nur `row.datum` auf. Da `formattedData` das Feld `datum` als `new Date(fahrt.datum)` setzt (Zeile 191/200), ist `row.datum` ein Date-Objekt. Das ist korrekt fuer `numFmt = 'DD.MM.YYYY'`.

   ABER: `formattedDatum` wird gar nicht verwendet in fillQuartalSheet — es existiert nur in formattedData aber wird nicht an fillQuartalSheet weitergegeben. Das ist korrekt, da fillQuartalSheet das Date-Objekt mit numFmt formatiert.

   Keine Aenderung noetig hier.

3. **Quartals-Sheet korrekt waehlen (per D-10):**
   Pruefen dass `getQuartalSheet(month)` korrekt arbeitet. Aktuell (Zeile 44-50):
   ```js
   function getQuartalSheet(month) {
     const m = parseInt(month);
     if (m >= 1 && m <= 3) return QUARTAL_SHEETS[0];
     if (m >= 4 && m <= 6) return QUARTAL_SHEETS[1];
     if (m >= 7 && m <= 9) return QUARTAL_SHEETS[2];
     return QUARTAL_SHEETS[3];
   }
   ```
   Das ist korrekt fuer Einzelmonats-Export.

   Fuer Range-Export (`exportToExcelRange`, Zeile 413): `const quartalSheetName = getQuartalSheet(startMonth);` — das verwendet den START-Monat. Wenn ein Zeitraum ueber mehrere Quartale geht (z.B. Maerz-Juni), wird nur das Quartal des Start-Monats gewaehlt. Das ist das aktuelle Verhalten und KORREKT fuer die chunked-Workbook-Logik (ein Workbook pro Chunk, alle mit dem gleichen Quartal-Sheet).

   ABER: Wenn der Zeitraum ueber Quartalsgrenzen geht, sollte eigentlich das Quartal des Start-Monats genommen werden (aktuelles Verhalten) da alle Daten chronologisch in ein Sheet geschrieben werden. Das ist OK.

   Keine Aenderung noetig.

4. **Template-Pfad und Dateiname pruefen:**
   Der Template-Pfad `path.join(__dirname, '..', 'templates', 'fahrtenabrechnung_vorlage.xlsx')` muss auf die aktuelle 2026-Vorlage zeigen. Pruefen ob die Datei existiert:
   ```bash
   ls -la backend/templates/fahrtenabrechnung_vorlage.xlsx
   ```
   Falls die neue 2026-Vorlage im Projekt-Root liegt (`Fahrtenbuch Stand 2026.xlsx`), muss sie als Template uebernommen werden. Wenn ja:
   ```bash
   cp "Fahrtenbuch Stand 2026.xlsx" backend/templates/fahrtenabrechnung_vorlage.xlsx
   ```
   VORSICHT: Nur kopieren wenn die bestehende Vorlage nicht bereits die 2026-Version ist. Pruefen durch Vergleich der Dateigroessen/Datum.

**Zusammenfassung der konkreten Code-Aenderungen:**
- Zeile 171-174 in exportToExcel: Font-Override-Block entfernen (6 Zeilen)
- Zeile 354-357 in exportToExcelRange: Font-Override-Block entfernen (6 Zeilen)
- Template-Datei pruefen/aktualisieren falls noetig
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Fahrtenbuch && grep -n "cell.font" backend/utils/excelExport.js | head -10 && ls -la backend/templates/fahrtenabrechnung_vorlage.xlsx</automated>
  </verify>
  <acceptance_criteria>
    - excelExport.js enthaelt KEIN `cell.font = { name: 'Arial', size: 10 }` mehr (Font-Overrides entfernt)
    - excelExport.js enthaelt weiterhin `fillQuartalSheet` Funktion
    - excelExport.js enthaelt weiterhin `getQuartalSheet` Funktion
    - excelExport.js enthaelt weiterhin `QUARTAL_SHEETS` Array
    - Template-Datei `backend/templates/fahrtenabrechnung_vorlage.xlsx` existiert
    - excelExport.js setzt in fillQuartalSheet nur `.value` und `.numFmt`, keine `.font`, `.border`, `.fill` oder `.alignment`
  </acceptance_criteria>
  <done>Excel-Export bewahrt Template-Formatierung vollstaendig. Font-Overrides entfernt. Quartals-Sheet-Selektion funktioniert korrekt.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Monatsuebersicht zeigt KPI-Cards mit Farben und Icons. Excel-Export bewahrt Template-Formatierung.</what-built>
  <how-to-verify>
    1. Oeffne die App und navigiere zu "Fahrten & Export"
    2. Scrolle zur Monatsuebersicht — pruefen:
       a. Erstattungs-Zusammenfassungen sind farbige Cards mit Icons (wie Dashboard-KPIs)
       b. Status-Buttons (Eingereicht/Erhalten) funktionieren noch
       c. Jahresfilter und Hide-Completed funktionieren noch
       d. Quick-Actions Dropdown funktioniert noch
    3. Navigiere zur Jahresuebersicht (MonthlyOverview Tab) — pruefen:
       a. Jahres-Erstattungscards sind farbig mit Icons
       b. Gesamt-Card ist hervorgehoben
    4. Exportiere eine Abrechnung als Excel — pruefen:
       a. Oeffne die .xlsx Datei
       b. Spaltenbreiten, Borders, Fonts und Merges entsprechen der Vorlage
       c. Das korrekte Quartals-Sheet ist vorhanden
       d. Daten sind in den richtigen Zellen
  </how-to-verify>
  <resume-signal>Schreibe "approved" oder beschreibe Issues</resume-signal>
</task>

</tasks>

<verification>
- MonthlyOverview zeigt farbige KPI-Cards mit Icons
- Alle bestehende Funktionalitaet (Filter, Status, QuickActions) erhalten
- Excel-Export ueberschreibt keine Template-Styles
- Quartals-Sheet korrekt gewaehlt
</verification>

<success_criteria>
1. Monatsuebersicht Erstattungen als farbige KPI-Cards mit Icons
2. Filter, Status-Buttons und QuickActions funktionieren wie vorher
3. Excel-Export bewahrt Vorlage-Layout (Spaltenbreiten, Borders, Fonts, Merges)
4. Korrektes Quartals-Sheet wird verwendet
</success_criteria>

<output>
After completion, create `.planning/phases/20-dashboard-kpis-fahrten-layout-excel-export/20-02-SUMMARY.md`
</output>
