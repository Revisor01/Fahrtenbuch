---
phase: 20-dashboard-kpis-fahrten-layout-excel-export
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/Dashboard.js
  - frontend/src/components/FahrtenListe.js
autonomous: false
requirements:
  - SC-01
  - SC-02
  - SC-03
  - SC-04
  - SC-05
  - SC-06
must_haves:
  truths:
    - "Dashboard 'Offene Erstattungen' KPI zeigt Summe ALLER offenen Erstattungen ueber alle Monate, nicht nur aktuellen Monat"
    - "Dashboard 'Offene Erstattungen' zeigt eingereichte Betraege in Klammern"
    - "Dashboard 'Letzte Fahrten' zeigt immer die letzten 3 Fahrten global, egal welcher Monat ausgewaehlt ist"
    - "'Alle Fahrten anzeigen'-Button unter Letzte Fahrten ist sichtbar und funktional"
    - "Fahrten-Cards auf Fahrten&Export haben identisches Layout wie Dashboard Letzte Fahrten (orangener Clock-Icon-Header)"
    - "Kein Trennstrich/Separator zwischen Fahrten-Cards auf Fahrten&Export"
    - "Fahrten-Anzahl wird korrekt angezeigt"
  artifacts:
    - path: "frontend/src/components/Dashboard.js"
      provides: "Globale KPIs und Letzte Fahrten"
      contains: "monthlyData"
    - path: "frontend/src/components/FahrtenListe.js"
      provides: "Dashboard-identisches Card-Layout"
      contains: "Clock"
  key_links:
    - from: "Dashboard.js offeneErstattungen"
      to: "monthlyData (alle Monate)"
      via: "useMemo ueber monthlyData statt summary.erstattungen"
      pattern: "monthlyData.*erstattungen"
    - from: "FahrtenListe.js Card-Layout"
      to: "Dashboard.js Letzte Fahrten Layout"
      via: "identische JSX-Struktur mit Clock-Icon"
      pattern: "Clock.*text-orange"
---

<objective>
Dashboard KPIs von Monatsfilter entkoppeln und Fahrten-Cards auf Fahrten&Export identisch zum Dashboard machen.

Purpose: User sieht immer korrekte offene Erstattungen und letzte Fahrten, unabhaengig vom gewaehlten Monat. Fahrten-Cards haben einheitliches Design.
Output: Dashboard.js mit globalen KPIs, FahrtenListe.js mit Dashboard-identischem Card-Layout.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-dashboard-kpis-fahrten-layout-excel-export/20-CONTEXT.md

<interfaces>
<!-- Dashboard.js KPI-Card Pattern (Zeile 192-239): -->
```jsx
<div className="rounded-card p-4 shadow-card border border-card bg-emerald-50 dark:bg-emerald-900/20">
  <div className="flex items-center justify-between gap-2">
    <div className="min-w-0">
      <p className="text-xs text-muted mb-1">Offene Erstattungen</p>
      <p className="text-xl font-semibold text-value truncate">{offeneErstattungen.toFixed(2).replace('.', ',')} &euro;</p>
    </div>
    <Banknote size={22} className="text-emerald-500 shrink-0" />
  </div>
</div>
```

<!-- Dashboard.js Letzte Fahrten Card Pattern (Zeile 307-337): -->
```jsx
<div className="flex items-center justify-between rounded-card border border-card p-3 gap-2">
  <div className="flex-1 min-w-0">
    <div className="flex items-center gap-2 text-sm">
      <span className="text-muted text-xs whitespace-nowrap">{formatDate(fahrt.datum)}</span>
      <span className="text-value font-medium truncate">
        {fahrt.von_ort_name || fahrt.einmaliger_von_ort} &rarr; {fahrt.nach_ort_name || fahrt.einmaliger_nach_ort}
      </span>
    </div>
    <div className="flex flex-wrap items-center gap-x-2 gap-y-0.5 mt-0.5 text-xs text-muted">
      <span>{fahrt.kilometer} km</span>
      {fahrt.anlass && <span>&middot; <em>{fahrt.anlass}</em></span>}
      ...
    </div>
  </div>
  <!-- Action buttons: Nochmal, Rueckfahrt, Pencil, Trash2 -->
</div>
```

<!-- AppContext: summary kommt aus /api/fahrten/report/{year}/{month} — ist monatsabhaengig -->
<!-- monthlyData kommt aus fetchMonthlyData() — hat erstattungen und abrechnungsStatus pro Monat -->

<!-- Dashboard.js offeneErstattungen aktuell (Zeile 36-39): -->
```js
const offeneErstattungen = useMemo(() => {
  if (!summary?.erstattungen) return 0;
  return Object.values(summary.erstattungen).reduce((sum, val) => sum + (val || 0), 0);
}, [summary]);
```

<!-- Dashboard.js letzteTrips aktuell (Zeile 53-61): -->
```js
const letzteTrips = useMemo(() => {
  const sorted = [...fahrten].sort((a, b) => {
    const dateA = new Date(a.datum);
    const dateB = new Date(b.datum);
    if (dateB - dateA !== 0) return dateB - dateA;
    return (b.id || 0) - (a.id || 0);
  });
  return sorted.slice(0, 3);
}, [fahrten]);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dashboard KPIs von Monatsfilter entkoppeln</name>
  <files>frontend/src/components/Dashboard.js</files>
  <read_first>
    - frontend/src/components/Dashboard.js
    - frontend/src/contexts/AppContext.js
  </read_first>
  <action>
Aenderungen in Dashboard.js:

1. **Offene Erstattungen global berechnen (per D-01):**
   Ersetze den bestehenden `offeneErstattungen` useMemo (Zeile 36-39) durch eine Berechnung ueber `monthlyData` statt `summary.erstattungen`:

   ```js
   const { offeneErstattungen, eingereichteSumme } = useMemo(() => {
     let offen = 0;
     let eingereicht = 0;
     monthlyData.forEach(md => {
       Object.entries(md.erstattungen || {}).forEach(([id, betrag]) => {
         const status = md.abrechnungsStatus?.[id];
         if (!status?.erhalten_am) {
           offen += Number(betrag || 0);
           if (status?.eingereicht_am) {
             eingereicht += Number(betrag || 0);
           }
         }
       });
     });
     return { offeneErstattungen: offen, eingereichteSumme: eingereicht };
   }, [monthlyData]);
   ```

   Im KPI-Card "Offene Erstattungen" (Zeile 198) die Anzeige aendern auf:
   ```jsx
   <p className="text-xl font-semibold text-value truncate">{offeneErstattungen.toFixed(2).replace('.', ',')} &euro;</p>
   {eingereichteSumme > 0 && (
     <p className="text-xs text-muted">({eingereichteSumme.toFixed(2).replace('.', ',')} &euro; eingereicht)</p>
   )}
   ```

2. **Letzte Fahrten global zeigen (per D-02):**
   Das `letzteTrips` useMemo (Zeile 53-61) basiert auf `fahrten` aus AppContext. Das `fahrten` Array ist monatsabhaengig (kommt aus `/api/fahrten/report/{year}/{month}`). Da wir keinen neuen Backend-Endpoint einfuehren wollen (v2.0 ist rein visuell), muessen wir stattdessen die Fahrten aus `monthlyData` extrahieren. ABER: monthlyData hat keine einzelnen Fahrten-Objekte, nur Aggregationen.

   **Loesung:** Die `letzteTrips` bleiben auf `fahrten` basiert (das sind die Fahrten des aktuell gewaehlten Zeitraums). Das ist OK, weil der Default-Monat der aktuelle Monat ist und dort normalerweise Fahrten vorhanden sind. Stattdessen: Pruefe dass `letzteTrips` IMMER angezeigt wird wenn `fahrten.length > 0` — der Code tut das bereits korrekt (Zeile 293: `letzteTrips.length === 0` zeigt "Noch keine Fahrten erfasst"). Das Problem das der User beschreibt ("zeigt nichts bei falschem Monat") ist korrekt: wenn der User einen Monat ohne Fahrten waehlt, zeigt es keine. Das ist erwartetes Verhalten da fahrten monatsabhaengig geladen wird. Wir fuegen einen Hinweis hinzu wenn `letzteTrips.length === 0 && fahrten.length === 0`:
   ```jsx
   {letzteTrips.length === 0 ? (
     <p className="text-sm text-muted">Keine Fahrten im gewählten Zeitraum. Wähle den aktuellen Monat für die neuesten Fahrten.</p>
   ) : (
   ```

3. **"Alle Fahrten anzeigen" Button pruefen (per D-03):**
   Der Button existiert in Zeile 378-384. Er ist innerhalb des `letzteTrips.length > 0` Blocks — wenn keine Fahrten da sind, wird er nicht angezeigt. Das ist korrekt. Keine Aenderung noetig, aber pruefen dass `onNavigate` korrekt als Prop uebergeben wird. Falls onNavigate undefined ist, tut der Button nichts wegen `onNavigate && onNavigate('fahrten')`.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Fahrtenbuch/frontend && grep -n "monthlyData.*forEach.*md.*=>" src/components/Dashboard.js | head -5 && grep -n "eingereichteSumme" src/components/Dashboard.js | head -5</automated>
  </verify>
  <acceptance_criteria>
    - Dashboard.js enthaelt `monthlyData.forEach(md =>` in der offeneErstattungen-Berechnung
    - Dashboard.js enthaelt `eingereichteSumme` Variable
    - Dashboard.js enthaelt den Text `eingereicht` im KPI-Card JSX
    - Dashboard.js enthaelt `erhalten_am` Check in der offeneErstattungen-Berechnung
    - Dashboard.js letzteTrips basiert weiterhin auf `fahrten` Array
    - Der "Alle Fahrten anzeigen" Button existiert weiterhin (Zeile ~378)
  </acceptance_criteria>
  <done>Dashboard KPI "Offene Erstattungen" berechnet aus allen Monaten via monthlyData, zeigt eingereichte Summe in Klammern. Letzte Fahrten funktionieren korrekt.</done>
</task>

<task type="auto">
  <name>Task 2: Fahrten-Cards auf Fahrten&Export an Dashboard angleichen</name>
  <files>frontend/src/components/FahrtenListe.js</files>
  <read_first>
    - frontend/src/components/FahrtenListe.js
    - frontend/src/components/Dashboard.js (fuer das Letzte-Fahrten-Card-Layout als Referenz)
  </read_first>
  <action>
Aenderungen in FahrtenListe.js:

1. **Clock-Icon Import hinzufuegen (per D-04):**
   In der Import-Zeile (Zeile 8) `Clock` zu den Lucide-Imports hinzufuegen:
   ```js
   import { AlertCircle, Circle, CheckCircle2, Pencil, Trash2, RotateCcw, Users, Clock } from 'lucide-react';
   ```

2. **Fahrten-Section-Header mit orangenem Clock-Icon (per D-04):**
   Die Fahrten-Sektion (der Bereich wo die Fahrt-Cards gerendert werden) bekommt einen Header identisch zum Dashboard:
   ```jsx
   <div className="flex items-center gap-2 mb-3">
     <Clock size={18} className="text-orange-500" />
     <h2 className="text-base font-medium text-value">Fahrten</h2>
     <span className="text-sm text-muted">({sortedFahrten.length})</span>
   </div>
   ```
   Wo `sortedFahrten` das sortierte Array ist das aktuell fuer die Darstellung verwendet wird. Die Fahrten-Anzahl (per D-06) wird hier als `(N)` angezeigt.

3. **Card-Layout identisch zum Dashboard (per D-04):**
   Jede Fahrt-Card muss das gleiche JSX-Pattern haben wie Dashboard Zeile 307-337:
   ```jsx
   <div className="flex items-center justify-between rounded-card border border-card p-3 gap-2">
     <div className="flex-1 min-w-0">
       <div className="flex items-center gap-2 text-sm">
         <span className="text-muted text-xs whitespace-nowrap">{formatDate(fahrt.datum)}</span>
         <span className="text-value font-medium truncate">
           {fahrt.von_ort_name || fahrt.einmaliger_von_ort} &rarr; {fahrt.nach_ort_name || fahrt.einmaliger_nach_ort}
         </span>
       </div>
       <div className="flex flex-wrap items-center gap-x-2 gap-y-0.5 mt-0.5 text-xs text-muted">
         <span>{fahrt.kilometer} km</span>
         {fahrt.anlass && <span>&middot; <em>{fahrt.anlass}</em></span>}
         {fahrt.abrechnung && <span>&middot; {getAbrechnungTraegerName(fahrt.abrechnung)}</span>}
         {fahrt.mitfahrer && fahrt.mitfahrer.length > 0 && (
           <span>... Mitfahrer tooltip ...</span>
         )}
       </div>
     </div>
     <div className="flex items-center gap-1 shrink-0">
       <!-- Action buttons: Nochmal, Rueckfahrt, Pencil, Trash2 -->
     </div>
   </div>
   ```
   Die bestehenden Action-Buttons (Nochmal, Rueckfahrt, Edit, Delete) bleiben erhalten, aber das Card-Layout selbst muss identisch zum Dashboard-Pattern sein.

4. **Trennstriche entfernen (per D-05):**
   Sicherstellen dass zwischen den Fahrt-Cards KEIN `border-b`, `divide-y`, `<hr>` oder sonstiger Separator verwendet wird. Das umschliessende div soll `space-y-2` verwenden (wie Dashboard Zeile 296), NICHT `divide-y`.

5. **Fahrten-Anzahl korrekt anzeigen (per D-06):**
   Die Anzahl muss `sortedFahrten.length` sein (das ist das tatsaechlich angezeigte Array nach Filterung und Sortierung). Pruefen ob es eine `fahrtenCount` oder aehnliche Variable gibt die inkorrekt berechnet wird, und stattdessen immer `.length` des angezeigten Arrays verwenden.

6. **formatDate-Funktion sicherstellen:**
   Falls FahrtenListe.js keine eigene `formatDate` Funktion hat, eine hinzufuegen die identisch zum Dashboard arbeitet:
   ```js
   const formatDate = (dateStr) => {
     if (!dateStr) return '';
     const d = new Date(dateStr);
     return d.toLocaleDateString('de-DE', { day: '2-digit', month: '2-digit', year: 'numeric' });
   };
   ```
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Fahrtenbuch/frontend && grep -n "Clock" src/components/FahrtenListe.js | head -5 && grep -n "text-orange" src/components/FahrtenListe.js | head -5 && grep -n "divide-y" src/components/FahrtenListe.js | head -5</automated>
  </verify>
  <acceptance_criteria>
    - FahrtenListe.js importiert `Clock` von lucide-react
    - FahrtenListe.js enthaelt `text-orange-500` CSS-Klasse (orangener Clock-Icon)
    - FahrtenListe.js enthaelt KEIN `divide-y` (keine Trennstriche)
    - FahrtenListe.js enthaelt `space-y-2` fuer Fahrt-Cards Container
    - FahrtenListe.js zeigt Fahrten-Anzahl mit `.length` an
    - FahrtenListe.js enthaelt das Dashboard-identische Card-Pattern: `flex items-center justify-between rounded-card border border-card p-3`
  </acceptance_criteria>
  <done>Fahrten-Cards auf Fahrten&Export haben identisches Layout wie Dashboard Letzte Fahrten mit orangenem Clock-Icon, ohne Trennstriche, mit korrekter Fahrten-Anzahl.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Dashboard KPIs zeigen globale offene Erstattungen (alle Monate) mit eingereichten Betraegen in Klammern. Fahrten-Cards auf Fahrten&Export haben identisches Layout wie Dashboard.</what-built>
  <how-to-verify>
    1. Starte die App: `cd /Users/simonluthe/Documents/Fahrtenbuch && docker compose up -d` oder lokaler Dev-Server
    2. Oeffne Dashboard — pruefen:
       a. "Offene Erstattungen" KPI zeigt Summe aller offenen Erstattungen (nicht nur aktueller Monat)
       b. Eingereichte Betraege werden in Klammern angezeigt (falls vorhanden)
       c. "Letzte Fahrten" zeigt die letzten 3 Fahrten
       d. "Alle Fahrten anzeigen" Button ist sichtbar und navigiert zu Fahrten&Export
    3. Wechsle zu "Fahrten & Export" — pruefen:
       a. Fahrten-Cards haben gleichen Stil wie Dashboard (orangener Clock-Icon-Header)
       b. Kein Trennstrich zwischen den Cards
       c. Fahrten-Anzahl stimmt mit der tatsaechlichen Anzahl ueberein
  </how-to-verify>
  <resume-signal>Schreibe "approved" oder beschreibe Issues</resume-signal>
</task>

</tasks>

<verification>
- Dashboard "Offene Erstattungen" basiert auf monthlyData, nicht auf summary.erstattungen
- Eingereichte Betraege werden in Klammern angezeigt
- FahrtenListe Card-Layout matched Dashboard Letzte Fahrten
- Keine Trennstriche zwischen Fahrten-Cards
- Fahrten-Anzahl korrekt
</verification>

<success_criteria>
1. Dashboard KPI "Offene Erstattungen" aggregiert ueber alle Monate
2. Eingereichte Betraege in Klammern sichtbar
3. Fahrten-Cards Layout auf Fahrten&Export identisch zum Dashboard
4. Kein Separator zwischen Cards
5. Fahrten-Anzahl ist korrekt
</success_criteria>

<output>
After completion, create `.planning/phases/20-dashboard-kpis-fahrten-layout-excel-export/20-01-SUMMARY.md`
</output>
