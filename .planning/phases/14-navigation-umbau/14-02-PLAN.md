---
phase: 14-navigation-umbau
plan: 02
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - frontend/src/components/AppContent.js
autonomous: false
requirements:
  - NAV-02

must_haves:
  truths:
    - "Admin-Nutzer sehen einen Verwaltung-Tab in der Navigation"
    - "Nicht-Admin-Nutzer sehen keinen Verwaltung-Tab"
    - "Klick auf Verwaltung zeigt UserManagement inline (kein Modal)"
  artifacts:
    - path: "frontend/src/components/AppContent.js"
      provides: "Verwaltung-Tab fuer Admins, Cleanup alter Modal-Logik"
  key_links:
    - from: "frontend/src/components/AppContent.js"
      to: "frontend/src/UserManagement.js"
      via: "Rendering bei activeTab === 'verwaltung'"
      pattern: "activeTab.*verwaltung.*UserManagement"
---

<objective>
Benutzerverwaltung als eigener Tab fuer Admins in der Hauptnavigation.

Purpose: Per D-05/D-06/D-07/D-09, Admin-Nutzer sehen einen zusaetzlichen Tab "Verwaltung" in der Navigation. UserManagement wird inline gerendert statt in einem Modal. Nicht-Admins sehen den Tab nicht.
Output: Angepasste AppContent.js mit Admin-Tab und entfernter Modal-Logik.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-navigation-umbau/14-CONTEXT.md
@.planning/phases/14-navigation-umbau/14-01-SUMMARY.md

@frontend/src/components/AppContent.js
@frontend/src/UserManagement.js

<interfaces>
<!-- After Plan 01, AppContent.js will have these tabs: -->
tabs = [
  { id: 'dashboard', label: 'Dashboard', icon: LayoutDashboard },
  { id: 'fahrten', label: 'Fahrten & Export', icon: Car },
  { id: 'monatsuebersicht', label: 'Monatsuebersicht', icon: CalendarDays },
  { id: 'einstellungen', label: 'Einstellungen', icon: SettingsIcon },
]

<!-- UserManagement.js is a self-contained component, already imported in AppContent -->
<!-- Currently rendered inside a Modal wrapper with showUserManagementModal state -->
<!-- user.role available via AppContext -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verwaltung-Tab fuer Admins und Modal-Cleanup</name>
  <files>frontend/src/components/AppContent.js</files>
  <read_first>frontend/src/components/AppContent.js</read_first>
  <action>
AppContent.js anpassen per D-05, D-06, D-07, D-09, D-10:

1. tabs-Array dynamisch machen: Das tabs-Array muss conditional den Verwaltung-Tab enthalten. Entweder:
   - `const tabs = [...]` beibehalten und danach: `if (user?.role === 'admin') tabs.push({ id: 'verwaltung', label: 'Verwaltung', icon: Users });`
   - Oder mit Spread: `...(user?.role === 'admin' ? [{ id: 'verwaltung', label: 'Verwaltung', icon: Users }] : [])`
   Users-Icon ist bereits von lucide-react importiert.

2. Tab-Inhalt hinzufuegen: `{activeTab === 'verwaltung' && <UserManagement />}` — OHNE Modal-Wrapper.

3. Modal-Cleanup entfernen:
   - `useState(false)` fuer `showUserManagementModal` entfernen
   - Den gesamten Admin-Button-Block im Header entfernen (Zeilen die `setShowUserManagementModal(true)` aufrufen und den "Benutzerverwaltung"-Button rendern)
   - Den `<Modal isOpen={showUserManagementModal} ...><UserManagement /></Modal>` Block entfernen
   - Import von `Modal` pruefen: Wird Modal noch anderswo benutzt? Wenn nicht, Import entfernen. (Achtung: Modal wird NICHT mehr benutzt nach Plan 01 + Plan 02 — ProfileModal ist weg, UserManagement-Modal ist weg. InfoModal und NewFeaturesModal importieren Modal selbst. Also kann Modal-Import aus AppContent entfernt werden.)

4. Per D-10: Die Tab-Bar muss mit 5 Tabs (Admin) bzw. 4 Tabs (User) funktionieren. Die bestehende `overflow-x-auto` Klasse auf der Tab-Navigation reicht dafuer aus. Keine weiteren Aenderungen noetig.

5. Pruefen ob `Users` Icon-Import noch gebraucht wird — ja, fuer den Verwaltung-Tab.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Fahrtenbuch/frontend && npx react-scripts build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
- Admin-Nutzer sehen 5 Tabs: Dashboard, Fahrten & Export, Monatsuebersicht, Einstellungen, Verwaltung
- Nicht-Admin-Nutzer sehen 4 Tabs (kein Verwaltung)
- Klick auf Verwaltung rendert UserManagement inline
- Kein Modal-Wrapper mehr fuer UserManagement
- Kein Benutzerverwaltung-Button mehr im Header
- Build fehlerfrei
  </acceptance_criteria>
  <done>Verwaltung-Tab funktioniert inline fuer Admins, alle Modals aus AppContent entfernt</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visueller Check — Navigation und Einstellungen</name>
  <files>frontend/src/components/AppContent.js</files>
  <action>Visueller Check durch den Nutzer — keine Code-Aenderungen. Pruefen ob Navigation-Umbau korrekt funktioniert.</action>
  <what-built>Kompletter Navigation-Umbau: Einstellungen inline mit Sub-Tabs, Verwaltung als Admin-Tab, keine Modals mehr fuer Einstellungen oder Benutzerverwaltung.</what-built>
  <how-to-verify>
1. App im Browser oeffnen (kkd-fahrtenbuch.de oder localhost)
2. Navigation pruefen: 4 Tabs sichtbar (Dashboard, Fahrten & Export, Monatsuebersicht, Einstellungen)
3. Einstellungen-Tab klicken: Alle 8 Sub-Tabs pruefen (Profil, Orte, Distanzen, Traeger, Favoriten, Erstattungen, Passwort, API)
4. Profil-Daten speichern testen
5. Einen Ort bearbeiten testen
6. Als Admin einloggen: 5. Tab "Verwaltung" sichtbar?
7. Verwaltung klicken: Benutzerliste inline sichtbar?
8. Mobile testen: Tabs horizontal scrollbar, Einstellungen Sub-Tabs als Dropdown
  </how-to-verify>
  <verify>Visueller Check durch Nutzer</verify>
  <done>Nutzer hat Navigation-Umbau geprueft und freigegeben</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. `cd frontend && npx react-scripts build` — Build fehlerfrei
2. Admin: 5 Tabs sichtbar, Verwaltung zeigt UserManagement inline
3. User: 4 Tabs sichtbar, kein Verwaltung-Tab
4. Kein Modal oeffnet sich mehr fuer Einstellungen oder Verwaltung
5. Mobile: Navigation scrollbar mit 4-5 Tabs
</verification>

<success_criteria>
- Verwaltung ist Admin-only Tab in der Navigation
- UserManagement rendert inline ohne Modal
- Alle alten Modal-Wrapper aus AppContent entfernt
- Visueller Check bestanden
</success_criteria>

<output>
After completion, create `.planning/phases/14-navigation-umbau/14-02-SUMMARY.md`
</output>
