---
phase: 14-navigation-umbau
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/Settings.js
  - frontend/src/components/AppContent.js
autonomous: true
requirements:
  - NAV-01

must_haves:
  truths:
    - "Nutzer klickt auf Einstellungen-Tab und sieht alle Einstellungen inline (kein Modal)"
    - "Alle 8 Bereiche (Profil, Orte, Distanzen, Traeger, Favoriten, Erstattungen, Passwort, API) sind als Sub-Tabs erreichbar"
    - "Mobile zeigt Sub-Tabs als Dropdown-Select"
  artifacts:
    - path: "frontend/src/components/Settings.js"
      provides: "Inline-Einstellungen-Komponente mit Sub-Tabs"
      min_lines: 200
    - path: "frontend/src/components/AppContent.js"
      provides: "Einstellungen als regulaerer Tab-Inhalt statt Modal-Oeffner"
  key_links:
    - from: "frontend/src/components/AppContent.js"
      to: "frontend/src/components/Settings.js"
      via: "import und Rendering bei activeTab === 'einstellungen'"
      pattern: "activeTab.*einstellungen.*Settings"
    - from: "frontend/src/components/Settings.js"
      to: "frontend/src/contexts/AppContext.js"
      via: "useContext(AppContext) fuer token, user, orte, etc."
      pattern: "useContext.*AppContext"
---

<objective>
Einstellungen-Tab zeigt alle Profil-/Konfigurations-Bereiche inline statt in einem Modal.

Purpose: Per D-01/D-02/D-03, der Einstellungen-Tab in der Hauptnavigation zeigt die bisherigen Modal-Inhalte (Profil, Orte, Distanzen, Traeger, Favoriten, Erstattungen, Passwort, API) als Sub-Tabs inline an. Das ProfileModal wird nicht mehr geoeffnet.
Output: Settings.js Komponente, angepasste AppContent.js
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-navigation-umbau/14-CONTEXT.md

@frontend/src/ProfileModal.js
@frontend/src/components/AppContent.js
@frontend/src/contexts/AppContext.js

<interfaces>
<!-- AppContext exports used by ProfileModal (needed by Settings.js) -->
From frontend/src/contexts/AppContext.js:
- token, user, setUser
- showNotification, refreshAllData
- favoriten, orte, abrechnungstraeger
- addFavorit, deleteFavorit, fetchFavoriten
- isProfileModalOpen, setIsProfileModalOpen

From frontend/src/ProfileModal.js:
- Internal tabs: profile, orte, distanzen, abrechnungen, favoriten, erstattungssaetze, security, api
- Imports: AbrechnungstraegerForm, ErstattungssaetzeForm, OrtForm, OrteListe, DistanzForm, DistanzenListe
- State: profile, activeTab, password fields, apiKeys, favoritForm, generatedKey

From frontend/src/components/AppContent.js:
- tabs array: dashboard, fahrten, monatsuebersicht
- Einstellungen currently: separate button that opens ProfileModal via setIsProfileModalOpen(true)
- activeTab state controls which content renders
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Settings.js erstellen — ProfileModal-Inhalt ohne Modal-Wrapper</name>
  <files>frontend/src/components/Settings.js</files>
  <read_first>frontend/src/ProfileModal.js</read_first>
  <action>
Neue Datei `frontend/src/components/Settings.js` erstellen. Der GESAMTE Inhalt von ProfileModal.js wird 1:1 uebernommen — OHNE den Modal-Wrapper.

Konkret:
1. Alle imports aus ProfileModal.js uebernehmen (axios, AppContext, AbrechnungstraegerForm, ErstattungssaetzeForm, OrtForm, OrteListe, DistanzForm, DistanzenListe). NICHT Modal importieren.
2. `function Settings()` (keine Props noetig — kein isOpen/onClose mehr).
3. ALLE useState-Hooks aus ProfileModal uebernehmen: profile, activeTab, oldPassword, newPassword, confirmPassword, apiKeys, newKeyDescription, generatedKey, isGeneratingKey, favoritForm.
4. ALLE useContext-Destructurings uebernehmen: token, user, setUser, showNotification, refreshAllData, favoriten, orte, abrechnungstraeger, addFavorit, deleteFavorit, fetchFavoriten.
5. useEffect: Statt `if (isOpen)` einfach beim Mount ausfuehren — `fetchProfile()` und `fetchApiKeys()` und `fetchFavoriten()` direkt im useEffect mit leerem Dependency-Array aufrufen. Kein `resetForm` beim Unmount noetig.
6. ALLE Handler-Funktionen 1:1 uebernehmen: resetForm, fetchProfile, fetchApiKeys, handleDeleteKey, handleProfileUpdate, handlePasswordChange, handleGenerateKey, handleResendVerification.
7. Das `if (!isOpen) return null;` entfernen — Settings ist immer sichtbar wenn aktiver Tab.
8. JSX: Den `<Modal>` Wrapper entfernen. Direkt mit dem aeusseren `<div className="flex flex-col">` beginnen (ohne `h-full` da nicht in Modal).
9. Sub-Tab-Navigation (Desktop: horizontale Buttons, Mobile: select-Dropdown) 1:1 uebernehmen. Sticky-Verhalten anpassen: statt `sticky top-0` nutze einfach `mb-4 border-b border-primary-100 dark:border-primary-800` (kein Sticky noetig ohne Modal-Scroll).
10. ALLE Tab-Inhalte (profile, orte, distanzen, abrechnungen, favoriten, erstattungssaetze, security, api) exakt 1:1 uebernehmen — NICHTS weglassen.

Per D-03: Neue Komponente Settings.js in components/.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Fahrtenbuch/frontend && node -e "require('./src/components/Settings.js')" 2>&1 || npx react-scripts build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
- Settings.js existiert in frontend/src/components/
- Enthaelt alle 8 Sub-Tabs: profile, orte, distanzen, abrechnungen, favoriten, erstattungssaetze, security, api
- Kein Modal-Import, kein Modal-Wrapper
- Alle Handler aus ProfileModal sind vorhanden (fetchProfile, handleProfileUpdate, handlePasswordChange, handleGenerateKey, handleDeleteKey, handleResendVerification)
- useEffect laedt Daten beim Mount
  </acceptance_criteria>
  <done>Settings.js rendert alle 8 Bereiche als Sub-Tabs ohne Modal</done>
</task>

<task type="auto">
  <name>Task 2: AppContent.js — Einstellungen als regulaeren Tab einbauen</name>
  <files>frontend/src/components/AppContent.js</files>
  <read_first>frontend/src/components/AppContent.js, frontend/src/components/Settings.js</read_first>
  <action>
AppContent.js anpassen per D-01, D-04, D-08:

1. Import hinzufuegen: `import Settings from './Settings';`
2. Import entfernen: `import ProfileModal from '../ProfileModal';` (per D-04)
3. tabs-Array erweitern: Nach 'monatsuebersicht' einfuegen:
   `{ id: 'einstellungen', label: 'Einstellungen', icon: Settings_icon }`
   WICHTIG: Settings (lucide-react) Icon ist bereits importiert als `Settings` — es gibt einen Namenskonflikt! Loesung: Den lucide-react Import umbenennen: `import { Settings as SettingsIcon }` und im tabs-Array `icon: SettingsIcon` verwenden. Die Komponente bleibt `Settings`.
4. Den separaten Einstellungen-Button (Zeilen 133-141 im aktuellen Code, der `setIsProfileModalOpen(true)` aufruft) ENTFERNEN — Einstellungen ist jetzt ein regulaerer Tab im Array.
5. Tab-Inhalt hinzufuegen nach monatsuebersicht: `{activeTab === 'einstellungen' && <Settings />}`
6. ProfileModal-Rendering entfernen (Zeilen 165-168): Die `<ProfileModal isOpen={...} onClose={...} />` Zeile entfernen.
7. State entfernen: `isProfileModalOpen` und `setIsProfileModalOpen` aus dem useContext-Destructuring entfernen (werden nicht mehr gebraucht in AppContent).
8. showUserManagementModal State und den Benutzerverwaltung-Button im Header BEIBEHALTEN — das kommt erst in Plan 02.

Per D-08: Tabs werden: Dashboard | Fahrten & Export | Monatsuebersicht | Einstellungen
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Fahrtenbuch/frontend && npx react-scripts build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
- Einstellungen ist regulaerer Tab im tabs-Array (nicht separater Button)
- Settings-Komponente wird bei activeTab === 'einstellungen' gerendert
- ProfileModal wird nicht mehr importiert oder gerendert
- Kein Namenskonflikt zwischen Settings-Komponente und Settings-Icon
- Build laeuft ohne Fehler
  </acceptance_criteria>
  <done>Einstellungen-Tab zeigt Settings inline, ProfileModal ist entfernt</done>
</task>

</tasks>

<verification>
1. `cd frontend && npx react-scripts build` — Build muss fehlerfrei durchlaufen
2. Im Browser: Einstellungen-Tab klicken zeigt alle 8 Sub-Tabs inline
3. Kein Modal oeffnet sich beim Klick auf Einstellungen
4. Alle Sub-Tabs funktionieren (Profil speichern, Passwort aendern, API Keys, Favoriten, Orte, Distanzen, Traeger, Erstattungen)
</verification>

<success_criteria>
- Einstellungen ist ein regulaerer Tab in der Navigation
- Alle 8 Bereiche sind inline als Sub-Tabs erreichbar
- ProfileModal wird nicht mehr verwendet
- Build ist fehlerfrei
</success_criteria>

<output>
After completion, create `.planning/phases/14-navigation-umbau/14-01-SUMMARY.md`
</output>
