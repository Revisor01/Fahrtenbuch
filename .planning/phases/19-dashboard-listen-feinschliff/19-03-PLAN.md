---
phase: 19-dashboard-listen-feinschliff
plan: 03
type: execute
wave: 2
depends_on:
  - 19-01
files_modified:
  - frontend/src/components/FahrtenListe.js
autonomous: true
requirements:
  - P19-02
  - P19-05
must_haves:
  truths:
    - "Fahrtenliste zeigt Fahrten als Cards statt Tabellenzeilen"
    - "Card-Design ist identisch zum Dashboard Letzte-Fahrten Design"
    - "Inline-Bearbeitung funktioniert: Klick auf Pencil ersetzt Card durch FahrtForm"
    - "Loeschen, Nochmal und Rueckfahrt Buttons funktionieren auf Cards"
    - "Filter/Sort Bereich bleibt als eigene card-container"
    - "Alle Buttons haben btn-primary/btn-secondary/btn-destructive statt individuelle Farben"
  artifacts:
    - path: "frontend/src/components/FahrtenListe.js"
      provides: "Card-based trip list with inline editing"
      contains: "editData"
  key_links:
    - from: "frontend/src/components/FahrtenListe.js"
      to: "frontend/src/FahrtForm.js"
      via: "editData prop for inline editing"
      pattern: "FahrtForm.*editData"
---

<objective>
Fahrtenliste von Tabelle zu Card-Layout umbauen mit Inline-Bearbeitung

Purpose: Die Fahrtenliste soll das gleiche Card-Design nutzen wie die "Letzte Fahrten" auf dem Dashboard (per D-04, D-05, D-06). Inline-Edit nutzt den in Plan 01 erweiterten FahrtForm mit Edit-Props.
Output: FahrtenListe.js mit Card-Layout fuer alle Fahrten, Inline-Edit via FahrtForm, konsistentes Button-Schema
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-dashboard-listen-feinschliff/19-CONTEXT.md
@.planning/phases/19-dashboard-listen-feinschliff/19-UI-SPEC.md
@.planning/phases/19-dashboard-listen-feinschliff/19-01-SUMMARY.md

@frontend/src/components/FahrtenListe.js
@frontend/src/components/Dashboard.js
@frontend/src/FahrtForm.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fahrtenliste Card-Layout und Inline-Edit</name>
  <files>frontend/src/components/FahrtenListe.js</files>
  <read_first>frontend/src/components/FahrtenListe.js, frontend/src/components/Dashboard.js, frontend/src/FahrtForm.js, frontend/src/index.css</read_first>
  <action>
**Imports ergaenzen:**

FahrtForm importieren: `import FahrtForm from '../FahrtForm';`
Lucide-Icons erweitern (falls nicht schon vorhanden): `Pencil, Trash2, RotateCcw, Users` zu den bestehenden Imports hinzufuegen.

**Inline-Edit State anpassen:**

Den bestehenden `editingFahrt` State (der fuer die Tabellen-Inline-Edit genutzt wurde) durch einen einfacheren `editingFahrtId` State ersetzen:

```javascript
const [editingFahrtId, setEditingFahrtId] = useState(null);
```

Die Funktionen `handleEdit`, `handleEditChange`, `handleSave` und die `renderFahrtRow` Funktion werden durch die neue Card-basierte Darstellung ersetzt.

Neue Callbacks fuer FahrtForm Edit-Modus:

```javascript
const handleEditComplete = () => {
  setEditingFahrtId(null);
  refreshAllData();
};

const handleEditCancel = () => {
  setEditingFahrtId(null);
};
```

**Tabellen-basierte Darstellung (Desktop + Mobile) durch einheitliches Card-Layout ersetzen (per D-04, D-05, D-06):**

Den gesamten Bereich ab `{/* Desktop View */}` und `{/* Mobile View */}` (zwei separate Bloecke mit `hidden md:block` und `md:hidden`) durch EINEN einheitlichen Card-Bereich ersetzen:

```jsx
{/* Fahrtenliste als Cards */}
{sortedFahrten.length === 0 ? (
  <div className="card-container text-center py-12">
    <p className="text-sm font-medium text-value">Keine Fahrten im gewaehlten Zeitraum</p>
    <p className="text-xs text-muted mt-1">Erfasse deine erste Fahrt ueber das Dashboard.</p>
  </div>
) : (
  <div className="card-container">
    <div className="px-0 pb-3 border-b border-card mb-3">
      <h3 className="text-base font-semibold text-value">Fahrten</h3>
      <p className="text-xs text-label mt-0.5">{sortedFahrten.length} Eintraege</p>
    </div>
    <div className="space-y-2">
      {sortedFahrten.map((fahrt) => {
        const traeger = abrechnungstraeger?.find(at => at.id === parseInt(fahrt.abrechnung));
        const abrechnungstraegerName = traeger ? traeger.name : 'Unbekannt';

        if (editingFahrtId === fahrt.id) {
          return (
            <div key={fahrt.id} className="rounded-card border border-card p-4 animate-tab-content-fade">
              <FahrtForm
                editData={fahrt}
                onUpdate={handleEditComplete}
                onCancel={handleEditCancel}
              />
            </div>
          );
        }

        return (
          <div
            key={fahrt.id}
            className="flex items-center justify-between rounded-card border border-card p-3 gap-2 hover:shadow-card-hover transition-shadow"
          >
            <div className="flex-1 min-w-0">
              <div className="flex items-center gap-2 text-sm">
                <span className="text-muted text-xs whitespace-nowrap">
                  {new Date(fahrt.datum).toLocaleDateString('de-DE', { day: '2-digit', month: '2-digit', year: 'numeric' })}
                </span>
                <span className="text-value font-medium truncate">
                  {fahrt.von_ort_name || fahrt.einmaliger_von_ort} &rarr; {fahrt.nach_ort_name || fahrt.einmaliger_nach_ort}
                </span>
              </div>
              <div className="flex flex-wrap items-center gap-x-2 gap-y-0.5 mt-0.5 text-xs text-muted">
                <span>{fahrt.kilometer} km</span>
                {fahrt.anlass && <span>&middot; <em>{fahrt.anlass}</em></span>}
                {abrechnungstraegerName && <span>&middot; {abrechnungstraegerName}</span>}
                {fahrt.mitfahrer && fahrt.mitfahrer.length > 0 && (
                  <span className="relative group inline-flex items-center gap-1 cursor-help">
                    <span>&middot;</span>
                    <Users size={11} />
                    <span className="underline decoration-dotted underline-offset-2">
                      {fahrt.mitfahrer.length} Mitfahrer:in{fahrt.mitfahrer.length > 1 ? 'nen' : ''}
                    </span>
                    <span className="absolute bottom-full left-0 mb-1 hidden group-hover:block z-10 bg-card shadow-card-hover border border-card rounded-card px-3 py-2 text-xs text-value whitespace-nowrap">
                      {fahrt.mitfahrer.map((m, i) => (
                        <span key={i} className="block">{m.name}{m.arbeitsstaette ? ` (${m.arbeitsstaette})` : ''}</span>
                      ))}
                    </span>
                  </span>
                )}
              </div>
            </div>
            <div className="flex items-center gap-1 shrink-0">
              <button
                onClick={() => handleNochmal(fahrt)}
                className="btn-primary flex items-center gap-1 text-xs"
                aria-label="Fahrt kopieren"
                title="Nochmal fuer heute"
              >
                <RotateCcw size={12} />
                <span className="hidden sm:inline">Nochmal</span>
              </button>
              <button
                onClick={() => handleNochmalAndereRichtung(fahrt)}
                className="btn-secondary flex items-center gap-1 text-xs"
                aria-label="Rueckfahrt erstellen"
                title="Rueckfahrt fuer heute eintragen"
              >
                <RotateCcw size={12} className="scale-x-[-1]" />
                <span className="hidden sm:inline">Rueckfahrt</span>
              </button>
              <button
                onClick={() => setEditingFahrtId(fahrt.id)}
                className="p-1.5 rounded-card text-muted hover:text-value hover:bg-primary-50 dark:hover:bg-primary-900 transition-colors"
                aria-label="Fahrt bearbeiten"
                title="Bearbeiten"
              >
                <Pencil size={13} />
              </button>
              <button
                onClick={() => handleDelete(fahrt.id)}
                className="p-1.5 rounded-card text-muted hover:text-secondary-500 hover:bg-secondary-50 dark:hover:bg-secondary-900 transition-colors"
                aria-label="Fahrt loeschen"
                title="Loeschen"
              >
                <Trash2 size={13} />
              </button>
            </div>
          </div>
        );
      })}
    </div>
  </div>
)}
```

**Nochmal/Rueckfahrt Buttons (per D-05, D-12):**

Die bisherigen Nochmal/Rueckfahrt Buttons in der Fahrtenliste hatten individuelle Farben (bg-blue-50, bg-green-50). Diese werden durch `btn-primary` und `btn-secondary` ersetzt (siehe Card-Template oben). Die bestehenden `handleNochmal` und `handleNochmalAndereRichtung` Funktionen bleiben unveraendert.

**Sortierung beibehalten:**

Die `sortedFahrten` useMemo und `requestSort` Logik bleibt bestehen. Der Sort-Header kann als einfache Buttons ueber dem Card-Container gerendert werden (z.B. in einer kleinen Leiste), oder die Sortierung bleibt im bestehenden Filter-Bereich. Die sortedFahrten-Variable wird weiterhin fuer die Card-Iteration genutzt.

**Alte renderFahrtRow Funktion entfernen** — wird nicht mehr gebraucht. Auch die `editingFahrt` State-Variable und die dazugehoerigen Handler (`handleEditChange`, die alte `handleSave` etc.) koennen entfernt werden, da FahrtForm jetzt die Edit-Logik uebernimmt.

**Bestehende Filter/Export/Abrechnungsstatus-Bereiche beibehalten** — nur die Fahrten-Darstellung aendert sich.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Fahrtenbuch && grep -c "FahrtForm" frontend/src/components/FahrtenListe.js && grep -c "editingFahrtId" frontend/src/components/FahrtenListe.js && grep -c "btn-primary" frontend/src/components/FahrtenListe.js && grep -c "aria-label" frontend/src/components/FahrtenListe.js && grep -c "editData" frontend/src/components/FahrtenListe.js</automated>
  </verify>
  <acceptance_criteria>
    - FahrtenListe.js enthaelt `import FahrtForm from '../FahrtForm'`
    - FahrtenListe.js enthaelt `editingFahrtId` State
    - FahrtenListe.js enthaelt `FahrtForm` mit `editData` und `onUpdate` Props
    - FahrtenListe.js enthaelt KEINE `<table>`, `<thead>`, `<tbody>` Elemente mehr fuer Fahrten (Abrechnungsstatus-Tabelle ist ok)
    - FahrtenListe.js enthaelt `btn-primary` und `btn-secondary` fuer Nochmal/Rueckfahrt (nicht bg-blue-50/bg-green-50)
    - FahrtenListe.js enthaelt mindestens 4 `aria-label` Attribute
    - FahrtenListe.js enthaelt `Keine Fahrten im gewaehlten Zeitraum` als Empty-State
    - FahrtenListe.js enthaelt `Erfasse deine erste Fahrt ueber das Dashboard` als Empty-State Body
    - Die bestehenden Filter/Export/Abrechnungsstatus-Bereiche sind weiterhin vorhanden
  </acceptance_criteria>
  <done>Fahrtenliste zeigt alle Fahrten als Cards mit identischem Design wie Dashboard. Inline-Edit via FahrtForm. Konsistente Button-Farben (primary/secondary statt individuelle Farbcodes). Keine Tabelle mehr fuer Fahrten.</done>
</task>

</tasks>

<verification>
- `grep "table" frontend/src/components/FahrtenListe.js | grep -v "Abrechnungsstatus\|comment"` zeigt keine Fahrt-Tabellen mehr
- `grep "FahrtForm" frontend/src/components/FahrtenListe.js` zeigt FahrtForm Import und Verwendung
- `grep "editData" frontend/src/components/FahrtenListe.js` zeigt Edit-Modus Props
- `grep "btn-primary\|btn-secondary" frontend/src/components/FahrtenListe.js` zeigt konsistentes Button-Schema
- `cd frontend && npx react-scripts build 2>&1 | tail -3` kompiliert ohne Fehler
</verification>

<success_criteria>
- Fahrtenliste zeigt Fahrten als Cards statt Tabellenzeilen
- Card-Design ist identisch zum Dashboard "Letzte Fahrten" Design
- Inline-Bearbeitung via FahrtForm funktioniert (Pencil -> Form -> Save/Cancel)
- Button-Farbschema nutzt btn-primary/btn-secondary konsistent
- Filter und Export bleiben funktional
</success_criteria>

<output>
After completion, create `.planning/phases/19-dashboard-listen-feinschliff/19-03-SUMMARY.md`
</output>
