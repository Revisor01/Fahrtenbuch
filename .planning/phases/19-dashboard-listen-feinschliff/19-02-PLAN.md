---
phase: 19-dashboard-listen-feinschliff
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/UserManagement.js
autonomous: true
requirements:
  - P19-03
must_haves:
  truths:
    - "Verwaltung-Tab zeigt User als Cards statt Tabellenzeilen"
    - "Jede User-Card zeigt Avatar-Initialen, Name, Email, Kirchengemeinde, Rolle-Badge, Status-Badge"
    - "Bearbeiten und Loeschen Buttons sind direkt auf der Card"
    - "Cards sind responsive: 1 Spalte mobile, 2 Spalten Tablet, 3 Spalten Desktop"
  artifacts:
    - path: "frontend/src/UserManagement.js"
      provides: "Card-based user management layout"
      contains: "grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3"
  key_links:
    - from: "frontend/src/UserManagement.js"
      to: "existing Modal component"
      via: "isCreateModalOpen and isEditModalOpen state"
      pattern: "Modal.*isOpen"
---

<objective>
Verwaltung-Tab von Tabelle zu Card-Layout umbauen

Purpose: Verwaltung-Tab soll das gleiche Card-Design wie Einstellungen haben (per D-07, D-08, D-09, D-10). User werden als individuelle Cards dargestellt statt als Tabellenzeilen.
Output: UserManagement.js mit Card-Grid-Layout, Avatar-Initialen, Rolle/Status-Badges
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-dashboard-listen-feinschliff/19-CONTEXT.md
@.planning/phases/19-dashboard-listen-feinschliff/19-UI-SPEC.md

@frontend/src/UserManagement.js
@frontend/src/components/Settings.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: User-Cards statt Tabelle</name>
  <files>frontend/src/UserManagement.js</files>
  <read_first>frontend/src/UserManagement.js, frontend/src/components/Settings.js, frontend/src/index.css</read_first>
  <action>
**Imports ergaenzen:**

Lucide-Icons importieren: `import { Pencil, Trash2, CheckCircle2, AlertCircle, Plus } from 'lucide-react';`

**Tabellen-Bereich komplett durch Card-Grid ersetzen (per D-07, D-08, D-09, D-10):**

Den gesamten `<div className="table-container">...<table>...</table></div>` Block (Zeilen 225-293) durch folgendes ersetzen:

1. Header mit Zaehler und Button:
```jsx
<div className="flex items-center justify-between mb-6">
  <h2 className="text-base font-semibold text-value">{users.length} Benutzer</h2>
  <button
    onClick={() => setIsCreateModalOpen(true)}
    className="btn-primary flex items-center gap-1"
  >
    <Plus size={16} />
    <span>Neuer Benutzer</span>
  </button>
</div>
```

(Den bisherigen separaten "+ Neuer Benutzer" Button oben entfernen.)

2. Empty State wenn keine User:
```jsx
{users.length === 0 ? (
  <div className="card-container text-center py-12">
    <p className="text-label text-sm">Keine Benutzer vorhanden</p>
    <p className="text-muted text-xs mt-1">Erstelle einen neuen Benutzer ueber den Button oben.</p>
  </div>
) : (
```

3. Card-Grid:
```jsx
<div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
  {users.map(user => {
    const initialen = (user.username || '??').slice(0, 2).toUpperCase();
    const currentUserId = JSON.parse(localStorage.getItem('user') || '{}').id;
    const isOwnUser = user.id === currentUserId;

    return (
      <div key={user.id} className="card-container hover:shadow-card-hover transition-shadow">
        <div className="flex items-start gap-3">
          {/* Avatar mit Initialen */}
          <div className="w-10 h-10 rounded-full bg-primary-100 dark:bg-primary-800 text-primary-700 dark:text-primary-200 font-semibold flex items-center justify-center shrink-0 text-sm">
            {initialen}
          </div>

          <div className="flex-1 min-w-0">
            <div className="flex items-center justify-between gap-2">
              <span className="text-sm font-semibold text-value truncate">{user.username}</span>
              <span className={`text-xs px-2 py-0.5 rounded-full whitespace-nowrap ${
                user.role === 'admin'
                  ? 'bg-primary-100 dark:bg-primary-800 text-primary-700 dark:text-primary-200'
                  : 'bg-primary-50 dark:bg-primary-900 text-primary-600 dark:text-primary-300'
              }`}>
                {user.role === 'admin' ? 'Admin' : 'Benutzer'}
              </span>
            </div>
            <p className="text-xs text-label truncate mt-0.5">{user.email}</p>
            {user.kirchengemeinde && (
              <p className="text-xs text-label truncate">{user.kirchengemeinde}</p>
            )}
          </div>
        </div>

        {/* Status Badge */}
        <div className="mt-3">
          {user.email_verified ? (
            <span className="inline-flex items-center gap-1 text-xs px-2 py-0.5 rounded-full bg-emerald-50 dark:bg-emerald-900/20 text-emerald-600 dark:text-emerald-400">
              <CheckCircle2 size={12} />
              Verifiziert
            </span>
          ) : (
            <span className="inline-flex items-center gap-1 text-xs px-2 py-0.5 rounded-full bg-orange-50 dark:bg-orange-900/20 text-orange-600 dark:text-orange-400">
              <AlertCircle size={12} />
              Nicht verifiziert
            </span>
          )}
        </div>

        {/* Aktionen */}
        <div className="flex items-center gap-2 mt-4 pt-3 border-t border-card">
          <button
            onClick={() => openEditModal(user)}
            className="btn-secondary flex items-center gap-1 text-xs"
            aria-label="Benutzer bearbeiten"
          >
            <Pencil size={13} />
            <span>Bearbeiten</span>
          </button>
          {!isOwnUser && (
            <button
              onClick={() => {
                showNotification(
                  'Benutzer loeschen',
                  `Moechtest du ${user.username} wirklich loeschen?`,
                  () => handleDelete(user.id),
                  true
                );
              }}
              className="btn-destructive flex items-center gap-1 text-xs"
              aria-label="Benutzer loeschen"
            >
              <Trash2 size={13} />
              <span>Loeschen</span>
            </button>
          )}
        </div>
      </div>
    );
  })}
</div>
)}
```

**Bestehende Modal-Logik beibehalten** — die Modals fuer Create und Edit bleiben unveraendert.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Fahrtenbuch && grep -c "grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3" frontend/src/UserManagement.js && grep -c "btn-destructive" frontend/src/UserManagement.js && grep -c "CheckCircle2" frontend/src/UserManagement.js && grep -c "aria-label" frontend/src/UserManagement.js && grep -c "rounded-full" frontend/src/UserManagement.js</automated>
  </verify>
  <acceptance_criteria>
    - UserManagement.js enthaelt `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3` fuer responsives Grid
    - UserManagement.js enthaelt `btn-destructive` fuer Loeschen-Button
    - UserManagement.js enthaelt `CheckCircle2` und `AlertCircle` Imports fuer Status-Badges
    - UserManagement.js enthaelt `Pencil` und `Trash2` Imports fuer Action-Buttons
    - UserManagement.js enthaelt mindestens 2 `aria-label` Attribute
    - UserManagement.js enthaelt `rounded-full` fuer Avatar-Initialen
    - UserManagement.js enthaelt KEINE `<table>`, `<thead>`, `<tbody>`, `<tr>`, `<td>`, `<th>` Elemente mehr
    - UserManagement.js enthaelt `Keine Benutzer vorhanden` als Empty-State Text
    - Die bestehenden Modal-Komponenten (isCreateModalOpen, isEditModalOpen) bleiben erhalten
  </acceptance_criteria>
  <done>Verwaltung-Tab zeigt User als Cards mit Avatar-Initialen, Rolle/Status-Badges und Aktions-Buttons. Responsive Grid mit 1/2/3 Spalten. Loeschen-Button nutzt btn-destructive. Eigener User kann nicht geloescht werden.</done>
</task>

</tasks>

<verification>
- `grep "table" frontend/src/UserManagement.js` zeigt KEINE table/tr/td Elemente mehr
- `grep "grid grid-cols" frontend/src/UserManagement.js` zeigt responsive Grid
- `grep "btn-destructive" frontend/src/UserManagement.js` zeigt roten Loeschen-Button
- `cd frontend && npx react-scripts build 2>&1 | tail -3` kompiliert ohne Fehler
</verification>

<success_criteria>
- Verwaltung-Tab hat Card-Layout statt Tabelle
- Jeder User ist eine Card mit Avatar, Info und Aktionen
- Layout ist responsive (1/2/3 Spalten)
- Card-Design ist konsistent mit Settings Sub-Tabs
</success_criteria>

<output>
After completion, create `.planning/phases/19-dashboard-listen-feinschliff/19-02-SUMMARY.md`
</output>
