---
phase: 19-dashboard-listen-feinschliff
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/index.css
  - frontend/src/FahrtForm.js
  - frontend/src/components/Dashboard.js
autonomous: true
requirements:
  - P19-01
  - P19-02
  - P19-04
  - P19-05
must_haves:
  truths:
    - "btn-destructive Klasse existiert in index.css und erzeugt roten Button"
    - "barGrow Keyframe-Animation existiert und Statistik-Balken animieren beim Mount"
    - "FahrtForm akzeptiert editData, onUpdate, onCancel Props fuer Edit-Modus"
    - "Klick auf Pencil-Icon im Dashboard ersetzt die Fahrt-Card durch FahrtForm vorausgefuellt"
    - "Abbrechen-Button stellt die urspruengliche Card wieder her"
    - "Speichern aktualisiert die Fahrt und zeigt die aktualisierte Card"
  artifacts:
    - path: "frontend/src/index.css"
      provides: "btn-destructive class and barGrow keyframe"
      contains: "btn-destructive"
    - path: "frontend/src/FahrtForm.js"
      provides: "Edit-mode capable form component"
      contains: "editData"
    - path: "frontend/src/components/Dashboard.js"
      provides: "Inline editing on dashboard trip cards"
      contains: "editingFahrtId"
  key_links:
    - from: "frontend/src/components/Dashboard.js"
      to: "frontend/src/FahrtForm.js"
      via: "editData prop with trip data"
      pattern: "editData.*editingFahrt"
---

<objective>
CSS-Grundlagen (btn-destructive, barGrow), FahrtForm Edit-Modus und Dashboard Inline-Bearbeitung

Purpose: Schafft die Basis fuer Inline-Bearbeitung (FahrtForm Edit-Mode) und vereinheitlicht Button-Schema + Chart-Animation. Dashboard bekommt als erstes die Inline-Edit-Funktionalitaet.
Output: btn-destructive Klasse, barGrow Animation, FahrtForm mit Edit-Props, Dashboard mit funktionierender Inline-Bearbeitung
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-dashboard-listen-feinschliff/19-CONTEXT.md
@.planning/phases/19-dashboard-listen-feinschliff/19-UI-SPEC.md

@frontend/src/index.css
@frontend/src/FahrtForm.js
@frontend/src/components/Dashboard.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: CSS-Klassen und FahrtForm Edit-Modus</name>
  <files>frontend/src/index.css, frontend/src/FahrtForm.js</files>
  <read_first>frontend/src/index.css, frontend/src/FahrtForm.js, frontend/src/components/Dashboard.js</read_first>
  <action>
**index.css — btn-destructive Klasse hinzufuegen (per D-12):**

Neben den bestehenden `.btn-primary` und `.btn-secondary` Klassen eine neue `.btn-destructive` Klasse einfuegen:

```css
.btn-destructive {
  @apply bg-secondary-500 dark:bg-secondary-600
         text-white px-4 h-8 rounded-card
         hover:bg-secondary-600 dark:hover:bg-secondary-700
         transition-all duration-200
         text-sm whitespace-nowrap
         disabled:opacity-50 disabled:cursor-not-allowed;
}
```

**index.css — barGrow Keyframe hinzufuegen (per D-11):**

Am Ende der Datei (oder bei den bestehenden @keyframes) hinzufuegen:

```css
@keyframes barGrow {
  from { transform: scaleY(0); }
  to { transform: scaleY(1); }
}
```

**FahrtForm.js — Edit-Modus Props hinzufuegen (per D-01, D-02):**

1. Funktionssignatur aendern von `function FahrtForm()` zu `function FahrtForm({ editData, onUpdate, onCancel })`.

2. Am Anfang der Komponente (nach den useState-Deklarationen) einen useEffect hinzufuegen, der bei vorhandenem `editData` die Formularfelder vorausfuellt:

```javascript
useEffect(() => {
  if (editData) {
    setFormData({
      datum: editData.datum ? editData.datum.slice(0, 10) : '',
      vonOrtId: editData.von_ort_id ? String(editData.von_ort_id) : '',
      nachOrtId: editData.nach_ort_id ? String(editData.nach_ort_id) : '',
      einmaligerVonOrt: editData.einmaliger_von_ort || '',
      einmaligerNachOrt: editData.einmaliger_nach_ort || '',
      anlass: editData.anlass || '',
      manuelleKilometer: editData.kilometer ? String(editData.kilometer) : '',
      abrechnung: editData.abrechnung ? String(editData.abrechnung) : ''
    });
    setUseEinmaligenVonOrt(!!editData.einmaliger_von_ort);
    setUseEinmaligenNachOrt(!!editData.einmaliger_nach_ort);
    if (editData.mitfahrer) setMitfahrer(editData.mitfahrer);
  }
}, [editData]);
```

3. In `handleSubmit`: Wenn `editData` und `onUpdate` vorhanden, statt `addFahrt` einen PUT-Request an `/api/fahrten/${editData.id}` senden mit `updateFahrt` aus AppContext (oder direkt axios.put). Nach Erfolg `onUpdate()` aufrufen statt Form zu resetten. Konkreter Code:

```javascript
// Am Anfang von handleSubmit, nach fahrtData-Erstellung:
if (editData && onUpdate) {
  try {
    await axios.put(`${API_BASE_URL}/fahrten/${editData.id}`, fahrtData);
    showNotification("Erfolg", "Fahrt wurde aktualisiert.");
    onUpdate();
    return;
  } catch (error) {
    console.error('Fehler beim Aktualisieren:', error);
    showNotification("Fehler", "Aenderungen konnten nicht gespeichert werden. Bitte versuche es erneut.");
    return;
  }
}
```

Dabei `const API_BASE_URL = '/api';` oben in der Datei hinzufuegen (falls nicht schon vorhanden).

4. Submit-Button-Text aendern: `{editData ? 'Fahrt speichern' : 'Speichern'}`.

5. Wenn `onCancel` vorhanden, einen Abbrechen-Button neben dem Submit-Button rendern:

```jsx
{onCancel && (
  <button type="button" onClick={onCancel} className="btn-secondary">
    Abbrechen
  </button>
)}
```

6. Wenn `editData` gesetzt, das aeussere `<div className="card-container-highlight">` durch ein einfaches `<div>` ersetzen (kein eigenes Card-Styling im Edit-Modus, da es bereits in einer Card sitzt).
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Fahrtenbuch && grep -c "btn-destructive" frontend/src/index.css && grep -c "barGrow" frontend/src/index.css && grep -c "editData" frontend/src/FahrtForm.js && grep -c "onUpdate" frontend/src/FahrtForm.js && grep -c "onCancel" frontend/src/FahrtForm.js</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/index.css enthaelt `.btn-destructive` mit `bg-secondary-500`
    - frontend/src/index.css enthaelt `@keyframes barGrow` mit `scaleY(0)` und `scaleY(1)`
    - frontend/src/FahrtForm.js enthaelt `editData` in der Funktionssignatur
    - frontend/src/FahrtForm.js enthaelt `onUpdate` in der Funktionssignatur
    - frontend/src/FahrtForm.js enthaelt `onCancel` in der Funktionssignatur
    - frontend/src/FahrtForm.js enthaelt `axios.put` fuer Edit-Modus
    - frontend/src/FahrtForm.js enthaelt `Fahrt speichern` als Button-Text im Edit-Modus
  </acceptance_criteria>
  <done>btn-destructive und barGrow existieren in index.css. FahrtForm akzeptiert editData/onUpdate/onCancel Props und sendet PUT-Request im Edit-Modus.</done>
</task>

<task type="auto">
  <name>Task 2: Dashboard Inline-Bearbeitung und Chart-Animation</name>
  <files>frontend/src/components/Dashboard.js</files>
  <read_first>frontend/src/components/Dashboard.js, frontend/src/FahrtForm.js, frontend/src/index.css</read_first>
  <action>
**Dashboard.js — Inline-Edit State und Logik (per D-01, D-02, D-03):**

1. Neuen State hinzufuegen: `const [editingFahrtId, setEditingFahrtId] = useState(null);`

2. Die editierte Fahrt aus fahrten holen: `const editingFahrt = editingFahrtId ? fahrten.find(f => f.id === editingFahrtId) : null;`

3. Callback-Funktionen fuer FahrtForm:

```javascript
const handleEditComplete = () => {
  setEditingFahrtId(null);
  refreshAllData();
};

const handleEditCancel = () => {
  setEditingFahrtId(null);
};
```

4. Im "Letzte Fahrten" Bereich: In der `letzteTrips.map()` Schleife eine Bedingung einbauen. Wenn `editingFahrtId === fahrt.id`, statt der Card den FahrtForm rendern:

```jsx
{editingFahrtId === fahrt.id ? (
  <div key={fahrt.id} className="rounded-card border border-card p-4 animate-tab-content-fade">
    <FahrtForm
      editData={fahrt}
      onUpdate={handleEditComplete}
      onCancel={handleEditCancel}
    />
  </div>
) : (
  // Bestehende Card-Darstellung
  <div key={fahrt.id} className="flex items-center justify-between rounded-card border border-card p-3 gap-2">
    ...
  </div>
)}
```

5. Den Pencil-Button von `onClick={() => onNavigate && onNavigate('fahrten')}` aendern zu `onClick={() => setEditingFahrtId(fahrt.id)}`. Aria-Label hinzufuegen: `aria-label="Fahrt bearbeiten"`.

6. Trash2-Button: aria-label hinzufuegen: `aria-label="Fahrt loeschen"`.

7. Nochmal-Button: aria-label hinzufuegen: `aria-label="Fahrt kopieren"`.

8. Rueckfahrt-Button: aria-label hinzufuegen: `aria-label="Rueckfahrt erstellen"`.

**Dashboard.js — Chart-Animation (per D-11):**

9. Im km-Balkendiagramm-Bereich die Bars mit der barGrow Animation versehen. Die bestehende `<div>` fuer jeden Balken erhaelt zusaetzliche Inline-Styles:

```jsx
<div
  className="w-full rounded-t bg-primary-500 transition-all duration-300 relative"
  style={{
    height: `${Math.max(heightPercent, 1.5)}%`,
    animation: 'barGrow 400ms ease-out forwards',
    animationDelay: `${i * 50}ms`,
    transformOrigin: 'bottom'
  }}
  title={`${km.toFixed(1)} km | ${fahrtenProMonat[i]} Fahrten`}
>
```

10. Um die Animation bei Year-Wechsel neu auszuloesen, einen key auf den Container setzen: `<div key={statistikJahr} className="flex items-end gap-1" style={{ height: '160px' }}>` — der key-Wechsel erzwingt ein Remount.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Fahrtenbuch && grep -c "editingFahrtId" frontend/src/components/Dashboard.js && grep -c "handleEditComplete" frontend/src/components/Dashboard.js && grep -c "barGrow" frontend/src/components/Dashboard.js && grep -c "aria-label" frontend/src/components/Dashboard.js</automated>
  </verify>
  <acceptance_criteria>
    - Dashboard.js enthaelt `const [editingFahrtId, setEditingFahrtId] = useState(null)`
    - Dashboard.js enthaelt `handleEditComplete` und `handleEditCancel` Funktionen
    - Dashboard.js enthaelt `editData={fahrt}` im FahrtForm-Aufruf
    - Dashboard.js enthaelt `onUpdate={handleEditComplete}` im FahrtForm-Aufruf
    - Dashboard.js enthaelt `onCancel={handleEditCancel}` im FahrtForm-Aufruf
    - Dashboard.js enthaelt `setEditingFahrtId(fahrt.id)` im Pencil-Button onClick
    - Dashboard.js enthaelt `barGrow` als animation-Name in den Chart-Bars
    - Dashboard.js enthaelt `key={statistikJahr}` auf dem Chart-Container
    - Dashboard.js enthaelt mindestens 4 aria-label Attribute
  </acceptance_criteria>
  <done>Dashboard zeigt Inline-Edit via FahrtForm beim Pencil-Klick. Statistik-Balken animieren mit barGrow beim Mount und bei Year-Wechsel. Alle Action-Buttons haben aria-labels.</done>
</task>

</tasks>

<verification>
- `grep "btn-destructive" frontend/src/index.css` zeigt die neue Klasse
- `grep "barGrow" frontend/src/index.css` zeigt die Keyframe-Animation
- `grep "editData" frontend/src/FahrtForm.js` zeigt Edit-Modus Props
- `grep "editingFahrtId" frontend/src/components/Dashboard.js` zeigt Inline-Edit State
- `grep "barGrow" frontend/src/components/Dashboard.js` zeigt Chart-Animation
- `cd frontend && npx react-scripts build 2>&1 | tail -3` kompiliert ohne Fehler
</verification>

<success_criteria>
- btn-destructive CSS-Klasse erzeugt roten Button mit secondary-500 Hintergrund
- barGrow Animation laesst Balken von unten wachsen
- FahrtForm kann im Edit-Modus mit vorausgefuellten Daten gerendert werden
- Dashboard Pencil-Button oeffnet Inline-Edit statt zu navigieren
- Abbrechen schliesst Edit, Speichern aktualisiert Fahrt
- Chart-Balken animieren beim Mount und bei Jahreswechsel
</success_criteria>

<output>
After completion, create `.planning/phases/19-dashboard-listen-feinschliff/19-01-SUMMARY.md`
</output>
