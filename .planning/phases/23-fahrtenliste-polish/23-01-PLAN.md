---
phase: 23-fahrtenliste-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/FahrtenListe.js
autonomous: false
requirements: [FL-01, FL-02, FL-03]

must_haves:
  truths:
    - "Abrechnungs-Bereich hat Section-Header mit CreditCard Icon"
    - "Zeitraum-Filter hat Section-Header mit CalendarRange Icon"
    - "Export-Bereich hat Section-Header mit FileDown Icon"
    - "Export-Buttons zeigen FileSpreadsheet/FileText Icons"
    - "Card-Layout hat klare Sektionen mit konsistentem Whitespace"
  artifacts:
    - path: "frontend/src/components/FahrtenListe.js"
      provides: "Polished FahrtenListe with section-headers and export icons"
      contains: "section-header"
  key_links:
    - from: "FahrtenListe.js"
      to: "index.css"
      via: "section-header CSS class"
      pattern: "section-header"
    - from: "FahrtenListe.js"
      to: "lucide-react"
      via: "icon imports"
      pattern: "CreditCard.*FileDown.*CalendarRange"
---

<objective>
FahrtenListe auf Dashboard-Niveau bringen: Section-Headers mit Lucide-Icons fuer alle Bereiche, strukturiertes Card-Layout und aufgewerteter Export-Bereich mit Icons.

Purpose: Visuelle Konsistenz zwischen Dashboard und FahrtenListe herstellen
Output: FahrtenListe.js mit Section-Headers, Card-Struktur und Export-Icons
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-globale-patterns/22-01-SUMMARY.md
@.planning/phases/22-globale-patterns/22-02-SUMMARY.md
@.planning/phases/23-fahrtenliste-polish/23-CONTEXT.md

<interfaces>
<!-- CSS patterns from Phase 22 (index.css) -->
.section-header: flex items-center gap-2 mb-3
.section-header h2: text-base font-medium text-value
.section-header .section-count: text-sm text-muted
.card-container: standard card wrapper
.card-container-highlight: highlighted card variant

<!-- Dashboard section-header usage pattern -->
<div className="section-header">
  <IconName size={18} className="text-color-500" />
  <h2>Section Title</h2>
  <span className="section-count">(count)</span>
</div>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Section-Headers und Card-Struktur (FL-01, FL-02)</name>
  <files>frontend/src/components/FahrtenListe.js</files>
  <read_first>
    - frontend/src/components/FahrtenListe.js
    - frontend/src/components/Dashboard.js (lines 258-310, section-header pattern reference)
    - frontend/src/index.css (search for section-header class definition)
  </read_first>
  <action>
    Per FL-01 und FL-02: Section-Headers mit Lucide-Icons zu allen Bereichen der FahrtenListe hinzufuegen.

    1. **Lucide Imports erweitern** (Zeile 8):
       Aktuelle Imports: AlertCircle, Circle, CheckCircle2, Pencil, Trash2, RotateCcw, Users, Clock
       Hinzufuegen: CreditCard, FileDown, CalendarRange, FileSpreadsheet, FileText

    2. **Abrechnungs-Bereich Section-Header** (innerhalb renderAbrechnungsStatus, vor dem Header mit Navigation, ca. Zeile 386):
       Das bestehende h2 "Monatsübersicht"/"Zeitraum-Übersicht" (Zeile 389) durch ein section-header ersetzen:
       ```jsx
       <div className="section-header">
         <CreditCard size={18} className="text-emerald-500" />
         <h2>{selectedVonMonth && selectedVonMonth !== selectedMonth ? 'Zeitraum-Übersicht' : 'Monatsübersicht'}</h2>
       </div>
       ```
       Das bestehende `<h2 className="text-lg font-medium text-value">` entfernen und durch das section-header Pattern ersetzen. Der "Aktueller Monat" Button bleibt daneben — die flex-Struktur drumherum bleibt bestehen.

    3. **Zeitraum-Filter Section-Header** (vor den Von/Bis-Dropdowns, ca. Zeile 405):
       Einen neuen Section-Header ueber den Von/Bis-Dropdowns einfuegen:
       ```jsx
       <div className="section-header">
         <CalendarRange size={18} className="text-blue-500" />
         <h2>Zeitraum</h2>
       </div>
       ```
       Diesen Header als Label fuer die Von/Bis-Dropdowns verwenden. Die Dropdowns selbst bleiben unveraendert.

    4. **Cards Grid Section-Header** (vor dem grid div, ca. Zeile 476):
       Einen Section-Header mit Zaehler vor dem Cards Grid einfuegen:
       ```jsx
       <div className="section-header">
         <CreditCard size={18} className="text-emerald-500" />
         <h2>Erstattungen</h2>
         <span className="section-count">({getKategorienMitErstattung().length})</span>
       </div>
       ```

    5. **Card-Struktur verbessern** (FL-02):
       - Zwischen den Hauptsektionen (Zeitraum-Filter, Erstattungen-Grid, Gesamt, Export) konsistentes Spacing sicherstellen: `space-y-6` ist bereits vorhanden (Zeile 385)
       - Die card-container-highlight (Zeile 384) um den gesamten Abrechnungsstatus bleibt
       - Fahrten-Section (Zeile 692+) hat bereits section-header mit Clock Icon — das ist korrekt

    WICHTIG: Das Pattern MUSS identisch zum Dashboard sein. Section-Header = div.section-header > Icon(size=18) + h2 + optional span.section-count.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Fahrtenbuch && grep -c "section-header" frontend/src/components/FahrtenListe.js</automated>
  </verify>
  <acceptance_criteria>
    - grep "CreditCard" frontend/src/components/FahrtenListe.js findet mindestens 1 Treffer
    - grep "CalendarRange" frontend/src/components/FahrtenListe.js findet mindestens 1 Treffer
    - grep "section-header" frontend/src/components/FahrtenListe.js findet mindestens 4 Treffer (Abrechnungs-Header, Zeitraum, Erstattungen, Fahrten)
    - Frontend Build kompiliert ohne Fehler: cd frontend && npx react-scripts build 2>&1 | tail -5
  </acceptance_criteria>
  <done>FahrtenListe hat Section-Headers mit Lucide-Icons in allen Bereichen (Abrechnungsstatus, Zeitraum-Filter, Erstattungen-Grid, Fahrten-Liste), identisches Pattern wie Dashboard</done>
</task>

<task type="auto">
  <name>Task 2: Export-Bereich aufwerten (FL-03)</name>
  <files>frontend/src/components/FahrtenListe.js</files>
  <read_first>
    - frontend/src/components/FahrtenListe.js (nach Task 1 Aenderungen — Zeilen um 571-594 Export-Bereich)
  </read_first>
  <action>
    Per FL-03: Den Export-Bereich (aktuell Zeile 571-594) visuell aufwerten mit Section-Header und Button-Icons.

    1. **Export Section-Header** hinzufuegen (vor den Export-Buttons):
       ```jsx
       <div className="section-header">
         <FileDown size={18} className="text-purple-500" />
         <h2>Export</h2>
       </div>
       ```

    2. **Export-Buttons mit Icons versehen**:
       Die bestehenden dynamisch generierten Export-Buttons (getKategorienMitErstattung().map) erhalten Icons.
       Statt nur "Export {displayName}" soll jeder Button so aussehen:
       ```jsx
       <button
         key={key}
         onClick={() => {
           const typeKey = key.toLowerCase();
           showNotification(
             "Export " + displayName,
             "In welchem Format möchten Sie exportieren?",
             () => handleExportToExcel(typeKey),
             true,
             "Excel",
             () => handleExportToPdf(typeKey),
             "PDF",
             () => handleExportBoth(typeKey),
             "Beide"
           );
         }}
         className="btn-primary flex items-center gap-1.5">
         <FileSpreadsheet size={14} />
         Export {displayName}
       </button>
       ```
       (FileSpreadsheet als Standard-Icon, da der primaere Anwendungsfall Excel ist)

    3. **Export-Container als eigene Card-Sektion**:
       Den Export-Bereich in eine eigene Sektion mit leichtem Hintergrund wrappen — NICHT card-container (ist schon innerhalb von card-container-highlight), sondern nur den Section-Header + Buttons-Row als eigene Gruppe:
       ```jsx
       <div className="pt-4 border-t border-card">
         <div className="section-header">
           <FileDown size={18} className="text-purple-500" />
           <h2>Export</h2>
         </div>
         <div className="flex flex-col sm:flex-row gap-2">
           {/* buttons hier */}
         </div>
       </div>
       ```
       Das `justify-end` und `mt-6` vom aktuellen div entfernen, stattdessen `pt-4 border-t border-card` als visuelle Trennung.

    WICHTIG: Die onClick-Logik der Buttons NICHT aendern. Nur visuelles Upgrade.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Fahrtenbuch && grep -c "FileDown\|FileSpreadsheet" frontend/src/components/FahrtenListe.js</automated>
  </verify>
  <acceptance_criteria>
    - grep "FileDown" frontend/src/components/FahrtenListe.js findet mindestens 1 Treffer
    - grep "FileSpreadsheet" frontend/src/components/FahrtenListe.js findet mindestens 1 Treffer
    - grep "border-t border-card" frontend/src/components/FahrtenListe.js findet mindestens 1 Treffer (Export-Trennung)
    - Frontend Build kompiliert ohne Fehler: cd frontend && npx react-scripts build 2>&1 | tail -5
  </acceptance_criteria>
  <done>Export-Bereich hat Section-Header mit FileDown Icon, Export-Buttons haben FileSpreadsheet Icons, Bereich ist visuell abgetrennt mit border-top</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>FahrtenListe mit Section-Headers (CreditCard, CalendarRange, CreditCard+Count, Clock, FileDown) und Export-Buttons mit Icons — visuell auf Dashboard-Niveau</what-built>
  <how-to-verify>
    1. App oeffnen: https://kkd-fahrtenbuch.de (oder lokale Dev-Umgebung)
    2. Zum "Fahrten & Export" Tab navigieren
    3. Pruefen:
       - Abrechnungs-Header hat CreditCard Icon + "Monatsuebersicht" Text
       - Zeitraum-Bereich hat CalendarRange Icon + "Zeitraum" Header
       - Erstattungen-Grid hat CreditCard Icon + "Erstattungen" + Zaehler
       - Export hat FileDown Icon + "Export" Header, Buttons haben FileSpreadsheet Icons
       - Fahrten-Liste hat Clock Icon + "Fahrten" + Zaehler (bereits von Phase 22)
    4. Mobile-Ansicht pruefen (Browser responsive mode)
    5. Visuell vergleichen: Sehen die Section-Headers aus wie auf dem Dashboard?
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Alle Section-Headers nutzen identisches Pattern wie Dashboard (.section-header > Icon + h2 + optional .section-count)
- Mindestens 5 section-header Instanzen in FahrtenListe.js
- Export-Buttons haben Icons (FileSpreadsheet)
- Export-Bereich hat eigenen Section-Header (FileDown)
- Frontend Build kompiliert erfolgreich
</verification>

<success_criteria>
- FahrtenListe hat Section-Headers mit Lucide-Icons in allen Bereichen (FL-01)
- Card-Layout ist visuell strukturiert wie Dashboard mit klaren Sektionen und konsistentem Whitespace (FL-02)
- Export-Bereich hat Icons und klare visuelle Struktur (FL-03)
- Mobile und Desktop sehen korrekt aus
</success_criteria>

<output>
After completion, create `.planning/phases/23-fahrtenliste-polish/23-01-SUMMARY.md`
</output>
