---
phase: 11-dashboard-navigation-statistiken
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/AppContent.js
  - frontend/src/components/Dashboard.js
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-03

must_haves:
  truths:
    - "Navigation zeigt vier Tabs: Dashboard, Fahrten & Export, Monatsuebersicht, Einstellungen"
    - "Dashboard ist Default-Tab beim Laden der App"
    - "Dashboard zeigt KPI-Cards: offene Erstattungen, km diesen Monat, Fahrten diesen Monat"
    - "Dashboard hat aufklappbares Fahrt-Formular (bestehendes FahrtForm eingebettet)"
    - "Dashboard zeigt Favoriten-Schnelleingabe (Klick traegt Fahrt ein)"
    - "Dashboard zeigt letzte 3 Fahrten mit Nochmal-Button"
    - "Bestehende Views FahrtenListe und MonthlyOverview funktionieren wie bisher in ihren Tabs"
    - "Einstellungen-Tab oeffnet Profil-Modal"
  artifacts:
    - path: "frontend/src/components/Dashboard.js"
      provides: "Dashboard-Komponente mit KPIs, Formular, Favoriten, Quick-Copy"
      min_lines: 100
    - path: "frontend/src/components/AppContent.js"
      provides: "Tab-Navigation mit 4 Tabs und Tab-Routing"
  key_links:
    - from: "frontend/src/components/Dashboard.js"
      to: "frontend/src/contexts/AppContext.js"
      via: "useContext(AppContext)"
      pattern: "useContext.*AppContext"
    - from: "frontend/src/components/Dashboard.js"
      to: "frontend/src/FahrtForm.js"
      via: "import und Einbettung"
      pattern: "import FahrtForm"
    - from: "frontend/src/components/AppContent.js"
      to: "frontend/src/components/Dashboard.js"
      via: "import und Tab-Rendering"
      pattern: "import Dashboard"
---

<objective>
Tab-Navigation in AppContent.js einbauen und neue Dashboard-Komponente erstellen mit KPI-Cards, aufklappbarem Fahrt-Formular, Favoriten-Schnelleingabe und Quick-Copy der letzten 3 Fahrten.

Purpose: Dashboard als neue Startseite mit Schnellzugriff auf alle wichtigen Funktionen, Tab-Navigation fuer bessere Struktur.
Output: Dashboard.js (neu), AppContent.js (umgebaut mit Tabs)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-dashboard-navigation-statistiken/11-CONTEXT.md

@frontend/src/components/AppContent.js
@frontend/src/contexts/AppContext.js
@frontend/src/FahrtForm.js
@frontend/src/components/FahrtenListe.js

<interfaces>
<!-- Key state and functions from AppContext.js that Dashboard.js needs -->

From frontend/src/contexts/AppContext.js:
- `summary` state: Objekt mit `erstattungen` (Object: traegerName -> Betrag), `abrechnungsStatus`, `gesamtErstattung` (number)
- `monthlyData` state: Array von `{ yearMonth, monthName, year, monatNr, erstattungen, abrechnungsStatus, totalErstattung }`
- `fahrten` state: Array von Fahrt-Objekten (mit id, datum, von_ort_name, nach_ort_name, anlass, kilometer, abrechnung, etc.)
- `favoriten` state: Array von Favorit-Objekten (mit id, von_ort_id, nach_ort_id, anlass, abrechnung, von_ort_name, nach_ort_name)
- `executeFavorit(id)`: POST /api/favoriten/:id/execute, erstellt Fahrt mit heutigem Datum, ruft refreshAllData()
- `refreshAllData()`: Aktualisiert fahrten, monthlyData, orte, distanzen, abrechnungstraeger
- `showNotification(title, message)`: Zeigt Benachrichtigung
- `selectedMonth`, `setSelectedMonth`: Aktueller Monat fuer Report
- `isProfileModalOpen`, `setIsProfileModalOpen`: Profil-Modal Steuerung

From frontend/src/FahrtForm.js:
- Default export `FahrtForm` — React-Komponente, keine Props noetig
- Nutzt useContext(AppContext) intern fuer alle Daten
- Kann direkt eingebettet werden: `<FahrtForm />`

From frontend/src/components/FahrtenListe.js:
- Default export `FahrtenListe` — React-Komponente, keine Props
- Enthaelt bereits handleNochmal und letzteDreiIds Logik
- Quick-Copy Logik im Dashboard muss NICHT aus FahrtenListe importiert werden — eigene Implementierung mit gleicher axios-POST Logik
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tab-Navigation in AppContent.js einbauen</name>
  <files>frontend/src/components/AppContent.js</files>
  <read_first>frontend/src/components/AppContent.js, frontend/src/contexts/AppContext.js</read_first>
  <action>
AppContent.js umbauen: Tab-Navigation mit 4 Tabs einfuegen (per D-07, D-08, D-09, D-10).

1. State hinzufuegen: `const [activeTab, setActiveTab] = useState('dashboard')` — Dashboard ist Default (per D-08)

2. Tab-Navigation-Bar UNTER dem Header einfuegen (zwischen Header-Section und Hauptinhalt):
   - Vier Tabs: "Dashboard" (LayoutDashboard icon), "Fahrten & Export" (Car icon), "Monatsuebersicht" (Calendar icon), "Einstellungen" (Settings icon)
   - Tabs als horizontale Button-Leiste mit Tailwind: `flex gap-1 border-b border-border`
   - Aktiver Tab: `border-b-2 border-primary-500 text-primary-600 font-medium`
   - Inaktiver Tab: `text-muted hover:text-value`
   - Icons aus lucide-react: LayoutDashboard, Car, CalendarDays, Settings
   - Mobile: Gleiche Tab-Bar, Icons + Text nebeneinander, bei sehr kleinen Screens nur Icons (per D-11 — pragmatisch Bottom-Tabs sind uebertrieben fuer diese App)

3. Hauptinhalt-Bereich (bisher `<div className="space-y-6">`) aendern zu bedingtem Rendering:
   ```jsx
   {activeTab === 'dashboard' && <Dashboard />}
   {activeTab === 'fahrten' && (
     <div className="space-y-6">
       <FahrtForm />
       <FahrtenListe />
     </div>
   )}
   {activeTab === 'monatsuebersicht' && <MonthlyOverview />}
   ```
   - "Einstellungen"-Tab: KEIN eigener Content — beim Klick `setIsProfileModalOpen(true)` aufrufen und Tab NICHT wechseln (per D-10). Also im onClick: `() => setIsProfileModalOpen(true)` statt `() => setActiveTab('einstellungen')`.

4. Import hinzufuegen: `import Dashboard from './Dashboard';` und die Lucide-Icons (LayoutDashboard, Car, CalendarDays).

5. Den bestehenden Settings-Button im Header ENTFERNEN (ist jetzt im Tab). Den Benutzerverwaltung-Button bleibt im Header (Admin only).

6. Bestehende Funktionalitaet beibehalten: Header mit Titel, Admin-Button, Theme-Toggle, Info, Bell, Hilfe, Logout — alles bleibt wie es ist.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Fahrtenbuch/frontend && npx react-scripts build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - AppContent.js hat activeTab State mit Default 'dashboard'
    - 4 Tab-Buttons sind sichtbar (Dashboard, Fahrten & Export, Monatsuebersicht, Einstellungen)
    - Dashboard-Tab rendert Dashboard-Komponente
    - Fahrten-Tab rendert FahrtForm + FahrtenListe
    - Monatsuebersicht-Tab rendert MonthlyOverview
    - Einstellungen-Tab oeffnet Profil-Modal ohne Tab-Wechsel
    - Build kompiliert ohne Fehler
  </acceptance_criteria>
  <done>Tab-Navigation sichtbar, alle 4 Tabs navigieren korrekt, bestehende Views funktionieren unveraendert</done>
</task>

<task type="auto">
  <name>Task 2: Dashboard-Komponente erstellen</name>
  <files>frontend/src/components/Dashboard.js</files>
  <read_first>frontend/src/contexts/AppContext.js, frontend/src/FahrtForm.js, frontend/src/components/FahrtenListe.js (Zeilen 85-120 fuer handleNochmal)</read_first>
  <action>
Neue Datei `frontend/src/components/Dashboard.js` erstellen (per D-01 bis D-04).

1. **KPI-Cards** (per D-01, implements DASH-01):
   Drei Cards in einer Reihe (grid grid-cols-1 sm:grid-cols-3 gap-4):

   a) "Offene Erstattungen": Summe aus `summary.erstattungen` (Object.values().reduce(sum)). Format: `XX,XX €`. Icon: Banknote oder Euro von lucide-react. Hintergrundfarbe: leichtes Gruen (bg-emerald-50 dark:bg-emerald-900/20).

   b) "Kilometer diesen Monat": Summe der `fahrten` im aktuellen Monat filtern (datum beginnt mit aktuellem YYYY-MM) und deren `.kilometer` summieren. Icon: Route. Hintergrund: leichtes Blau.

   c) "Fahrten diesen Monat": Anzahl der Fahrten im aktuellen Monat (gleicher Filter wie b). Icon: Car. Hintergrund: leichtes Lila.

   KPI-Card Design: `rounded-lg p-4 shadow-sm border border-border` mit Icon links, Zahl gross rechts, Label klein darunter. Bestehende CSS-Variablen/Tailwind-Klassen nutzen (card, text-value, text-muted etc.).

2. **Aufklappbares Fahrt-Formular** (per D-02, implements DASH-02):
   - Collapsible Section mit Chevron-Toggle
   - Titel: "Neue Fahrt erfassen"
   - Default: zugeklappt
   - State: `const [isFormOpen, setIsFormOpen] = useState(false)`
   - Wenn offen: `<FahrtForm />` einbetten (keine Props noetig, nutzt AppContext intern)
   - Styling: `card p-4` Container, ChevronDown/ChevronUp Icon im Header

3. **Favoriten-Schnelleingabe** (per D-03, implements teilweise DASH-02):
   - Sektion "Favoriten" mit Stern-Icon
   - `favoriten` aus AppContext, `.map()` ueber alle Favoriten
   - Jeder Favorit als kompakter Button/Card: `von_ort_name → nach_ort_name` mit Anlass darunter
   - onClick: `executeFavorit(favorit.id)` aufrufen, dann `showNotification('Fahrt erstellt', 'Favorit wurde fuer heute eingetragen.')`
   - Wenn keine Favoriten: "Keine Favoriten gespeichert. Erstelle Favoriten in den Einstellungen." anzeigen
   - Fehlerbehandlung: try/catch um executeFavorit, bei Fehler showNotification('Fehler', ...)

4. **Letzte 3 Fahrten mit Nochmal-Button** (per D-04):
   - Sektion "Letzte Fahrten"
   - Gleiche Logik wie FahrtenListe: `fahrten` sortieren nach Datum (neueste zuerst), erste 3 nehmen
   - Jede Fahrt als kompakte Zeile: Datum | Von → Nach | km | Nochmal-Button
   - Nochmal-Button: POST `/api/fahrten` mit gleichem Payload wie handleNochmal in FahrtenListe:
     ```js
     await axios.post('/api/fahrten', {
       vonOrtId: fahrt.von_ort_id || null,
       nachOrtId: fahrt.nach_ort_id || null,
       datum: new Date().toISOString().slice(0, 10),
       anlass: fahrt.anlass,
       abrechnung: fahrt.abrechnung,
       einmaligerVonOrt: fahrt.einmaliger_von_ort || null,
       einmaligerNachOrt: fahrt.einmaliger_nach_ort || null,
       kilometer: fahrt.kilometer
     });
     ```
   - Nach Erfolg: `refreshAllData()` + `showNotification(...)`
   - Wenn keine Fahrten: "Noch keine Fahrten erfasst." anzeigen

5. **Layout-Reihenfolge im Dashboard:**
   - KPI-Cards (oben)
   - Favoriten-Schnelleingabe
   - Aufklappbares Formular
   - Letzte 3 Fahrten
   - (Statistiken kommen in Plan 02)

6. **Imports:** React, useContext, useState, useMemo, AppContext, FahrtForm, axios, Lucide-Icons (Banknote, Route, Car, Star, ChevronDown, ChevronUp, RotateCcw).

7. **Responsive:** Alle Sektionen stacken auf Mobile (grid-cols-1), KPI-Cards nebeneinander ab sm.

8. App nicht ueberladen — pragmatisches Design beibehalten. Keine Animationen, keine Transitions.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Fahrtenbuch/frontend && npx react-scripts build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Dashboard.js existiert mit KPI-Cards, Formular, Favoriten, Quick-Copy
    - KPI-Cards zeigen Erstattungen (€), km diesen Monat, Fahrten diesen Monat
    - FahrtForm ist eingebettet und aufklappbar
    - Favoriten-Buttons rufen executeFavorit auf
    - Letzte-3-Fahrten-Sektion mit Nochmal-Buttons
    - Build kompiliert ohne Fehler
  </acceptance_criteria>
  <done>Dashboard-Komponente rendert KPI-Cards mit korrekten Daten, Formular ist aufklappbar, Favoriten funktionieren per Klick, Nochmal-Buttons duplizieren Fahrten</done>
</task>

</tasks>

<verification>
1. `cd frontend && npx react-scripts build` kompiliert ohne Fehler
2. Dashboard ist Default-Tab nach Login
3. Alle 4 Tabs navigieren zu korrektem Inhalt
4. KPI-Cards zeigen reale Daten aus AppContext
5. Favoriten-Klick erstellt Fahrt
6. Nochmal-Button erstellt Fahrt fuer heute
7. Bestehende Views (FahrtenListe, MonthlyOverview) funktionieren unveraendert
</verification>

<success_criteria>
- Tab-Navigation mit 4 Tabs sichtbar und funktional
- Dashboard als Startseite mit KPIs, Formular, Favoriten, Quick-Copy
- Build kompiliert fehlerfrei
- Responsive auf Desktop und Mobile
</success_criteria>

<output>
After completion, create `.planning/phases/11-dashboard-navigation-statistiken/11-01-SUMMARY.md`
</output>
