---
phase: 22-globale-patterns
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/index.css
autonomous: true
requirements:
  - GP-01
  - GP-03
  - GP-04

must_haves:
  truths:
    - "btn-primary, btn-secondary und btn-destructive haben alle h-10 (40px)"
    - "Eine .kpi-card CSS-Klasse existiert mit Farbvarianten (emerald, blue, purple, primary)"
    - "Eine .section-header CSS-Klasse existiert mit flex, items-center, gap-2, mb-3"
  artifacts:
    - path: "frontend/src/index.css"
      provides: "Button-Fix, .kpi-card, .kpi-card-emerald/blue/purple/primary, .section-header Klassen"
      contains: ".kpi-card"
  key_links:
    - from: "frontend/src/index.css"
      to: "all components using btn-primary"
      via: "CSS class inheritance"
      pattern: "\\.btn-primary.*h-10"
---

<objective>
CSS-Fundament bereinigen: Button-Hoehen vereinheitlichen, KPI-Card-Pattern und Section-Header-Pattern als wiederverwendbare CSS-Klassen definieren.

Purpose: Alle nachfolgenden Komponenten-Aenderungen (Plan 02) basieren auf diesen zentralen CSS-Klassen.
Output: Bereinigte index.css mit drei neuen Pattern-Klassen.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-globale-patterns/22-CONTEXT.md
@.planning/phases/22-globale-patterns/22-UI-SPEC.md
@frontend/src/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Button-Hoehen vereinheitlichen (GP-01)</name>
  <files>frontend/src/index.css</files>
  <read_first>frontend/src/index.css</read_first>
  <action>
In frontend/src/index.css die Button-Klassen anpassen:

1. `.btn-primary` (ca. Zeile 271-278): `h-8` aendern zu `h-10`
2. `.btn-destructive` (ca. Zeile 292-299): `h-8` aendern zu `h-10`
3. `.btn-secondary` (ca. Zeile 280-290): bereits `h-10`, keine Aenderung noetig

Ergebnis: Alle drei btn-Klassen haben identisch `h-10` in ihrer @apply Direktive.
  </action>
  <verify>
    <automated>grep -n "h-8" frontend/src/index.css | grep -i btn && echo "FAIL: btn still has h-8" || echo "PASS: no btn with h-8"</automated>
  </verify>
  <acceptance_criteria>
- `grep "btn-primary" -A5 frontend/src/index.css` enthaelt `h-10`
- `grep "btn-destructive" -A5 frontend/src/index.css` enthaelt `h-10`
- `grep "h-8" frontend/src/index.css` liefert KEINE Treffer in btn-primary oder btn-destructive
  </acceptance_criteria>
  <done>btn-primary, btn-secondary und btn-destructive haben alle h-10 (40px)</done>
</task>

<task type="auto">
  <name>Task 2: KPI-Card und Section-Header CSS-Klassen definieren (GP-03, GP-04)</name>
  <files>frontend/src/index.css</files>
  <read_first>frontend/src/index.css, .planning/phases/22-globale-patterns/22-UI-SPEC.md</read_first>
  <action>
In frontend/src/index.css nach den Button-Klassen (nach Zeile 299) neue Klassen einfuegen:

**KPI-Card Pattern (GP-03):**
```css
/* KPI Cards */
.kpi-card {
  @apply rounded-card p-4 shadow-card border border-card;
}

.kpi-card-emerald {
  @apply bg-emerald-50 dark:bg-emerald-900/20;
}

.kpi-card-blue {
  @apply bg-blue-50 dark:bg-blue-900/20;
}

.kpi-card-purple {
  @apply bg-purple-50 dark:bg-purple-900/20;
}

.kpi-card-primary {
  @apply bg-primary-50 dark:bg-primary-900/30;
}
```

**Section-Header Pattern (GP-04):**
```css
/* Section Headers */
.section-header {
  @apply flex items-center gap-2 mb-3;
}

.section-header h2 {
  @apply text-base font-medium text-value;
}

.section-header .section-count {
  @apply text-sm text-muted;
}
```

Position: Direkt nach `.btn-destructive` Block (nach Zeile 299), vor den Table Styles.
  </action>
  <verify>
    <automated>grep -c "\.kpi-card" frontend/src/index.css | xargs -I{} test {} -ge 5 && grep -c "\.section-header" frontend/src/index.css | xargs -I{} test {} -ge 2 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
- `grep ".kpi-card {" frontend/src/index.css` liefert mindestens 1 Treffer
- `grep ".kpi-card-emerald" frontend/src/index.css` liefert mindestens 1 Treffer
- `grep ".kpi-card-blue" frontend/src/index.css` liefert mindestens 1 Treffer
- `grep ".kpi-card-purple" frontend/src/index.css` liefert mindestens 1 Treffer
- `grep ".kpi-card-primary" frontend/src/index.css` liefert mindestens 1 Treffer
- `grep ".section-header {" frontend/src/index.css` liefert mindestens 1 Treffer
- `grep "section-header" frontend/src/index.css` enthaelt `flex items-center gap-2 mb-3`
  </acceptance_criteria>
  <done>KPI-Card-Klassen (.kpi-card + 4 Farbvarianten) und Section-Header-Klasse (.section-header) existieren in index.css</done>
</task>

</tasks>

<verification>
- `grep "h-10" frontend/src/index.css` zeigt h-10 in allen drei btn-Klassen
- `grep "\.kpi-card" frontend/src/index.css` zeigt Basis-Klasse + 4 Varianten
- `grep "\.section-header" frontend/src/index.css` zeigt Header-Pattern
- `cd frontend && npx tailwindcss --help > /dev/null 2>&1` — Tailwind ist installiert (Build wuerde funktionieren)
</verification>

<success_criteria>
1. Alle btn-* Klassen nutzen h-10
2. .kpi-card Basis-Klasse und 4 Farbvarianten (.kpi-card-emerald, .kpi-card-blue, .kpi-card-purple, .kpi-card-primary) existieren
3. .section-header Klasse existiert mit flex items-center gap-2 mb-3
</success_criteria>

<output>
After completion, create `.planning/phases/22-globale-patterns/22-01-SUMMARY.md`
</output>
