---
phase: 04-von-bis-zeitraum-in-monats-bersicht
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - frontend/src/App.js
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05
  - D-06
  - D-07
  - D-08
  - D-13
  - D-14
  - D-15
  - D-16
  - D-17
  - D-18
  - D-19

must_haves:
  truths:
    - "Nutzer sieht Von-Dropdown und Bis-Dropdown statt einzelnem Monats-Dropdown"
    - "Von steht auf '---' per Default (Einzelmonat-Modus)"
    - "Bis steht auf aktuellem Monat per Default"
    - "Wenn Von = '---' werden Fahrten fuer den Bis-Monat geladen (Einzelmonat)"
    - "Wenn Von != '---' und Von != Bis werden Fahrten ueber Zeitraum geladen (Range-API)"
    - "Export-Buttons nutzen im Zeitraum-Modus die Range-Export-Route"
    - "Der separate Zeitraum-Export-Bereich aus Phase 3 ist entfernt"
    - "exportVonYear/Month und exportBisYear/Month State-Variablen sind entfernt"
    - "handleExportToExcelRange ist entfernt, Logik in handleExportToExcel integriert"
  artifacts:
    - path: "frontend/src/App.js"
      provides: "Von/Bis UI, Range-Datenloading, integrierter Export"
      contains: "selectedVonMonth"
  key_links:
    - from: "frontend/src/App.js"
      to: "/api/fahrten/report-range/"
      via: "fetchFahrten conditional"
      pattern: "report-range"
    - from: "frontend/src/App.js"
      to: "/api/fahrten/export-range/"
      via: "handleExportToExcel conditional"
      pattern: "export-range"
---

<objective>
Frontend: Von/Bis-Dropdowns ersetzen die Monatsauswahl, fetchFahrten nutzt Range-API bei Zeitraum, Export-Buttons adaptieren sich, Phase-3-Zeitraum-UI wird entfernt.

Purpose: Nutzer Benjamin will mehrere Monate in einer Tabelle sehen — Von/Bis oben loest das elegant, ohne separaten Zeitraum-Export-Bereich.
Output: Komplett integrierte Von/Bis-Monatsuebersicht mit adaptivem Export
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-von-bis-zeitraum-in-monats-bersicht/04-01-SUMMARY.md
@frontend/src/App.js

<interfaces>
<!-- New backend endpoint from Plan 01 -->
GET /api/fahrten/report-range/:startYear/:startMonth/:endYear/:endMonth
Response: { fahrten: [...], summary: { erstattungen: {}, gesamtErstattung: number, abrechnungsStatus: {} } }

<!-- Existing export-range endpoint from Phase 3 -->
GET /api/fahrten/export-range/:type/:startYear/:startMonth/:endYear/:endMonth
Response: blob (xlsx or zip)

<!-- Existing single-month endpoints (keep working) -->
GET /api/fahrten/report/:year/:month
GET /api/fahrten/export/:type/:year/:month
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Von/Bis State + Monatsauswahl-UI + fetchFahrten-Logik</name>
  <files>frontend/src/App.js</files>
  <read_first>frontend/src/App.js (lines 28-55 for AppProvider state, lines 56-70 for refreshAllData/fetchFahrten, lines 202-216 for fetchFahrten, lines 507-550 for FahrtenListe state and month handlers, lines 920-965 for Monatsauswahl UI rendering)</read_first>
  <action>
**A) Neuer State in AppProvider (lines ~40):**
Per D-01, D-02, D-03 — fuege hinzu nach `selectedMonth`:
```javascript
const [selectedVonMonth, setSelectedVonMonth] = useState(''); // '' = Einzelmonat-Modus (per D-02, D-05)
```
Exportiere `selectedVonMonth` und `setSelectedVonMonth` im AppContext value.

`selectedMonth` bleibt bestehen und dient als "Bis"-Wert (per D-03, Default aktueller Monat — bereits so).

**B) fetchFahrten anpassen (lines 202-216):**
Per D-05, D-06, D-07, D-08, D-09:
```javascript
const fetchFahrten = async () => {
  try {
    const [bisYear, bisMonth] = selectedMonth.split('-');

    let response;
    if (selectedVonMonth && selectedVonMonth !== selectedMonth) {
      // Zeitraum-Modus (D-07): Von != Bis und Von != '---'
      const [vonYear, vonMonth] = selectedVonMonth.split('-');
      response = await axios.get(`${API_BASE_URL}/fahrten/report-range/${vonYear}/${vonMonth}/${bisYear}/${bisMonth}`);
    } else {
      // Einzelmonat-Modus (D-05, D-06): Von = '---' oder Von = Bis
      response = await axios.get(`${API_BASE_URL}/fahrten/report/${bisYear}/${bisMonth}`);
    }

    setFahrten(response.data.fahrten.map(fahrt => ({
      ...fahrt,
      mitfahrer: fahrt.mitfahrer || []
    })));
    setSummary(response.data.summary);
  } catch (error) {
    console.error('Fehler beim Abrufen der Fahrten:', error);
    setFahrten([]);
    setSummary({});
  }
};
```

**C) useEffect fuer fetchFahrten erweitern (line 529-531 in FahrtenListe):**
Aendere die dependency von `[selectedMonth]` zu `[selectedMonth, selectedVonMonth]`:
```javascript
useEffect(() => {
  fetchFahrten();
}, [selectedMonth, selectedVonMonth]);
```

**D) Monatsauswahl-UI umbauen (lines 920-965):**
Per D-01, D-02, D-03, D-04 — ersetze den bisherigen Monats/Jahr-Dropdown-Block. Die zwei separaten Dropdowns (Monat + Jahr) werden durch Von/Bis ersetzt.

Neue Von/Bis-Handler in FahrtenListe (ersetzen handleMonthChange/handleYearChange):
```javascript
const handleVonMonthChange = (e) => {
  setSelectedVonMonth(e.target.value); // '' or 'YYYY-MM'
};

const handleBisMonthChange = (e) => {
  const monthIndex = e.target.value;
  const date = new Date(selectedYear, monthIndex);
  setSelectedMonthName(date.toLocaleString('default', { month: 'long' }));
  setSelectedMonth(`${selectedYear}-${(parseInt(monthIndex) + 1).toString().padStart(2, '0')}`);
};

const handleBisYearChange = (e) => {
  const year = e.target.value;
  setSelectedYear(year);
  setSelectedMonth(`${year}-${selectedMonth.split('-')[1]}`);
};
```

Neues JSX fuer die Monatsauswahl (innerhalb des bestehenden flex-Containers, ersetze die alten Dropdowns):
```jsx
<div className="w-full sm:w-auto flex flex-col sm:flex-row items-end gap-3">
  <div className="flex items-center justify-end gap-2 w-full">
    {selectedMonth !== currentMonth && (
      <button onClick={resetToCurrentMonth} className="btn-secondary hidden sm:block">
        Aktueller Monat
      </button>
    )}
    {/* Von-Dropdown (per D-01, D-02) */}
    <label className="text-xs text-label">Von:</label>
    <select
      value={selectedVonMonth}
      onChange={handleVonMonthChange}
      className="form-select w-36"
    >
      <option value="">---</option>
      {[...Array(12)].map((_, i) => {
        const m = (i + 1).toString().padStart(2, '0');
        return (
          <option key={`von-${selectedYear}-${m}`} value={`${selectedYear}-${m}`}>
            {new Date(0, i).toLocaleString("default", { month: "long" })}
          </option>
        );
      })}
    </select>
    {/* Bis-Dropdown (per D-01, D-03) */}
    <label className="text-xs text-label">Bis:</label>
    <select
      value={new Date(`${selectedMonth}-01`).getMonth().toString()}
      onChange={handleBisMonthChange}
      className="form-select w-32"
    >
      {[...Array(12)].map((_, i) => (
        <option key={i} value={i}>
          {new Date(0, i).toLocaleString("default", { month: "long" })}
        </option>
      ))}
    </select>
    <select
      value={selectedYear}
      onChange={handleBisYearChange}
      className="form-select w-24"
    >
      {[...Array(6)].map((_, i) => {
        const year = 2024 + i;
        return <option key={year} value={year}>{year}</option>;
      })}
    </select>
  </div>
</div>
```

Ueberschrift dynamisch (per Specific Ideas):
```jsx
<h2 className="text-lg font-medium text-value">
  {selectedVonMonth && selectedVonMonth !== selectedMonth ? 'Zeitraum-Übersicht' : 'Monatsübersicht'}
</h2>
```

Hole `selectedVonMonth` und `setSelectedVonMonth` aus AppContext in FahrtenListe (useContext Zeile 508).
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Fahrtenbuch && grep -n "selectedVonMonth" frontend/src/App.js | head -20 && grep -n "report-range" frontend/src/App.js && grep -c "Von:" frontend/src/App.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep "selectedVonMonth" frontend/src/App.js` finds state declaration and usage
    - `grep "report-range" frontend/src/App.js` finds the range API call in fetchFahrten
    - `grep 'Von:' frontend/src/App.js` finds the Von label in UI
    - `grep "value=\"\".*---" frontend/src/App.js` finds the --- default option
    - `grep "Zeitraum-Übersicht" frontend/src/App.js` finds dynamic heading
  </acceptance_criteria>
  <done>Von/Bis-Dropdowns ersetzen die bisherige Monatsauswahl, fetchFahrten ruft bei Zeitraum die report-range API, bei Einzelmonat die alte API</done>
</task>

<task type="auto">
  <name>Task 2: Export-Integration + Phase-3-Cleanup</name>
  <files>frontend/src/App.js</files>
  <read_first>frontend/src/App.js (lines 524-527 for exportVon/Bis state, lines 624-670 for handleExportToExcel, lines 672-719 for handleExportToExcelRange, lines 1062-1140 for Export UI sections)</read_first>
  <action>
**A) handleExportToExcel erweitern (per D-13, D-14, D-15, D-18):**
Ersetze die bestehende `handleExportToExcel` Funktion (lines 624-670). Die neue Version entscheidet basierend auf selectedVonMonth ob Einzel- oder Range-Export:

```javascript
const handleExportToExcel = async (type) => {
  try {
    const [bisYear, bisMonth] = selectedMonth.split('-');
    const formattedBisMonth = bisMonth.padStart(2, '0');

    let response;
    let defaultFilename;

    if (selectedVonMonth && selectedVonMonth !== selectedMonth) {
      // Zeitraum-Export (per D-15): nutze Range-Route
      const [vonYear, vonMonth] = selectedVonMonth.split('-');
      const formattedVonMonth = vonMonth.padStart(2, '0');

      // Validierung: Bis >= Von
      const vonDate = new Date(parseInt(vonYear), parseInt(vonMonth) - 1);
      const bisDate = new Date(parseInt(bisYear), parseInt(bisMonth) - 1);
      if (bisDate < vonDate) {
        showNotification("Fehler", "Der Bis-Monat muss gleich oder nach dem Von-Monat liegen.");
        return;
      }

      response = await axios.get(
        `/api/fahrten/export-range/${type}/${vonYear}/${formattedVonMonth}/${bisYear}/${formattedBisMonth}`,
        { responseType: 'blob' }
      );
      defaultFilename = `fahrtenabrechnung_${type}_${vonYear}_${formattedVonMonth}_bis_${bisYear}_${formattedBisMonth}`;
    } else {
      // Einzelmonat-Export (per D-14): nutze alte Route
      response = await axios.get(
        `/api/fahrten/export/${type}/${bisYear}/${formattedBisMonth}`,
        { responseType: 'blob' }
      );
      defaultFilename = `fahrtenabrechnung_${type}_${bisYear}_${formattedBisMonth}`;
    }

    const contentType = response.headers['content-type'];
    const contentDisposition = response.headers['content-disposition'];
    const filenameMatch = contentDisposition && contentDisposition.match(/filename="?(.+)"?/i);
    let filename = filenameMatch ? filenameMatch[1] : defaultFilename;

    if (contentType === 'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet') {
      filename = filename.endsWith('.xlsx') ? filename : `${filename}.xlsx`;
    } else if (contentType === 'application/zip') {
      filename = filename.endsWith('.zip') ? filename : `${filename}.zip`;
    } else {
      throw new Error('Unerwarteter Dateityp vom Server erhalten');
    }

    const blob = new Blob([response.data], { type: contentType });
    if (blob.size === 22) {
      throw new Error('Die heruntergeladene Datei scheint leer oder fehlerhaft zu sein');
    }

    const url = window.URL.createObjectURL(blob);
    const link = document.createElement('a');
    link.href = url;
    link.setAttribute('download', filename);
    document.body.appendChild(link);
    link.click();
    link.parentNode.removeChild(link);
    window.URL.revokeObjectURL(url);

    showNotification("Erfolg", "Export wurde heruntergeladen");
  } catch (error) {
    console.error('Fehler beim Exportieren nach Excel:', error);
    if (error.response && error.response.status === 404) {
      showNotification("Hinweis", "Keine Daten fuer den ausgewaehlten Zeitraum gefunden.");
    } else {
      showNotification("Fehler", "Export konnte nicht erstellt werden.");
    }
  }
};
```

**B) Export-Buttons vereinfachen (per D-13, D-16):**
Ersetze den gesamten Export-Bereich (lines 1062-1140) mit einem einzigen Block:

```jsx
{/* Export (per D-13) */}
<h3 className="text-lg font-medium text-value mt-6 mb-3">
  {selectedVonMonth && selectedVonMonth !== selectedMonth ? 'Zeitraum exportieren' : 'Monat exportieren'}
</h3>
<div className="flex flex-col sm:flex-row justify-end gap-2">
  {getKategorienMitErstattung().map(([key, displayName]) => (
    <button
      key={key}
      onClick={() => handleExportToExcel(key.toLowerCase())}
      className="btn-primary"
    >
      Export {displayName}
    </button>
  ))}
</div>
```

**C) Phase-3-Cleanup (per D-17, D-18, D-19):**
1. ENTFERNE die State-Variablen `exportVonYear`, `exportVonMonth`, `exportBisYear`, `exportBisMonth` (lines 524-527)
2. ENTFERNE die Setter-Aufrufe `setExportVonYear`, `setExportVonMonth`, `setExportBisYear`, `setExportBisMonth`
3. ENTFERNE die gesamte `handleExportToExcelRange` Funktion (lines 672-719)
4. ENTFERNE den "Zeitraum exportieren" Div-Block (lines 1075-1139) — wird durch den neuen vereinfachten Block oben ersetzt
5. ENTFERNE den "Aktuellen Monat exportieren" Header und sein Div (lines 1062-1073) — wird durch den neuen vereinfachten Block oben ersetzt

Hinweis: `handleExportToExcel` Signatur aendert sich — die alten Aufrufe `handleExportToExcel(key.toLowerCase(), selectedYear, selectedMonth.split("-")[1])` muessen zu `handleExportToExcel(key.toLowerCase())` vereinfacht werden, da die Funktion jetzt selbst selectedMonth und selectedVonMonth aus dem Closure nutzt.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Fahrtenbuch && echo "=== No exportVonYear ===" && (grep -c "exportVonYear" frontend/src/App.js || echo "0") && echo "=== No handleExportToExcelRange ===" && (grep -c "handleExportToExcelRange" frontend/src/App.js || echo "0") && echo "=== No Zeitraum exportieren div ===" && (grep -c "Zeitraum exportieren</h3>" frontend/src/App.js || echo "0") && echo "=== export-range in handleExportToExcel ===" && grep -n "export-range" frontend/src/App.js && echo "=== Unified export button ===" && grep -n "handleExportToExcel(key" frontend/src/App.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "exportVonYear" frontend/src/App.js` returns 0 (per D-17)
    - `grep -c "exportVonMonth" frontend/src/App.js` returns 0 (per D-17)
    - `grep -c "handleExportToExcelRange" frontend/src/App.js` returns 0 (per D-18)
    - `grep "export-range" frontend/src/App.js` finds the range-route call inside handleExportToExcel (per D-15)
    - No "Aktuellen Monat exportieren" header exists (per D-16)
    - Only one set of export buttons exists, not two (per D-13, D-16)
    - `grep "handleExportToExcel(key" frontend/src/App.js` finds simplified calls without year/month args
  </acceptance_criteria>
  <done>Export-Buttons adaptieren sich automatisch zwischen Einzel- und Zeitraum-Modus, separater Zeitraum-Export-Bereich ist entfernt, alle Phase-3-Export-State-Variablen sind bereinigt</done>
</task>

</tasks>

<verification>
- Von-Dropdown auf "---": Einzelmonat-Verhalten wie bisher (alte API)
- Von-Dropdown auf z.B. Januar, Bis auf Maerz: Fahrten aus Jan-Maerz geladen (Range-API)
- Export im Einzelmonat: alte Export-Route
- Export im Zeitraum: Range-Export-Route
- Kein separater Zeitraum-Export-Bereich mehr sichtbar
- exportVonYear/Month State entfernt
</verification>

<success_criteria>
Von/Bis-Dropdowns steuern die gesamte Monatsuebersicht (Fahrten, Summary, Export). Der separate Zeitraum-Export aus Phase 3 ist komplett entfernt. Beide Modi (Einzel/Zeitraum) funktionieren ueber die gleichen Buttons.
</success_criteria>

<output>
After completion, create `.planning/phases/04-von-bis-zeitraum-in-monats-bersicht/04-02-SUMMARY.md`
</output>
