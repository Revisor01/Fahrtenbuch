---
phase: 02-excel-export-formular
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/utils/excelExport.js
  - backend/templates/fahrtenabrechnung_vorlage.xlsx
autonomous: true
requirements: [EXCEL-01, EXCEL-02, EXCEL-03, EXCEL-04]

must_haves:
  truths:
    - "Datum jeder Fahrt erscheint im Export als TT.MM.JJJJ (z.B. 15.03.2026), nicht als '15. Mär'"
    - "Nutzer kann das Datum in Excel nachtraeglich umformatieren weil es ein echtes Date-Objekt ist"
    - "Kostentraeger-Zeile im Header zeigt 'Kostentraeger: Name - Kst.: 760130' wenn Kostenstelle hinterlegt"
    - "Kostentraeger-Zeile zeigt nur 'Kostentraeger: Name' wenn keine Kostenstelle hinterlegt"
    - "Unterschriftszeile 'Unterschrift: ___' erscheint links unten im Export"
    - "Genehmigungszeile 'Angeordnet/genehmigt Unterschrift: ___' erscheint rechts unten im Export"
    - "Hinweistexte aus dem Referenz-PDF erscheinen zwischen Gesamt-Zeile und Unterschriftsbereich"
    - "Datumsfeld und Erstattungsberechnung (X km x 0,30 EUR = Y EUR) erscheinen im Unterschriftsbereich"
  artifacts:
    - path: "backend/utils/excelExport.js"
      provides: "Datum als Date-Objekt, Kostenstelle im Header, Unterschriftsbereich"
      contains: "new Date(fahrt.datum)"
    - path: "backend/templates/fahrtenabrechnung_vorlage.xlsx"
      provides: "Hinweistexte, Unterschrift-Labels, Datum-Label in Vorlage"
  key_links:
    - from: "backend/utils/excelExport.js"
      to: "abrechnungstraeger DB table"
      via: "SQL query for kostenstelle"
      pattern: "kostenstelle"
    - from: "backend/utils/excelExport.js"
      to: "backend/templates/fahrtenabrechnung_vorlage.xlsx"
      via: "ExcelJS readFile"
      pattern: "fahrtenabrechnung_vorlage"
---

<objective>
Excel-Export bildet das offizielle Dienstfahrten-Abrechnungsformular korrekt ab: Datum als echtes Excel-Datum (TT.MM.JJJJ), Kostenstelle im Header, vollstaendiger Unterschriftsbereich mit Hinweistexten.

Purpose: Nutzer koennen die Abrechnung direkt einreichen ohne manuelle Nacharbeit.
Output: Angepasste excelExport.js und Template-Datei.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-datenmodell-logik/01-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From backend/utils/excelExport.js (current state):
- formatDate(dateString) returns string like "22. Mai" — MUST be replaced with Date object
- exportToExcel(req, res) is the main export function
- Data rows written to "monatliche Abrechnung" sheet, rows 8-29 (22 per sheet)
- formattedDatum is set via formatDate() and written to cell A column as text
- Vorlage!C7 receives abrechnungstraeger name (currently no kostenstelle)

From backend/models/AbrechnungsTraeger.js:
- findById(id, userId) returns { id, name, kostenstelle, active }
- kostenstelle is VARCHAR(255), nullable

Template "monatliche Abrechnung" sheet layout:
- Row 2: A2="Zeitraum: ", B2=month+year (set by code)
- Row 3: A3="Kostenträger:", B3=formula(Vorlage!C7)
- Rows 6-7: Column headers (merged cells)
- Rows 8-29: Data rows (A=Datum, E=VonOrt, G=NachOrt, H=Anlass[merged H:J], K=km)
- Row 30: J30="Gesamt gefahrene km :", K30=SUM(K8:K29)
- Row 31-32: EMPTY (need hint texts)
- Row 33: B33:C33 merged with date format, E33="Abrechnung der oben aufgeführten gefahrenen km:", J33="km x 0,30 € =", K33=formula(K30*0.3)
- Row 34-35: EMPTY
- Row 36: E36="Datum: ___", F36="Angeordnet/genehmigt Unterschrift: ___"

Missing from template (per PDF reference):
- A33: "Datum:" label
- A36: "Unterschrift: ___" label
- Rows 31-32: Hint texts
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Datum als echtes Excel-Datum und Kostenstelle im Header</name>
  <files>backend/utils/excelExport.js</files>
  <read_first>
    - backend/utils/excelExport.js
    - backend/models/AbrechnungsTraeger.js
    - Dienstfahrtenabrechnung - Excel - Alt.pdf
  </read_first>
  <action>
Zwei Aenderungen in backend/utils/excelExport.js:

**1. Datum als echtes Excel-Datum (per D-01, D-02, D-03 — EXCEL-01):**

In der `exportToExcel` Funktion, im Block wo die Datenzeilen geschrieben werden (ca. Zeile 181-197, innerhalb `chunk.forEach`):

Ersetze:
```javascript
excelRow.getCell('A').value = row.formattedDatum;
```
mit:
```javascript
excelRow.getCell('A').value = row.datum;
excelRow.getCell('A').numFmt = 'DD.MM.YYYY';
```

`row.datum` ist bereits ein `new Date()` Objekt (Zeile 118: `datum: new Date(fahrt.datum)`). Das `formattedDatum` Feld wird nicht mehr fuer die Zelle benoetigt (kann im Objekt bleiben, wird aber nicht mehr in die Zelle geschrieben).

WICHTIG: Im Style-Kopier-Block (Zeile 190-196) muss sichergestellt werden, dass das numFmt nicht ueberschrieben wird. Der bestehende Code kopiert den kompletten Style von Zeile 8:
```javascript
cell.style = { ...abrechnungWorksheet.getCell(`${col}8`).style };
```
Fuer Spalte A muss nach dem Style-Kopieren das numFmt explizit nochmal gesetzt werden:
```javascript
if (col === 'A') {
  cell.numFmt = 'DD.MM.YYYY';
}
```

**2. Kostenstelle im Header (per D-09, D-10, D-11 — EXCEL-04):**

In der `exportToExcel` Funktion, im Block wo der Kostentraeger gesetzt wird (ca. Zeile 168-173), die DB-Query erweitern um `kostenstelle` abzufragen:

Ersetze:
```javascript
const [abrechnungstraeger] = await db.execute(
  'SELECT name FROM abrechnungstraeger WHERE id = ? AND user_id = ?',
  [type, userId]
);
vorlageWorksheet.getCell('C7').value = abrechnungstraeger[0]?.name;
```
mit:
```javascript
const [abrechnungstraeger] = await db.execute(
  'SELECT name, kostenstelle FROM abrechnungstraeger WHERE id = ? AND user_id = ?',
  [type, userId]
);
const traegerName = abrechnungstraeger[0]?.name || '';
const kostenstelle = abrechnungstraeger[0]?.kostenstelle;
vorlageWorksheet.getCell('C7').value = kostenstelle
  ? `${traegerName} - Kst.: ${kostenstelle}`
  : traegerName;
```

Format per D-09: "Rentamt - Kst.: 760130". Per D-10: Wenn kostenstelle leer/null, nur "Rentamt".
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Fahrtenbuch/backend && node -e "
const fs = require('fs');
const code = fs.readFileSync('utils/excelExport.js', 'utf8');
const checks = [
  [/\.numFmt\s*=\s*'DD\.MM\.YYYY'/, 'numFmt DD.MM.YYYY gesetzt'],
  [/SELECT\s+name,\s*kostenstelle/, 'kostenstelle in SQL Query'],
  [/Kst\.:\s/, 'Kst.-Format vorhanden'],
  [/kostenstelle\s*\?/, 'Kostenstelle conditional check'],
];
let ok = true;
checks.forEach(([rx, desc]) => {
  const found = rx.test(code);
  console.log(found ? 'PASS' : 'FAIL', desc);
  if (!found) ok = false;
});
process.exit(ok ? 0 : 1);
"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "numFmt.*DD.MM.YYYY" backend/utils/excelExport.js returns >= 1
    - grep -c "SELECT name, kostenstelle" backend/utils/excelExport.js returns 1
    - grep -c "Kst.:" backend/utils/excelExport.js returns >= 1
    - Die Zeile `excelRow.getCell('A').value = row.formattedDatum` existiert NICHT mehr
    - Stattdessen: `excelRow.getCell('A').value = row.datum` (Date-Objekt)
  </acceptance_criteria>
  <done>Datum wird als echtes Excel-Datum mit Format DD.MM.YYYY exportiert. Kostenstelle erscheint im Header neben dem Kostentraeger im Format "Name - Kst.: Wert" (oder nur "Name" wenn leer).</done>
</task>

<task type="auto">
  <name>Task 2: Unterschriftsbereich mit Hinweistexten im Template und Code</name>
  <files>backend/utils/excelExport.js, backend/templates/fahrtenabrechnung_vorlage.xlsx</files>
  <read_first>
    - backend/utils/excelExport.js
    - Dienstfahrtenabrechnung - Excel - Alt.pdf
  </read_first>
  <action>
Unterschriftsbereich im Template und Code ergaenzen (per D-04 bis D-08 — EXCEL-02, EXCEL-03).

**1. Template-Datei anpassen (backend/templates/fahrtenabrechnung_vorlage.xlsx):**

Das Template muss programmatisch mit ExcelJS angepasst und ueberschrieben werden. Schreibe ein einmaliges Node-Script (z.B. inline oder als temporaeres Script) das die Template-Datei laedt, aendert und zurueckschreibt.

Aenderungen im Sheet "monatliche Abrechnung":

a) **Zeile 31** — Hinweistext 1 (per D-08):
   - A31: "Für die Abrechnung von Tagegeldern nutzen Sie bitte die Einzel-Dienstreiseanträge"
   - Font: Arial, 10, nicht fett

b) **Zeile 32** — Hinweistext 2 (per D-08):
   - A32: "Bitte beachten Sie die Ausschlussfrist von 6 Monaten (§3 Abs. 1 BRKG) je abgeschlossener Dienstreise"
   - Font: Arial, 10, fett (wie im PDF — dieser Text ist im PDF fett/hervorgehoben dargestellt)

c) **Zeile 33** — "Datum:"-Label links (per D-06):
   - A33: "Datum:"
   - Font: Arial, 12, fett (konsistent mit E33/J33)
   - B33:C33 bleibt wie es ist (merged, date format mm-dd-yy) — hier wird im Code das aktuelle Datum oder leer gesetzt

d) **Zeile 36** — "Unterschrift:"-Label links (per D-07):
   - A36: "Unterschrift: _________________________"
   - Font: Arial, 12, fett (konsistent mit E36/F36)

e) **Footer** — "Fahrtenbuch Stand 2025" rechts unten (aus PDF):
   - Diesen Footer NICHT hinzufuegen — das ist ein statisches Element im Original-PDF, nicht relevant fuer den Export.

**2. Code in excelExport.js anpassen:**

Nach dem Block wo die Datenzeilen geschrieben werden (nach `chunk.forEach`), folgenden Code hinzufuegen um dynamische Werte im Unterschriftsbereich zu setzen:

```javascript
// Gesamt-km fuer Erstattungsberechnung in Zeile 33
const gesamtKm = chunk.reduce((sum, row) => sum + (typeof row.kilometer === 'number' ? row.kilometer : 0), 0);
abrechnungWorksheet.getCell('J33').value = `${gesamtKm} km x 0,30 € =`;
```

WICHTIG: Die K33-Zelle hat bereits die Formel `K30*0.3` die den Erstattungsbetrag berechnet. Diese Formel NICHT ueberschreiben. Nur J33 braucht den dynamischen km-Wert.

HINWEIS: Den Erstattungssatz (0,30 EUR) per D-14 als festen Wert im Template/Code verwenden. Der tatsaechliche Satz kommt zwar aus der erstattungsbetraege-Tabelle, aber fuer den Unterschriftsbereich-Text reicht der Template-Standardwert. Die Formel K30*0.3 im Template berechnet korrekt.

Zum Template-Update-Script: Erstelle eine Datei `backend/scripts/update-template.js` die einmalig ausgefuehrt wird:
```javascript
const ExcelJS = require('exceljs');
const path = require('path');

async function updateTemplate() {
  const templatePath = path.join(__dirname, '..', 'templates', 'fahrtenabrechnung_vorlage.xlsx');
  const wb = new ExcelJS.Workbook();
  await wb.xlsx.readFile(templatePath);
  const ws = wb.getWorksheet('monatliche Abrechnung');

  // Row 31: Hinweistext 1
  ws.getCell('A31').value = 'Für die Abrechnung von Tagegeldern nutzen Sie bitte die Einzel-Dienstreiseanträge';
  ws.getCell('A31').font = { name: 'Arial', size: 10, bold: false };

  // Row 32: Hinweistext 2
  ws.getCell('A32').value = 'Bitte beachten Sie die Ausschlussfrist von 6 Monaten (§3 Abs. 1 BRKG) je abgeschlossener Dienstreise';
  ws.getCell('A32').font = { name: 'Arial', size: 10, bold: true };

  // Row 33: Datum-Label links
  ws.getCell('A33').value = 'Datum:';
  ws.getCell('A33').font = { name: 'Arial', size: 12, bold: true };

  // Row 36: Unterschrift-Label links
  ws.getCell('A36').value = 'Unterschrift: _________________________';
  ws.getCell('A36').font = { name: 'Arial', size: 12, bold: true };

  await wb.xlsx.writeFile(templatePath);
  console.log('Template updated successfully');
}

updateTemplate().catch(console.error);
```

Fuehre das Script aus: `cd backend && node scripts/update-template.js`

Danach kann das Script geloescht oder behalten werden (es ist idempotent).
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Fahrtenbuch/backend && node -e "
const ExcelJS = require('exceljs');
const path = require('path');
async function verify() {
  const wb = new ExcelJS.Workbook();
  await wb.xlsx.readFile(path.join(__dirname, 'templates/fahrtenabrechnung_vorlage.xlsx'));
  const ws = wb.getWorksheet('monatliche Abrechnung');
  const checks = [
    [ws.getCell('A31').value?.toString().includes('Tagegeldern'), 'A31 Hinweistext 1'],
    [ws.getCell('A32').value?.toString().includes('Ausschlussfrist'), 'A32 Hinweistext 2'],
    [ws.getCell('A33').value === 'Datum:', 'A33 Datum-Label'],
    [ws.getCell('A36').value?.toString().includes('Unterschrift'), 'A36 Unterschrift-Label'],
    [ws.getCell('E33').value?.toString().includes('Abrechnung'), 'E33 Abrechnungstext (ungeaendert)'],
    [ws.getCell('F36').value?.toString().includes('Angeordnet'), 'F36 Genehmigungstext (ungeaendert)'],
  ];
  let ok = true;
  checks.forEach(([pass, desc]) => {
    console.log(pass ? 'PASS' : 'FAIL', desc);
    if (!pass) ok = false;
  });
  // Check code for J33 dynamic value
  const fs = require('fs');
  const code = fs.readFileSync('utils/excelExport.js', 'utf8');
  const j33 = /getCell\('J33'\)\.value/.test(code);
  console.log(j33 ? 'PASS' : 'FAIL', 'J33 dynamic km value in code');
  if (!j33) ok = false;
  process.exit(ok ? 0 : 1);
}
verify().catch(e => { console.error(e); process.exit(1); });
"</automated>
  </verify>
  <acceptance_criteria>
    - Template-Zelle A31 enthaelt "Tagegeldern"
    - Template-Zelle A32 enthaelt "Ausschlussfrist"
    - Template-Zelle A33 hat Wert "Datum:"
    - Template-Zelle A36 enthaelt "Unterschrift"
    - Template-Zelle E33 enthaelt weiterhin "Abrechnung" (nicht ueberschrieben)
    - Template-Zelle F36 enthaelt weiterhin "Angeordnet" (nicht ueberschrieben)
    - Template-Zelle K33 hat weiterhin die Formel K30*0.3 (nicht ueberschrieben)
    - excelExport.js enthaelt getCell('J33').value mit dynamischem km-Wert
  </acceptance_criteria>
  <done>Unterschriftsbereich ist vollstaendig: Hinweistexte in Zeilen 31-32, Datum-Label in A33, Unterschrift-Label in A36, dynamischer km-Wert in J33. Genehmigungszeile (F36) und Erstattungsformel (K33) aus Template bleiben erhalten.</done>
</task>

</tasks>

<verification>
Nach Abschluss beider Tasks:
1. Template-Datei enthaelt alle statischen Elemente (Hinweistexte, Labels)
2. excelExport.js schreibt Datum als Date-Objekt mit DD.MM.YYYY Format
3. excelExport.js setzt Kostentraeger mit optionaler Kostenstelle
4. excelExport.js setzt dynamischen km-Wert in J33
5. Bestehende Formeln (K30 SUM, K33 Erstattung) bleiben intakt
</verification>

<success_criteria>
- EXCEL-01: Datum als echtes Excel-Datum im Format DD.MM.YYYY (nicht als Text-String)
- EXCEL-02: Unterschriftszeile "Unterschrift: ___" in A36 sichtbar
- EXCEL-03: Genehmigungszeile "Angeordnet/genehmigt Unterschrift: ___" in F36 sichtbar
- EXCEL-04: Kostentraeger + Kostenstelle im Format "Name - Kst.: Wert" im Header
</success_criteria>

<output>
After completion, create `.planning/phases/02-excel-export-formular/02-01-SUMMARY.md`
</output>
