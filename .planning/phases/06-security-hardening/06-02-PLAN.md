---
phase: 06-security-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/controllers/distanzController.js
  - backend/controllers/fahrtController.js
  - backend/controllers/ortController.js
  - backend/controllers/profileController.js
  - backend/controllers/userController.js
  - backend/controllers/abrechnungstraegerController.js
autonomous: true
requirements:
  - SEC-03
must_haves:
  truths:
    - "Fehlermeldungen an den Client enthalten keine internen Details (DB-Fehlercodes, Stack-Traces, Tabellennamen)"
    - "Server-side Logs enthalten weiterhin vollstaendige Fehlerdetails fuer Debugging"
  artifacts:
    - path: "backend/controllers/distanzController.js"
      provides: "Sanitized error responses"
    - path: "backend/controllers/fahrtController.js"
      provides: "Sanitized error responses"
    - path: "backend/controllers/ortController.js"
      provides: "Sanitized error responses"
    - path: "backend/controllers/profileController.js"
      provides: "Sanitized error responses"
    - path: "backend/controllers/userController.js"
      provides: "Sanitized error responses"
    - path: "backend/controllers/abrechnungstraegerController.js"
      provides: "Sanitized error responses"
  key_links:
    - from: "backend/controllers/*.js"
      to: "client responses"
      via: "res.status(500).json()"
      pattern: "res\\.status\\(500\\)\\.json.*message:"
---

<objective>
Error-Messages in allen Controllern sanitizen: Keine internen Details (error.message, DB-Fehlercodes, Stack-Traces) an den Client senden. Nur generische deutsche Fehlermeldungen zurueckgeben, Details weiterhin server-side loggen.

Purpose: Verhindern dass Angreifer ueber Fehlermeldungen Rueckschluesse auf Datenbank-Struktur, Tabellennamen oder interne Logik ziehen koennen.
Output: Alle Controller geben nur noch generische Fehlermeldungen an den Client zurueck.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-security-hardening/06-CONTEXT.md
@backend/controllers/distanzController.js
@backend/controllers/fahrtController.js
@backend/controllers/ortController.js
@backend/controllers/profileController.js
@backend/controllers/userController.js
@backend/controllers/abrechnungstraegerController.js
</context>

<interfaces>
<!-- Aktuelles Muster in Controllern das geaendert werden muss -->
Aktuell (UNSICHER):
```javascript
res.status(500).json({ message: 'Fehler beim Erstellen der Fahrt', error: error.message });
```

Ziel (SICHER):
```javascript
console.error('Fehler beim Erstellen der Fahrt:', error);
res.status(500).json({ message: 'Fehler beim Erstellen der Fahrt' });
```

Betroffene Controller und Anzahl der Stellen:
- distanzController.js: 6 Stellen mit `error: error.message`
- fahrtController.js: 13 Stellen mit `error: error.message`
- ortController.js: 4 Stellen mit `error: error.message`
- profileController.js: 1 Stelle mit `error: error.message`
- userController.js: 1 Stelle mit `error: error.message`
- abrechnungstraegerController.js: 3 Stellen mit `error: error.message`

NICHT betroffen (bereits sicher):
- apiKeyController.js — gibt keine error.message an Client weiter
- authController.js — gibt keine error.message an Client weiter
- mitfahrerErstattungController.js — pruefen ob betroffen
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: error.message aus allen Controller-Responses entfernen</name>
  <files>
    backend/controllers/distanzController.js,
    backend/controllers/fahrtController.js,
    backend/controllers/ortController.js,
    backend/controllers/profileController.js,
    backend/controllers/userController.js,
    backend/controllers/abrechnungstraegerController.js,
    backend/controllers/mitfahrerErstattungController.js
  </files>
  <read_first>
    backend/controllers/distanzController.js,
    backend/controllers/fahrtController.js,
    backend/controllers/ortController.js,
    backend/controllers/profileController.js,
    backend/controllers/userController.js,
    backend/controllers/abrechnungstraegerController.js,
    backend/controllers/mitfahrerErstattungController.js
  </read_first>
  <action>
In JEDEM der gelisteten Controller-Files:

1. Alle `res.status(500).json({ message: '...', error: error.message })` aendern zu:
   `res.status(500).json({ message: '...' })`

   Die `error: error.message` Property wird komplett entfernt. Die bestehende generische message (z.B. "Fehler beim Erstellen der Fahrt") bleibt erhalten.

2. Sicherstellen dass VOR dem res.status(500) ein `console.error(...)` steht, das den vollstaendigen Error loggt. Falls keins vorhanden, hinzufuegen:
   ```javascript
   console.error('Fehler beim [Operation]:', error);
   ```

3. SONDERFAELLE beachten:
   - `abrechnungstraegerController.js` Zeile ~290: `error.message.includes('wird noch in Fahrten verwendet')` — das ist eine BUSINESS-LOGIC Pruefung, NICHT ein DB-Leak. Diese Stelle BEIBEHALTEN, da die Message aus dem Model kommt und bewusst user-facing ist.
   - Alle anderen `error: error.message` Properties entfernen.

4. Auch `error: error.message` in Responses mit anderen Status-Codes (400, etc.) pruefen und entfernen, sofern es keine bewusste Business-Logic-Message ist.

NICHT aendern:
- apiKeyController.js (bereits sicher)
- authController.js (bereits sicher)
- Stellen wo `error.message` fuer Business-Logik geprueft wird (z.B. includes-Check)
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Fahrtenbuch && grep -rn "error: error\.message\|error\.message\b" backend/controllers/ --include="*.js" | grep -v "console\.\|includes\|\.message ==\|\.message !=" | grep "res\." || echo "CLEAN: Keine error.message in Client-Responses"</automated>
  </verify>
  <acceptance_criteria>
- Kein Controller sendet `error.message` oder `error.code` in JSON-Responses an den Client
- Jeder catch-Block hat ein console.error() mit dem vollstaendigen Error-Objekt
- Business-Logic-Checks (z.B. "wird noch in Fahrten verwendet") bleiben intakt
- grep nach "error: error.message" in res.status()-Aufrufen findet keine Treffer
  </acceptance_criteria>
  <done>Alle 28+ Stellen bereinigt — Client bekommt nur generische deutsche Fehlermeldungen, Server loggt Details</done>
</task>

</tasks>

<verification>
- `grep -rn "error: error.message" backend/controllers/` zeigt keine Treffer in res.json()-Aufrufen
- `grep -rn "console.error" backend/controllers/` zeigt dass Logging weiterhin vorhanden ist
- Backend startet fehlerfrei: `cd backend && node -e "require('./app')"`
</verification>

<success_criteria>
1. Kein einziger Controller gibt `error.message` an den Client zurueck
2. Server-Logs enthalten weiterhin vollstaendige Fehlerdetails
3. Business-Logic-Messages (bewusst user-facing) bleiben funktional
</success_criteria>

<output>
After completion, create `.planning/phases/06-security-hardening/06-02-SUMMARY.md`
</output>
