---
phase: 08-frontend-refactoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/App.js
  - frontend/src/contexts/AppContext.js
  - frontend/src/components/FahrtenListe.js
autonomous: true
requirements: [REF-01, REF-02]

must_haves:
  truths:
    - "AppProvider und AppContext leben in einer eigenen Datei contexts/AppContext.js"
    - "FahrtenListe lebt in components/FahrtenListe.js als eigenstaendige Komponente"
    - "App.js importiert AppProvider aus contexts/AppContext.js"
    - "App.js importiert FahrtenListe nicht mehr direkt — sie wird in AppContent referenziert"
    - "Die App verhalt sich identisch zum Ist-Zustand"
  artifacts:
    - path: "frontend/src/contexts/AppContext.js"
      provides: "AppProvider Komponente und AppContext Export"
      exports: ["AppContext", "AppProvider"]
    - path: "frontend/src/components/FahrtenListe.js"
      provides: "FahrtenListe Komponente"
      exports: ["default FahrtenListe"]
  key_links:
    - from: "frontend/src/App.js"
      to: "frontend/src/contexts/AppContext.js"
      via: "import { AppContext } und import AppProvider"
      pattern: "import.*AppContext.*from.*contexts/AppContext"
    - from: "frontend/src/components/FahrtenListe.js"
      to: "frontend/src/contexts/AppContext.js"
      via: "useContext(AppContext)"
      pattern: "useContext\\(AppContext\\)"
---

<objective>
AppProvider und FahrtenListe aus App.js in eigene Dateien extrahieren.

Purpose: Erster Schritt des Refactorings — die zwei groessten Bloecke (AppProvider: ~510 Zeilen, FahrtenListe: ~1500 Zeilen) werden ausgelagert. Danach hat App.js nur noch ~1050 Zeilen statt 3056.
Output: contexts/AppContext.js, components/FahrtenListe.js, verkleinertes App.js
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-frontend-refactoring/08-CONTEXT.md
@frontend/src/App.js
@frontend/src/ThemeContext.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: AppProvider nach contexts/AppContext.js extrahieren</name>
  <files>frontend/src/contexts/AppContext.js, frontend/src/App.js</files>
  <read_first>frontend/src/App.js (Zeilen 1-536 — die gesamte AppProvider-Funktion inkl. Imports und createContext)</read_first>
  <action>
1. Verzeichnis `frontend/src/contexts/` erstellen.
2. Neue Datei `frontend/src/contexts/AppContext.js` anlegen mit:
   - Allen noetigen Imports (React, useState, useEffect, createContext, useContext, useMemo, useRef, axios) — NICHT die UI-Imports wie lucide-react, Modals etc.
   - `const API_BASE_URL = '/api';`
   - `export const AppContext = createContext();`
   - Die komplette `AppProvider` Funktion (Zeilen 28-536 aus App.js) — 1:1 kopieren.
   - WICHTIG: AppProvider rendert im return-Block mehrere Modals (NotificationModal, AbrechnungsStatusModal, Modal). Diese Imports muessen in AppContext.js mitgenommen werden: `NotificationModal`, `AbrechnungsStatusModal`, `Modal`, `Settings/MapPin/Ruler/Users/UserCircle` Icons die im Provider-JSX verwendet werden.
   - Pruefen welche Imports aus App.js Zeilen 1-22 tatsaechlich NUR im AppProvider verwendet werden und diese nach AppContext.js verschieben.
   - `export default AppProvider;` (oder `export { AppProvider }`)

3. In App.js:
   - Zeilen 24-536 loeschen (API_BASE_URL, createContext, AppProvider).
   - `export const AppContext = createContext();` loeschen.
   - Stattdessen: `import { AppContext } from './contexts/AppContext';` und `import AppProvider from './contexts/AppContext';`
   - Imports die NUR im AppProvider genutzt wurden aus App.js entfernen (die sind jetzt in AppContext.js).
   - Alle verbleibenden Funktionen (FahrtenListe, MonthlyOverview, LoginPage, ForgotPasswordForm, App, AppContent) behalten `useContext(AppContext)` — der Import kommt jetzt aus `'./contexts/AppContext'`.

4. KRITISCH: Nach der Extraktion `npm start` im frontend-Verzeichnis testen oder `npx react-scripts build` ausfuehren um sicherzustellen dass keine Import-Fehler auftreten.

Per D-01 (AppProvider-Extraktion), D-09 (schrittweise), D-10 (App muss nach jedem Schritt funktionieren).
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Fahrtenbuch/frontend && npx react-scripts build 2>&1 | tail -5</automated>
  </verify>
  <done>
- frontend/src/contexts/AppContext.js existiert mit exportiertem AppProvider und AppContext
- App.js enthaelt keine AppProvider-Definition mehr
- App.js importiert AppContext und AppProvider aus contexts/AppContext
- Build kompiliert ohne Fehler
- App.js Zeilenanzahl ist um ca. 510 Zeilen gesunken
  </done>
</task>

<task type="auto">
  <name>Task 2: FahrtenListe nach components/FahrtenListe.js extrahieren</name>
  <files>frontend/src/components/FahrtenListe.js, frontend/src/App.js</files>
  <read_first>frontend/src/App.js (nach Task 1 — die FahrtenListe-Funktion, urspruenglich Zeilen 538-2040)</read_first>
  <action>
1. Neue Datei `frontend/src/components/FahrtenListe.js` anlegen mit:
   - React-Imports (useState, useContext, useEffect, useRef, useMemo — was FahrtenListe braucht)
   - `import { AppContext } from '../contexts/AppContext';`
   - Alle weiteren Imports die FahrtenListe nutzt: axios, FahrtForm, MitfahrerModal, renderOrteOptions, lucide-react Icons die in FahrtenListe verwendet werden.
   - Die komplette FahrtenListe-Funktion — 1:1 aus App.js herauskopieren.
   - `export default FahrtenListe;`

2. In App.js:
   - Die gesamte FahrtenListe-Funktion entfernen (ca. 1500 Zeilen).
   - `import FahrtenListe from './components/FahrtenListe';` hinzufuegen.
   - Imports die NUR in FahrtenListe genutzt wurden aus App.js entfernen (z.B. FahrtForm, MitfahrerModal, renderOrteOptions — pruefen ob sie anderswo noch gebraucht werden).

3. KRITISCH: Build testen.

Per D-02 (FahrtenListe-Extraktion), D-09 (schrittweise), D-10 (App muss funktionieren).
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Fahrtenbuch/frontend && npx react-scripts build 2>&1 | tail -5</automated>
  </verify>
  <done>
- frontend/src/components/FahrtenListe.js existiert mit exportierter FahrtenListe-Komponente
- App.js enthaelt keine FahrtenListe-Definition mehr
- App.js importiert FahrtenListe aus components/FahrtenListe
- Build kompiliert ohne Fehler
- App.js Zeilenanzahl ist um ca. 1500 Zeilen weiter gesunken (jetzt ca. 1050 Zeilen)
  </done>
</task>

</tasks>

<verification>
- `wc -l frontend/src/App.js` zeigt ca. 1050 Zeilen (von 3056 auf ~1050)
- `wc -l frontend/src/contexts/AppContext.js` zeigt ca. 520 Zeilen
- `wc -l frontend/src/components/FahrtenListe.js` zeigt ca. 1510 Zeilen
- `cd frontend && npx react-scripts build` kompiliert ohne Fehler
- `grep -c "useContext(AppContext)" frontend/src/components/FahrtenListe.js` gibt 1 zurueck
</verification>

<success_criteria>
- AppProvider lebt in eigener Datei (REF-01, REF-02)
- FahrtenListe lebt in eigener Datei (REF-01)
- App.js hat nur noch MonthlyOverview, LoginPage, ForgotPasswordForm, App, AppContent
- Build laeuft durch ohne Fehler
</success_criteria>

<output>
After completion, create `.planning/phases/08-frontend-refactoring/08-01-SUMMARY.md`
</output>
