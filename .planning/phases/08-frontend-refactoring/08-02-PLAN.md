---
phase: 08-frontend-refactoring
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - frontend/src/App.js
  - frontend/src/components/MonthlyOverview.js
  - frontend/src/components/LoginPage.js
  - frontend/src/components/AppContent.js
autonomous: true
requirements: [REF-01, REF-03]

must_haves:
  truths:
    - "MonthlyOverview lebt in components/MonthlyOverview.js als eigenstaendige Komponente"
    - "LoginPage und ForgotPasswordForm leben in components/LoginPage.js"
    - "AppContent lebt in components/AppContent.js als Layout-Wrapper"
    - "App.js enthaelt nur noch die App-Funktion (Root Router) mit Imports — unter 50 Zeilen"
    - "Export-Logik (handleExportToExcel) lebt klar in FahrtenListe, nicht verstreut in App.js (REF-03)"
    - "Die App verhalt sich identisch zum Ist-Zustand"
  artifacts:
    - path: "frontend/src/components/MonthlyOverview.js"
      provides: "MonthlyOverview Komponente mit QuickActions"
      exports: ["default MonthlyOverview"]
    - path: "frontend/src/components/LoginPage.js"
      provides: "LoginPage und ForgotPasswordForm"
      exports: ["default LoginPage"]
    - path: "frontend/src/components/AppContent.js"
      provides: "AppContent Layout-Wrapper"
      exports: ["default AppContent"]
    - path: "frontend/src/App.js"
      provides: "Root App mit Router — nur Imports und Router-Setup"
      min_lines: 20
  key_links:
    - from: "frontend/src/components/AppContent.js"
      to: "frontend/src/components/FahrtenListe.js"
      via: "import und Rendering"
      pattern: "import FahrtenListe"
    - from: "frontend/src/components/AppContent.js"
      to: "frontend/src/components/MonthlyOverview.js"
      via: "import und Rendering"
      pattern: "import MonthlyOverview"
    - from: "frontend/src/components/AppContent.js"
      to: "frontend/src/components/LoginPage.js"
      via: "import und Rendering wenn nicht eingeloggt"
      pattern: "import LoginPage"
    - from: "frontend/src/App.js"
      to: "frontend/src/components/AppContent.js"
      via: "Route element"
      pattern: "<AppContent"
---

<objective>
MonthlyOverview, LoginPage und AppContent aus App.js extrahieren — App.js wird zur reinen Router-Datei.

Purpose: Abschluss des Refactorings. Nach diesem Plan enthaelt App.js nur noch die App-Funktion (~20 Zeilen) mit Router-Setup und Imports. Alle UI-Logik lebt in eigenstaendigen Komponenten.
Output: components/MonthlyOverview.js, components/LoginPage.js, components/AppContent.js, minimales App.js
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-frontend-refactoring/08-CONTEXT.md
@.planning/phases/08-frontend-refactoring/08-01-SUMMARY.md
@frontend/src/App.js
@frontend/src/contexts/AppContext.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: MonthlyOverview nach components/MonthlyOverview.js extrahieren</name>
  <files>frontend/src/components/MonthlyOverview.js, frontend/src/App.js</files>
  <read_first>frontend/src/App.js (den aktuellen Stand nach Plan 01 — MonthlyOverview-Funktion finden, urspruenglich Zeilen 2042-2648)</read_first>
  <action>
1. Neue Datei `frontend/src/components/MonthlyOverview.js` anlegen mit:
   - React-Imports (useState, useContext, useEffect — was MonthlyOverview braucht)
   - `import { AppContext } from '../contexts/AppContext';`
   - axios Import (fuer Export-Aufrufe falls vorhanden)
   - Alle weiteren Imports die MonthlyOverview nutzt: lucide-react Icons, AbrechnungsStatusModal falls referenziert.
   - Die komplette MonthlyOverview-Funktion — 1:1 aus App.js kopieren.
   - QuickActions bleibt inline in MonthlyOverview (per Claude's Discretion).
   - `export default MonthlyOverview;`

2. In App.js:
   - Die gesamte MonthlyOverview-Funktion entfernen (~600 Zeilen).
   - `import MonthlyOverview from './components/MonthlyOverview';` hinzufuegen.
   - Imports die NUR in MonthlyOverview genutzt wurden aus App.js entfernen.

3. Build testen.

Per D-03 (MonthlyOverview-Extraktion), D-08 (Export-Logik klar benannt — handleExportToExcel lebt bereits in FahrtenListe aus Plan 01), D-09, D-10.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Fahrtenbuch/frontend && npx react-scripts build 2>&1 | tail -5</automated>
  </verify>
  <done>
- frontend/src/components/MonthlyOverview.js existiert mit exportierter MonthlyOverview-Komponente
- App.js enthaelt keine MonthlyOverview-Definition mehr
- Build kompiliert ohne Fehler
- App.js Zeilenanzahl ist um ca. 600 Zeilen gesunken (jetzt ca. 450 Zeilen)
  </done>
</task>

<task type="auto">
  <name>Task 2: LoginPage und AppContent extrahieren — App.js wird minimal</name>
  <files>frontend/src/components/LoginPage.js, frontend/src/components/AppContent.js, frontend/src/App.js</files>
  <read_first>frontend/src/App.js (den aktuellen Stand nach Task 1 — sollte noch LoginPage, ForgotPasswordForm, App, AppContent enthalten)</read_first>
  <action>
1. Neue Datei `frontend/src/components/LoginPage.js` anlegen mit:
   - React-Imports (useState, useContext)
   - `import { AppContext } from '../contexts/AppContext';`
   - axios Import (fuer Registrierung und Passwort-Reset)
   - Die komplette LoginPage-Funktion UND ForgotPasswordForm-Funktion — beide gehoeren zusammen.
   - `export default LoginPage;` (ForgotPasswordForm wird nur intern von LoginPage genutzt, kein separater Export noetig)

2. Neue Datei `frontend/src/components/AppContent.js` anlegen mit:
   - React-Imports (useState, useContext, useEffect)
   - `import { AppContext } from '../contexts/AppContext';`
   - `import LoginPage from './LoginPage';`
   - `import FahrtenListe from './FahrtenListe';`
   - `import MonthlyOverview from './MonthlyOverview';`
   - Alle weiteren Imports die AppContent nutzt: ProfileModal, Modal, UserManagement, InfoModal, NewFeaturesModal, ThemeToggle, lucide-react Icons.
   - Die komplette AppContent-Funktion aus App.js.
   - `export default AppContent;`

3. App.js auf das Minimum reduzieren:
   - NUR die App-Funktion behalten (Zeilen 2903-2922 im Original).
   - Imports: React, BrowserRouter/Routes/Route/Navigate, ThemeProvider, AppProvider (aus contexts/AppContext), AppContent (aus components/AppContent), LandingPage, VerifyEmail, ResetPassword, SetPassword.
   - CSS-Imports: `import './index.css';` und `import './darkMode.css';` bleiben in App.js.
   - `export default App;`
   - Ziel: App.js hat unter 50 Zeilen.

4. Build testen.

Per D-04 (LoginPage-Extraktion), D-05 (AppContent-Extraktion), D-06 (App bleibt in App.js als Root-Router), D-09, D-10.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Fahrtenbuch/frontend && npx react-scripts build 2>&1 | tail -5</automated>
  </verify>
  <done>
- frontend/src/components/LoginPage.js existiert mit LoginPage und ForgotPasswordForm
- frontend/src/components/AppContent.js existiert mit AppContent
- App.js enthaelt NUR die App-Funktion mit Router-Setup und Imports
- App.js hat unter 50 Zeilen
- Build kompiliert ohne Fehler
- REF-03 ist erfuellt: Export-Logik lebt in FahrtenListe (handleExportToExcel), nicht verstreut
  </done>
</task>

</tasks>

<verification>
- `wc -l frontend/src/App.js` zeigt unter 50 Zeilen
- `wc -l frontend/src/components/MonthlyOverview.js` zeigt ca. 610 Zeilen
- `wc -l frontend/src/components/LoginPage.js` zeigt ca. 260 Zeilen
- `wc -l frontend/src/components/AppContent.js` zeigt ca. 140 Zeilen
- `cd frontend && npx react-scripts build` kompiliert ohne Fehler
- `grep -c "function " frontend/src/App.js` gibt 1 zurueck (nur App)
- Alle Komponenten importieren AppContext aus '../contexts/AppContext'
</verification>

<success_criteria>
- App.js ist eine reine Router-Datei mit unter 50 Zeilen (REF-01)
- Alle UI-Komponenten leben in eigenen Dateien (REF-01)
- Export-Logik lebt klar in FahrtenListe, nicht verstreut (REF-03)
- State-Management lebt in AppProvider/AppContext (REF-02)
- Build laeuft durch ohne Fehler
- Keine sichtbare Verhaltensaenderung fuer Nutzer
</success_criteria>

<output>
After completion, create `.planning/phases/08-frontend-refactoring/08-02-SUMMARY.md`
</output>
