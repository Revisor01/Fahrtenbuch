---
phase: 27-backend-api-ui-konsistenz
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/index.css
  - frontend/src/components/FahrtenListe.js
  - frontend/src/components/Settings.js
  - frontend/src/components/MonthlyOverview.js
  - frontend/src/components/LoginPage.js
  - frontend/src/components/AbrechnungstraegerForm.js
  - frontend/src/components/OrtForm.js
  - frontend/src/components/ErstattungssaetzeForm.js
  - frontend/src/components/DistanzForm.js
  - frontend/src/UserManagement.js
  - frontend/src/ProfileModal.js
  - frontend/src/MitfahrerModal.js
  - frontend/src/HilfeModal.js
  - frontend/src/VerifyEmail.js
  - frontend/src/SetPassword.js
  - frontend/src/LandingPage.js
  - frontend/src/FahrtForm.js
  - frontend/src/contexts/AppContext.js
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-04

must_haves:
  truths:
    - "Erstattungs-Cards in FahrtenListe nutzen Traeger-Farben via kpi-card + getCardBg"
    - "Status-Chips sind visuell klar unterscheidbar: erhalten=gruen, eingereicht=amber, offen=grau"
    - "Kein card-container-highlight mehr im gesamten Frontend-Code"
    - "Erstattungs-Bereich hat section-header und konsistentes Spacing"
  artifacts:
    - path: "frontend/src/index.css"
      provides: "status-badge-warning CSS-Klasse"
      contains: "status-badge-warning"
    - path: "frontend/src/components/FahrtenListe.js"
      provides: "Erstattungs-Cards mit korrekten Status-Badge-Klassen"
      contains: "status-badge-warning"
  key_links:
    - from: "frontend/src/components/FahrtenListe.js"
      to: "frontend/src/index.css"
      via: "status-badge-warning CSS class"
      pattern: "status-badge-warning"
---

<objective>
FahrtenListe UI-Konsistenz: Status-Badges visuell unterscheidbar, card-container-highlight global entfernen, Erstattungs-Bereich v2.1-konform.

Purpose: Visuelle Konsistenz mit v2.1 Design-Patterns herstellen.
Output: Einheitliche Status-Badges, kein hellblauer Hintergrund, konsistentes Layout.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@frontend/src/index.css
@frontend/src/components/FahrtenListe.js
</context>

<interfaces>
<!-- Existing CSS classes in index.css -->
From frontend/src/index.css:
```css
.status-badge {
  @apply inline-flex items-center gap-2 px-2 py-0.5 rounded-full text-xs font-medium;
}
.status-badge-primary {
  @apply status-badge bg-primary-100 dark:bg-primary-900 text-primary-700 dark:text-primary-300;
}
.status-badge-secondary {
  @apply status-badge bg-secondary-100 dark:bg-secondary-900 text-secondary-700 dark:text-secondary-300;
}
/* status-badge-warning does NOT exist yet — must be created */
```

From frontend/src/components/FahrtenListe.js (lines 524-565):
```javascript
// Current status badge mapping:
// erhalten_am → status-badge-primary (green) ✓ correct
// eingereicht_am → status-badge-secondary (gray) ✗ should be status-badge-warning (amber)
// nicht eingereicht → status-badge-secondary (gray) — stays secondary
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Status-Badge-Warning CSS + FahrtenListe Status-Chips</name>
  <files>frontend/src/index.css, frontend/src/components/FahrtenListe.js</files>
  <read_first>frontend/src/index.css, frontend/src/components/FahrtenListe.js</read_first>
  <action>
**1. index.css — Add status-badge-warning class (per UI-02):**

After the `.status-badge-secondary` block (around line 203), add:

```css
  .status-badge-warning {
    @apply status-badge
           bg-amber-100 dark:bg-amber-900
           text-amber-700 dark:text-amber-300;
  }
```

**2. FahrtenListe.js — Fix "eingereicht" badge class (per UI-02):**

Find the "eingereicht_am" status badge (around line 539-540). Change:
```
className="status-badge-secondary cursor-pointer"
```
to:
```
className="status-badge-warning cursor-pointer"
```

This is the span that shows "Eingereicht am: {date}" — NOT the "Nicht eingereicht" span (that one stays status-badge-secondary).

**3. FahrtenListe.js — Verify kpi-card + getCardBg (per UI-01):**

Verify that line 512 already has `className="kpi-card" style={getCardBg(farbe)}` — if so, UI-01 is already done. Do NOT change if already correct.

**4. FahrtenListe.js — Verify section-header (per UI-04):**

Verify that the Erstattungen section header (around line 494) uses `className="section-header"`. If already present, UI-04 is done. Do NOT change if already correct.

**Result: Three visually distinct status states:**
- Erhalten: status-badge-primary (green) — already correct
- Eingereicht: status-badge-warning (amber) — changed from secondary
- Nicht eingereicht: status-badge-secondary (gray) — stays
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Fahrtenbuch && grep -c "status-badge-warning" frontend/src/index.css && grep -c "status-badge-warning" frontend/src/components/FahrtenListe.js</automated>
  </verify>
  <acceptance_criteria>
- grep "status-badge-warning" frontend/src/index.css returns match (new CSS class exists)
- grep "status-badge-warning" frontend/src/components/FahrtenListe.js returns match (eingereicht badge uses it)
- grep -c "status-badge-secondary" frontend/src/components/FahrtenListe.js returns exactly 1 (only "Nicht eingereicht" uses secondary)
- grep "kpi-card" frontend/src/components/FahrtenListe.js returns match (UI-01 verified)
- grep "section-header" frontend/src/components/FahrtenListe.js returns match (UI-04 verified)
  </acceptance_criteria>
  <done>Drei visuell unterscheidbare Status-Badges: gruen (erhalten), amber (eingereicht), grau (nicht eingereicht). Erstattungs-Cards nutzen kpi-card + getCardBg. Section-header vorhanden.</done>
</task>

<task type="auto">
  <name>Task 2: card-container-highlight global ersetzen</name>
  <files>frontend/src/components/Settings.js, frontend/src/components/MonthlyOverview.js, frontend/src/components/LoginPage.js, frontend/src/components/AbrechnungstraegerForm.js, frontend/src/components/OrtForm.js, frontend/src/components/ErstattungssaetzeForm.js, frontend/src/components/DistanzForm.js, frontend/src/UserManagement.js, frontend/src/ProfileModal.js, frontend/src/MitfahrerModal.js, frontend/src/HilfeModal.js, frontend/src/VerifyEmail.js, frontend/src/SetPassword.js, frontend/src/LandingPage.js, frontend/src/FahrtForm.js, frontend/src/contexts/AppContext.js, frontend/src/index.css</files>
  <read_first>frontend/src/index.css</read_first>
  <action>
**Global find-and-replace across all frontend source files (per UI-03):**

In every file listed below, replace ALL occurrences of `card-container-highlight` with `card-container`:

Files and occurrence counts:
- frontend/src/components/Settings.js (10 occurrences)
- frontend/src/ProfileModal.js (4)
- frontend/src/components/LoginPage.js (3)
- frontend/src/LandingPage.js (3)
- frontend/src/components/MonthlyOverview.js (1)
- frontend/src/components/AbrechnungstraegerForm.js (1)
- frontend/src/components/OrtForm.js (1)
- frontend/src/components/ErstattungssaetzeForm.js (1)
- frontend/src/components/DistanzForm.js (1)
- frontend/src/UserManagement.js (1)
- frontend/src/MitfahrerModal.js (1)
- frontend/src/HilfeModal.js (1)
- frontend/src/VerifyEmail.js (1)
- frontend/src/SetPassword.js (1)
- frontend/src/FahrtForm.js (1)
- frontend/src/contexts/AppContext.js (1)

**Also in frontend/src/index.css:**
Remove or rename the `.card-container-highlight` CSS class definition. If the class has different styles than `.card-container`, the styles are no longer needed — just delete the `.card-container-highlight` block entirely.

**IMPORTANT:** This is a pure string replacement. Do NOT change any other class names, logic, or structure. Keep all other classes on the same elements intact (e.g., `card-container-highlight space-y-6` becomes `card-container space-y-6`).
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Fahrtenbuch && grep -r "card-container-highlight" frontend/src/ --include="*.js" --include="*.css" | wc -l</automated>
  </verify>
  <acceptance_criteria>
- grep -r "card-container-highlight" frontend/src/ --include="*.js" --include="*.css" returns 0 matches
- grep -r "card-container" frontend/src/ --include="*.js" returns matches (replacement worked)
- The app compiles: cd frontend && npx react-scripts build 2>&1 | tail -1 shows "Compiled successfully"
  </acceptance_criteria>
  <done>Kein card-container-highlight mehr im gesamten Frontend-Quellcode. Ueberall einheitlich card-container.</done>
</task>

</tasks>

<verification>
- Kein card-container-highlight in frontend/src/
- status-badge-warning CSS-Klasse existiert in index.css
- FahrtenListe nutzt status-badge-warning fuer "eingereicht" und status-badge-secondary nur fuer "nicht eingereicht"
- Frontend kompiliert fehlerfrei
</verification>

<success_criteria>
- Drei visuell unterscheidbare Status-Badge-Varianten
- Kein hellblauer card-container-highlight Hintergrund mehr
- Erstattungs-Cards nutzen Traeger-Farben (kpi-card + getCardBg)
- Section-header und Spacing konsistent
</success_criteria>

<output>
After completion, create `.planning/phases/27-backend-api-ui-konsistenz/27-02-SUMMARY.md`
</output>
