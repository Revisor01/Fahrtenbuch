---
phase: 16-dashboard-makeover
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/Dashboard.js
  - frontend/src/components/AppContent.js
  - frontend/src/index.css
autonomous: false
requirements:
  - DASH-09
  - DASH-10
  - DASH-11

must_haves:
  truths:
    - "KPI-Cards zeigen sich im 2x2-Grid auf Desktop/iPad und gestapelt auf iPhone"
    - "Alle Touch-Targets (Buttons, Favoriten-Cards, Nochmal-Buttons) sind mindestens 44px hoch"
    - "Das Formular 'Neue Fahrt erfassen' hat ein modernes Card-Layout mit Gradient-Header"
    - "Ein Export-Schnellzugriff-Button ist direkt vom Dashboard erreichbar"
    - "Kein horizontales Scrollen auf iPhone (375px Breite)"
  artifacts:
    - path: "frontend/src/components/Dashboard.js"
      provides: "Modernisiertes Dashboard mit KPI-Grid, Export-Button, touch-optimierten Elementen"
      contains: "grid-cols-2"
    - path: "frontend/src/index.css"
      provides: "Optionale Dashboard-spezifische Utility-Klassen"
  key_links:
    - from: "Dashboard.js KPI-Grid"
      to: "Tailwind responsive classes"
      via: "grid-cols-2 sm:grid-cols-2 lg:grid-cols-4 breakpoints"
      pattern: "grid-cols-2.*sm:grid-cols-2"
    - from: "Export-Button"
      to: "AppContent Tab-Navigation"
      via: "setActiveTab callback or anchor"
      pattern: "export|Export"
---

<objective>
Dashboard UI Makeover: KPI-Grid auf 2x2 (Desktop/iPad) bzw. Stack (iPhone) umstellen, alle Touch-Targets auf min 44px, Formular-Card modernisieren und Export-Schnellzugriff hinzufuegen.

Purpose: Das Dashboard wird zur modernen, touch-freundlichen Zentrale fuer alle Geraete (iPhone Dienstgeraet, Desktop, iPad).
Output: Modernisiertes Dashboard.js mit responsive KPI-Grid, Export-Button und optimierten Touch-Targets.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-dashboard-makeover/16-CONTEXT.md

@frontend/src/components/Dashboard.js
@frontend/src/components/AppContent.js
@frontend/src/index.css

<interfaces>
<!-- Dashboard nutzt AppContext fuer Daten und Navigation -->
From frontend/src/contexts/AppContext.js:
- summary, fahrten, favoriten, executeFavorit, showNotification, refreshAllData, monthlyData

From frontend/src/components/AppContent.js:
- Tab-Navigation: activeTab state mit setActiveTab
- Tabs: 'dashboard', 'fahrten', 'monatsuebersicht', 'einstellungen', 'verwaltung'
- Dashboard braucht Zugriff auf setActiveTab fuer Export-Navigation

From frontend/src/index.css (Phase 15 Design-Tokens):
- card-container, card-container-flush, card-container-highlight
- rounded-card, shadow-card, shadow-card-hover, border-card, bg-card
- btn-primary, btn-secondary
- text-value, text-muted, text-label
- form-input, form-select, form-label
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dashboard KPI-Grid, Touch-Targets und Export-Schnellzugriff</name>
  <files>frontend/src/components/Dashboard.js, frontend/src/components/AppContent.js</files>
  <read_first>frontend/src/components/Dashboard.js, frontend/src/components/AppContent.js, frontend/src/index.css</read_first>
  <action>
Dashboard.js komplett ueberarbeiten:

**1. KPI-Cards Grid (per DASH-10):**
- Aktuell: `grid-cols-1 sm:grid-cols-3` (3 KPIs in einer Reihe)
- Neu: 4 KPIs (bisherige 3 + neuer "Fahrten gesamt" oder "Letzter Eintrag"), Grid: `grid-cols-2 lg:grid-cols-4 gap-4`
- Auf iPhone (< 640px): 2x2 Grid beibehalten (nicht stacken — per User-Decision "2x2 Grid auf Desktop/iPad, Stack auf iPhone" — hier ist "Stack" = 2x2 statt 4er-Reihe)
- Falls nur 3 KPIs sinnvoll: `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3` und vierte KPI-Card als Export-Schnellzugriff nutzen
- Jede KPI-Card: min-height 44px touch-target, padding mind. p-4

**2. Touch-Targets auf min 44px (per DASH-10):**
- Favoriten-Buttons: Aktuell p-3 — auf `min-h-[44px] p-3` erhoehen
- "Nochmal"-Buttons bei letzten Fahrten: Aktuell `px-3 py-1` — auf `min-h-[44px] px-3 py-2` erhoehen
- Formular-Toggle-Button: Bereits gut (ganzer Balken), aber `min-h-[44px]` explizit setzen
- Jahr-Navigation Buttons (Statistik): Aktuell `p-1` — auf `p-2 min-h-[44px] min-w-[44px]` erhoehen

**3. Export-Schnellzugriff (per DASH-11):**
- AppContent.js: `setActiveTab` als Prop an Dashboard uebergeben — ODER via AppContext
- Pruefen ob AppContext bereits setActiveTab hat. Falls nicht: Dashboard erhaelt `onNavigate` callback prop von AppContent
- In AppContent.js Zeile 123: `<Dashboard onNavigate={setActiveTab} />` statt `<Dashboard />`
- In Dashboard.js: `onNavigate` prop empfangen
- Export-Button als 4. KPI-Card ODER als eigener Button im Dashboard-Header platzieren:
  - Variante (Claude's Discretion): Kleine Card neben KPIs mit "Export" Icon (FileDown aus lucide-react) und Text "Zum Export", onClick navigiert zu 'fahrten' Tab
  - Styling: `card-interactive` Klasse aus index.css nutzen (hover:shadow-card-hover, hover:scale-[1.01])
  - Button-Text: "Fahrten & Export" mit FileDown Icon

**4. Formular-Card modernisieren (per DASH-09):**
- Gradient-Header beibehalten (aus Phase 13, aktuell vorhanden)
- Formular-Card-Container: `card-container-flush` beibehalten (ist bereits gut)
- Toggle-Button: Gradient-Hintergrund im offenen Zustand beibehalten, Icon-Box Styling aufwerten
- Geschlossener Zustand: Subtiler Call-to-Action-Stil — leichter blauer Hintergrund oder Border-Highlight statt plain

**5. Gesamtlayout:**
- `space-y-6` beibehalten fuer Sektionen-Abstand
- Alle Cards nutzen Phase-15 Design-Tokens (card-container, rounded-card, shadow-card etc.)
- Sicherstellen: Kein horizontales Scrollen auf 375px Breite (iPhone SE)
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Fahrtenbuch/frontend && npx react-scripts build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
- KPI-Cards in 2-spaltigem Grid auf kleinen Screens, 4-spaltig (oder 3+Export) auf Desktop
- Export-Schnellzugriff-Button/Card auf Dashboard sichtbar, navigiert zu Fahrten-Tab
- Alle interaktiven Elemente (Buttons, Cards) haben min-h-[44px]
- Build kompiliert ohne Fehler
- Formular-Toggle hat modernen Card-Stil mit Gradient im offenen Zustand
  </acceptance_criteria>
  <done>Dashboard zeigt responsive KPI-Grid, touch-optimierte Buttons (44px min), Export-Schnellzugriff und modernisiertes Formular-Card-Layout. Build erfolgreich.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visueller Check Dashboard auf Desktop und Mobile</name>
  <files>frontend/src/components/Dashboard.js</files>
  <action>Visueller Check durch den User — keine Code-Aenderungen, nur Verifikation des UI-Ergebnisses aus Task 1.</action>
  <what-built>Dashboard UI Makeover: KPI-Grid responsive, Export-Schnellzugriff, 44px Touch-Targets, modernisiertes Formular</what-built>
  <how-to-verify>
    1. Frontend starten: `cd frontend && npm start`
    2. Desktop (Chrome): Dashboard oeffnen, pruefen:
       - KPI-Cards in einer Reihe (3-4 Spalten)
       - Export-Schnellzugriff sichtbar, Klick navigiert zu "Fahrten & Export"
       - Formular aufklappen: Gradient-Header, modernes Layout
    3. Chrome DevTools > Toggle Device (iPhone SE 375px):
       - KPI-Cards im 2x2 Grid
       - Kein horizontales Scrollen
       - Favoriten-Buttons, Nochmal-Buttons gut tippbar (44px)
    4. Chrome DevTools > Toggle Device (iPad 768px):
       - KPI-Cards im 2x2 Grid
       - Alles gut lesbar und bedienbar
    5. Dark Mode testen (Toggle im Header)
  </how-to-verify>
  <read_first>frontend/src/components/Dashboard.js</read_first>
  <verify>User bestaetigt visuelles Ergebnis</verify>
  <acceptance_criteria>Dashboard sieht modern aus, KPIs reagieren responsive, Export erreichbar, Touch-Targets ausreichend gross, Dark Mode korrekt</acceptance_criteria>
  <done>User hat Dashboard-Makeover auf Desktop, iPhone und iPad visuell geprueft und bestaetigt.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `cd frontend && npx react-scripts build` kompiliert ohne Fehler
- Dashboard rendert KPI-Grid responsive (2-spaltig auf Mobile, 3-4-spaltig auf Desktop)
- Export-Schnellzugriff navigiert korrekt zum Fahrten-Tab
- Alle Touch-Targets >= 44px Hoehe
</verification>

<success_criteria>
- DASH-09: Formular hat modernes Card-Layout mit Gradient-Header
- DASH-10: Alle Dashboard-Elemente touch-freundlich, 2x2 KPI-Grid auf Mobile
- DASH-11: Export direkt vom Dashboard erreichbar
</success_criteria>

<output>
After completion, create `.planning/phases/16-dashboard-makeover/16-01-SUMMARY.md`
</output>
