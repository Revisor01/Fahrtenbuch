---
phase: 16-dashboard-makeover
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/AppContent.js
autonomous: true
requirements:
  - DASH-08

must_haves:
  truths:
    - "Im Tab 'Fahrten & Export' gibt es kein Eingabeformular mehr — nur noch die Fahrtenliste"
    - "Das Formular 'Neue Fahrt erfassen' existiert ausschliesslich auf dem Dashboard"
    - "Der FahrtForm-Import in AppContent.js wird nur noch fuer Dashboard genutzt (oder entfernt)"
  artifacts:
    - path: "frontend/src/components/AppContent.js"
      provides: "Fahrten-Tab ohne FahrtForm, nur FahrtenListe"
  key_links:
    - from: "AppContent.js fahrten-Tab"
      to: "FahrtenListe"
      via: "Direktes Rendering ohne FahrtForm-Wrapper"
      pattern: "activeTab === 'fahrten'.*FahrtenListe"
---

<objective>
FahrtForm aus dem Fahrten-Tab entfernen — Dashboard wird einziger Ort fuer neue Fahrten.

Purpose: Nutzer sollen Fahrten ausschliesslich ueber das Dashboard erfassen (DASH-08). Die Fahrtenliste zeigt nur bestehende Fahrten.
Output: AppContent.js ohne FahrtForm im Fahrten-Tab.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-dashboard-makeover/16-CONTEXT.md

@frontend/src/components/AppContent.js

<interfaces>
From frontend/src/components/AppContent.js (Zeilen 124-129):
```javascript
{activeTab === 'fahrten' && (
  <div className="space-y-6">
    <FahrtForm />       // <-- DIESE ZEILE ENTFERNEN
    <FahrtenListe />
  </div>
)}
```

Import auf Zeile 5:
```javascript
import FahrtForm from '../FahrtForm';  // <-- Wird noch fuer Dashboard gebraucht? NEIN — Dashboard importiert FahrtForm selbst
```

Dashboard.js importiert FahrtForm eigenstaendig (Zeile 4):
```javascript
import FahrtForm from '../FahrtForm';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: FahrtForm aus Fahrten-Tab und AppContent-Import entfernen</name>
  <files>frontend/src/components/AppContent.js</files>
  <read_first>frontend/src/components/AppContent.js</read_first>
  <action>
In AppContent.js zwei Aenderungen:

**1. FahrtForm-Import entfernen (Zeile 5):**
- Zeile `import FahrtForm from '../FahrtForm';` komplett loeschen
- Dashboard.js hat seinen eigenen FahrtForm-Import, daher wird dieser Import in AppContent nicht mehr benoetigt

**2. FahrtForm-Rendering im Fahrten-Tab entfernen (Zeilen 124-129):**
- Aktuell:
```javascript
{activeTab === 'fahrten' && (
  <div className="space-y-6">
    <FahrtForm />
    <FahrtenListe />
  </div>
)}
```
- Neu (FahrtForm und Wrapper-div entfernen):
```javascript
{activeTab === 'fahrten' && <FahrtenListe />}
```
- Das `space-y-6` div-Wrapper ist nicht mehr noetig, da nur noch eine Komponente gerendert wird

**WICHTIG:** NUR diese zwei Aenderungen. Nichts anderes an AppContent.js aendern. Die Dashboard-Komponente importiert FahrtForm selbst und ist nicht betroffen.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Fahrtenbuch/frontend && npx react-scripts build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
- `import FahrtForm` existiert NICHT mehr in AppContent.js
- Im fahrten-Tab wird nur `<FahrtenListe />` gerendert, kein `<FahrtForm />`
- Dashboard.js rendert weiterhin FahrtForm (eigener Import)
- Build kompiliert ohne Fehler und ohne unused-import Warnings
  </acceptance_criteria>
  <done>FahrtForm existiert nur noch auf dem Dashboard. Der Fahrten-Tab zeigt ausschliesslich die Fahrtenliste. Build erfolgreich.</done>
</task>

</tasks>

<verification>
- `cd frontend && npx react-scripts build` kompiliert ohne Fehler
- `grep -n "FahrtForm" frontend/src/components/AppContent.js` liefert KEINE Treffer
- `grep -n "FahrtForm" frontend/src/components/Dashboard.js` liefert weiterhin Treffer (Import + Rendering)
</verification>

<success_criteria>
- DASH-08: Dashboard ist die einzige Stelle zum Erfassen neuer Fahrten
- Fahrtenliste zeigt nur bestehende Fahrten (Vorbereitung fuer FE-01 in Phase 17)
</success_criteria>

<output>
After completion, create `.planning/phases/16-dashboard-makeover/16-02-SUMMARY.md`
</output>
