---
phase: 24-monatsuebersicht-refactor
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/MonthlyOverview.js
autonomous: true
requirements:
  - MU-02
  - MU-03
  - MU-04

must_haves:
  truths:
    - "Desktop und Mobile rendern aus einer einzigen Komponenten-Struktur"
    - "Kein hidden/sm:hidden Doppel-Rendering fuer separate Layouts"
    - "Jahresuebersicht hat section-header mit BarChart3 Icon"
    - "Cards nutzen kpi-card und card-container — keine Sonder-Styles"
  artifacts:
    - path: "frontend/src/components/MonthlyOverview.js"
      provides: "Vereinheitlichte responsive Monatsuebersicht"
      contains: "BarChart3"
  key_links:
    - from: "MonthlyOverview.js"
      to: "index.css section-header"
      via: "className section-header"
      pattern: "section-header"
    - from: "MonthlyOverview.js"
      to: "index.css kpi-card"
      via: "className kpi-card"
      pattern: "kpi-card"
---

<objective>
MonthlyOverview Desktop/Mobile-Merge, Section-Header und Card-Konsistenz.

Purpose: Eliminiert doppelten Render-Code (Desktop hidden sm:block + Mobile sm:hidden), fuegt das globale section-header Pattern hinzu und stellt Card-Konsistenz sicher.
Output: Eine einzige responsive Komponenten-Struktur in MonthlyOverview.js
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-monatsuebersicht-refactor/24-CONTEXT.md

<interfaces>
From frontend/src/index.css:
```css
.section-header {
  @apply flex items-center gap-2 mb-3;
}
.section-header h2 {
  @apply text-base font-medium text-value;
}
```

From frontend/src/components/Dashboard.js (BarChart3 usage reference):
```jsx
<div className="section-header" style={{marginBottom: 0}}>
  <BarChart3 size={18} className="text-blue-500" />
  <h2>Statistiken</h2>
</div>
```

Current MonthlyOverview.js structure to merge:
- Lines 337-421: Header mit Navigation (Desktop controls hidden sm:flex, Mobile controls sm:hidden)
- Lines 473-547: Desktop View (hidden sm:block) — card-container mit kpi-card Grid
- Lines 550-621: Mobile View (sm:hidden) — mobile-card mit Stack-Layout
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Section-Header mit BarChart3 Icon (MU-03)</name>
  <files>frontend/src/components/MonthlyOverview.js</files>
  <read_first>frontend/src/components/MonthlyOverview.js</read_first>
  <action>
In frontend/src/components/MonthlyOverview.js:

1. **BarChart3 importieren:** In der Lucide-Import-Zeile (Zeile 2) `BarChart3` hinzufuegen:
   - Vorher: `import { AlertCircle, Circle, CheckCircle2, CalendarDays } from 'lucide-react';`
   - Nachher: `import { AlertCircle, Circle, CheckCircle2, CalendarDays, BarChart3 } from 'lucide-react';`

2. **Jahresuebersicht-Header (Zeile 342-343) auf section-header umstellen:**
   - Vorher:
     ```jsx
     <h2 className="text-lg font-medium text-value">
       Jahresübersicht {selectedYear === 'all' ? '— Alle Jahre' : selectedYear}
     </h2>
     ```
   - Nachher:
     ```jsx
     <div className="section-header" style={{marginBottom: 0}}>
       <BarChart3 size={18} className="text-blue-500" />
       <h2>Abrechnungen {selectedYear === 'all' ? '— Alle Jahre' : selectedYear}</h2>
     </div>
     ```
   
   Hinweis: Seitentitel wird zu "Abrechnungen" per MU-01 Entscheidung (im section-header). Das BarChart3-Icon und die Farbe text-blue-500 entsprechen dem Dashboard-Statistik-Pattern.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Fahrtenbuch && grep -c "BarChart3" frontend/src/components/MonthlyOverview.js | grep -q "^[2-9]" && grep -c "section-header" frontend/src/components/MonthlyOverview.js | grep -q "^[1-9]" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep "BarChart3" frontend/src/components/MonthlyOverview.js returns at least 2 matches (import + usage)
    - grep "section-header" frontend/src/components/MonthlyOverview.js returns at least 1 match
    - grep "Abrechnungen" frontend/src/components/MonthlyOverview.js returns at least 1 match (page title)
  </acceptance_criteria>
  <done>Section-Header mit BarChart3 Icon und "Abrechnungen" Titel ist in der Jahresuebersicht sichtbar, nutzt das globale .section-header CSS-Pattern.</done>
</task>

<task type="auto">
  <name>Task 2: Desktop/Mobile-Merge zu einer responsiven Struktur (MU-02 + MU-04)</name>
  <files>frontend/src/components/MonthlyOverview.js</files>
  <read_first>frontend/src/components/MonthlyOverview.js</read_first>
  <action>
In frontend/src/components/MonthlyOverview.js das Doppel-Rendering eliminieren. Drei Bereiche zusammenfuehren:

**A) Header-Controls (Zeilen 337-421) — Desktop/Mobile Controls mergen:**

Die zwei separaten Control-Bloecke (hidden sm:flex auf Zeile 353 und flex sm:hidden auf Zeile 386) durch EINE responsive Struktur ersetzen:

```jsx
<div className="flex flex-col gap-4">
  {/* Zeile 1: Titel */}
  <div className="flex flex-col sm:flex-row sm:items-center gap-4">
    <div className="flex justify-between items-center">
      <div className="section-header" style={{marginBottom: 0}}>
        <BarChart3 size={18} className="text-blue-500" />
        <h2>Abrechnungen {selectedYear === 'all' ? '— Alle Jahre' : selectedYear}</h2>
      </div>
      {selectedYear !== currentYear && (
        <button onClick={() => setSelectedYear(currentYear)} className="sm:hidden btn-secondary">
          Aktuelles Jahr
        </button>
      )}
    </div>

    {/* Controls — auf Mobile unter Titel, auf Desktop rechts */}
    <div className="flex items-center gap-4 sm:ml-auto">
      {selectedYear !== currentYear && (
        <button onClick={() => setSelectedYear(currentYear)} className="hidden sm:block btn-secondary">
          Aktuelles Jahr
        </button>
      )}
      <label className="checkbox-label">
        <input
          type="checkbox"
          checked={hideCompleted}
          onChange={(e) => setHideCompleted(e.target.checked)}
          className="checkbox-input h-3 w-3"
        />
        <span className="text-xs text-label">Abgeschlossene</span>
      </label>
      <select
        value={selectedYear}
        onChange={(e) => setSelectedYear(e.target.value)}
        className="form-select w-24"
      >
        <option value="all">Gesamt</option>
        {[...new Set(monthlyData.map(m => m.year))]
          .sort((a, b) => b - a)
          .map(year => (
            <option key={year} value={year}>{year}</option>
          ))
        }
      </select>
    </div>
  </div>

  {/* Quick Actions */}
  <div>
    <QuickActions
      filteredData={filteredData}
      handleAbrechnungsStatus={handleAbrechnungsStatus}
      abrechnungstraeger={abrechnungstraeger}
    />
  </div>
</div>
```

**B) Monats-Cards (Zeilen 473-621) — Desktop und Mobile zu EINEM Block mergen:**

Den Desktop-Block (`hidden sm:block`, Zeilen 473-547) und den Mobile-Block (`sm:hidden`, Zeilen 550-621) durch EINE responsive Struktur ersetzen. Basis ist das Desktop-Layout, das bereits responsive ist:

```jsx
<div className="space-y-4">
  {filteredData.map((month) => {
    const originalGesamt = Object.values(month.erstattungen || {}).reduce((sum, betrag) =>
      sum + Number(betrag || 0), 0
    );
    const gesamtAusstehend = Object.entries(month.erstattungen || {}).reduce((sum, [id, betrag]) => {
      const received = month.abrechnungsStatus?.[id]?.erhalten_am;
      return sum + (received ? 0 : Number(betrag || 0));
    }, 0);

    return (
      <div key={month.yearMonth} className="card-container">
        {/* Header mit Monat und Gesamtsumme */}
        <div className="flex justify-between items-center mb-4 pb-2">
          <div className="flex items-center gap-2">
            <CalendarDays size={20} className="text-primary-400 dark:text-primary-500" />
            <h3 className="text-lg font-medium text-value">
              {month.monthName} {month.year}
            </h3>
          </div>
          <div className="text-right">
            <div className="text-value font-medium">
              {gesamtAusstehend.toFixed(2)} €
            </div>
            {gesamtAusstehend !== originalGesamt && (
              <div className="text-xs text-muted">
                Ursprünglich: {originalGesamt.toFixed(2)} €
              </div>
            )}
          </div>
        </div>

        {/* Grid fuer Abrechnungstraeger — responsiv ohne Doppel-Rendering */}
        <div className="grid grid-cols-1 md:grid-cols-2 xl:grid-cols-3 gap-4">
          {abrechnungstraeger
            .filter(traeger => (month.erstattungen?.[traeger.id] || 0) > 0)
            .map(traeger => (
              <div key={traeger.id} className="kpi-card" style={getCardBg(traeger.farbe)}>
                <div className="flex justify-between items-start mb-2">
                  <span className="text-sm font-medium text-value">{traeger.name}</span>
                  <span className={month.abrechnungsStatus?.[traeger.id]?.erhalten_am ? "text-muted" : "text-value"}>
                    {Number(month.erstattungen?.[traeger.id] || 0).toFixed(2)} €
                  </span>
                </div>
                <div className="mt-2">
                  {renderStatusCell(month, traeger.id)}
                </div>
              </div>
            ))}

          {/* Mitfahrer Card */}
          {month.erstattungen?.mitfahrer > 0 && (
            <div className="kpi-card" style={getCardBg(MITFAHRER_FARBE)}>
              <div className="flex justify-between items-start mb-2">
                <span className="text-sm font-medium text-value">Mitfahrer:innen</span>
                <span className={month.abrechnungsStatus?.mitfahrer?.erhalten_am ? "text-muted" : "text-value"}>
                  {Number(month.erstattungen?.mitfahrer || 0).toFixed(2)} €
                </span>
              </div>
              <div className="mt-2">
                {renderStatusCell(month, 'mitfahrer')}
              </div>
            </div>
          )}
        </div>
      </div>
    );
  })}
</div>
```

Das Desktop-Layout (card-container mit kpi-card Grid) wird die einzige Struktur. Das Grid `grid-cols-1 md:grid-cols-2 xl:grid-cols-3` sorgt dafuer, dass auf Mobile automatisch eine Spalte gerendert wird — kein separater Mobile-Block noetig.

**C) Card-Konsistenz pruefen (MU-04):**
- Alle Cards muessen `kpi-card` oder `card-container` nutzen — kein `mobile-card` oder andere Sonder-Klassen
- Der `mobile-card` Klassen-Verweis aus dem alten Mobile-Block (Zeile 562) faellt automatisch weg
- Pruefen dass keine hardcoded Farb-Backgrounds uebrig bleiben (nur getCardBg inline styles fuer Traeger-Farben)

**WICHTIG:** Die `className="w-full text-center"` prop auf QuickActions (Zeile 419) hat keinen Effekt, da QuickActions keine className-Prop akzeptiert — kann entfernt werden.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Fahrtenbuch && grep -c "hidden sm:block\|sm:hidden\|hidden sm:flex\|flex sm:hidden" frontend/src/components/MonthlyOverview.js | grep -q "^[01]$" && grep -c "mobile-card" frontend/src/components/MonthlyOverview.js | grep -q "^0$" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep "hidden sm:block" frontend/src/components/MonthlyOverview.js returns 0 matches
    - grep "sm:hidden" frontend/src/components/MonthlyOverview.js returns at most 1 match (fuer "Aktuelles Jahr" Button, der nur auf Mobile sichtbar ist — sm:hidden ist OK fuer einzelne Elemente, nur nicht fuer ganze Layout-Bloecke)
    - grep "mobile-card" frontend/src/components/MonthlyOverview.js returns 0 matches
    - grep "hidden sm:flex" frontend/src/components/MonthlyOverview.js returns 0 matches (kein separater Desktop-Controls-Block mehr)
    - grep "kpi-card" frontend/src/components/MonthlyOverview.js returns multiple matches
    - grep "card-container" frontend/src/components/MonthlyOverview.js returns multiple matches
  </acceptance_criteria>
  <done>Eine einzige responsive Komponenten-Struktur. Kein hidden/sm:hidden fuer separate Desktop/Mobile-Layouts. Alle Cards nutzen kpi-card und card-container. Mobile rendert automatisch einspaltig durch Grid-Breakpoints.</done>
</task>

</tasks>

<verification>
- grep "hidden sm:block" frontend/src/components/MonthlyOverview.js — muss leer sein
- grep "sm:hidden" frontend/src/components/MonthlyOverview.js — maximal 1 Treffer (einzelnes Element, nicht Layout-Block)
- grep "mobile-card" frontend/src/components/MonthlyOverview.js — muss leer sein
- grep "section-header" frontend/src/components/MonthlyOverview.js — mindestens 1 Treffer
- grep "BarChart3" frontend/src/components/MonthlyOverview.js — mindestens 2 Treffer (import + usage)
- Frontend kompiliert ohne Fehler
</verification>

<success_criteria>
- Desktop und Mobile rendern aus einer einzigen Komponenten-Struktur
- Section-Header mit BarChart3 Icon in der Jahresuebersicht
- Alle Cards nutzen globale kpi-card/card-container Patterns
- Kein Doppel-Rendering durch hidden/sm:hidden Layout-Bloecke
</success_criteria>

<output>
After completion, create `.planning/phases/24-monatsuebersicht-refactor/24-02-SUMMARY.md`
</output>
