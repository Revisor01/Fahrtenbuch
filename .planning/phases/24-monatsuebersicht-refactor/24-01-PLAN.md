---
phase: 24-monatsuebersicht-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/AppContent.js
autonomous: true
requirements:
  - MU-01

must_haves:
  truths:
    - "Tab zeigt 'Abrechnungen' statt 'Monatsübersicht'"
    - "Tab-ID ist 'abrechnungen' statt 'monatsuebersicht'"
    - "activeTab-Check rendert MonthlyOverview bei 'abrechnungen'"
  artifacts:
    - path: "frontend/src/components/AppContent.js"
      provides: "Umbenannter Tab"
      contains: "abrechnungen"
  key_links:
    - from: "AppContent.js tabs array"
      to: "AppContent.js activeTab check"
      via: "tab.id === 'abrechnungen'"
      pattern: "activeTab === 'abrechnungen'"
---

<objective>
Tab-Umbenennung von "Monatsübersicht" zu "Abrechnungen" in der Navigation.

Purpose: Per Entscheidung MU-01 beschreibt "Abrechnungen" den Inhalt besser — es geht um Erstattungs-Status pro Träger, nicht um eine kalendarische Monatsübersicht.
Output: AppContent.js mit umbenanntem Tab
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-monatsuebersicht-refactor/24-CONTEXT.md

<interfaces>
From frontend/src/components/AppContent.js:
- Line 59: Tab-Definition { id: 'monatsuebersicht', label: 'Monatsübersicht', icon: CalendarDays }
- Line 134: activeTab check: {activeTab === 'monatsuebersicht' && <MonthlyOverview />}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tab-Umbenennung in AppContent.js (MU-01)</name>
  <files>frontend/src/components/AppContent.js</files>
  <read_first>frontend/src/components/AppContent.js</read_first>
  <action>
In frontend/src/components/AppContent.js drei Stellen aendern:

1. **Tab-Definition (Zeile 59):** id von 'monatsuebersicht' auf 'abrechnungen' aendern, label von 'Monats\u00FCbersicht' auf 'Abrechnungen' aendern. Icon bleibt CalendarDays.
   - Vorher: `{ id: 'monatsuebersicht', label: 'Monats\u00FCbersicht', icon: CalendarDays }`
   - Nachher: `{ id: 'abrechnungen', label: 'Abrechnungen', icon: CalendarDays }`

2. **activeTab-Check (Zeile 134):** 'monatsuebersicht' durch 'abrechnungen' ersetzen.
   - Vorher: `{activeTab === 'monatsuebersicht' && <MonthlyOverview />}`
   - Nachher: `{activeTab === 'abrechnungen' && <MonthlyOverview />}`

3. **Pruefen ob 'monatsuebersicht' noch an weiteren Stellen vorkommt** (z.B. in handleNavigate deeplinks). Falls ja, ebenfalls auf 'abrechnungen' aendern.

Per D-01/MU-01: "Abrechnungen" beschreibt den Inhalt besser.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Fahrtenbuch && grep -c "monatsuebersicht" frontend/src/components/AppContent.js | grep -q "^0$" && grep -c "abrechnungen" frontend/src/components/AppContent.js | grep -q "^[2-9]" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep "monatsuebersicht" frontend/src/components/AppContent.js returns 0 matches
    - grep "id: 'abrechnungen'" frontend/src/components/AppContent.js returns 1 match
    - grep "activeTab === 'abrechnungen'" frontend/src/components/AppContent.js returns 1 match
    - grep "'Abrechnungen'" frontend/src/components/AppContent.js returns 1 match
  </acceptance_criteria>
  <done>Tab-Name ist "Abrechnungen", Tab-ID ist "abrechnungen", MonthlyOverview wird bei activeTab === 'abrechnungen' gerendert. Kein Vorkommen von "monatsuebersicht" mehr in AppContent.js.</done>
</task>

</tasks>

<verification>
- grep "monatsuebersicht" frontend/src/components/AppContent.js — muss leer sein
- grep "abrechnungen" frontend/src/components/AppContent.js — muss id und activeTab-Check zeigen
- Frontend startet ohne Fehler (npm start im frontend/)
</verification>

<success_criteria>
- Tab zeigt "Abrechnungen" in der Navigation
- Klick auf Tab rendert MonthlyOverview korrekt
- Kein "monatsuebersicht" mehr in AppContent.js
</success_criteria>

<output>
After completion, create `.planning/phases/24-monatsuebersicht-refactor/24-01-SUMMARY.md`
</output>
