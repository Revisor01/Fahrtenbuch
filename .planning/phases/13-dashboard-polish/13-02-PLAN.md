---
phase: 13-dashboard-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/controllers/favoritController.js
  - frontend/src/contexts/AppContext.js
  - frontend/src/components/Dashboard.js
  - frontend/src/components/FahrtenListe.js
autonomous: true
requirements: [FAV-04]

must_haves:
  truths:
    - "Beim Ausfuehren eines Favoriten erscheint ein Dialog 'Mit Rueckfahrt?'"
    - "Bei 'Ja' werden Hin- und Rueckfahrt (Von/Nach vertauscht) angelegt"
    - "Bei 'Nein' wird nur die Hinfahrt angelegt"
    - "In der FahrtenListe bieten die Nochmal-Buttons eine 'Andere Richtung'-Option an"
  artifacts:
    - path: "backend/controllers/favoritController.js"
      provides: "Execute-Endpoint mit optionalem mitRueckfahrt-Parameter"
      contains: "mitRueckfahrt"
    - path: "frontend/src/contexts/AppContext.js"
      provides: "executeFavorit mit mitRueckfahrt-Parameter"
      contains: "mitRueckfahrt"
    - path: "frontend/src/components/Dashboard.js"
      provides: "Rueckfahrt-Dialog bei Favorit-Ausfuehrung"
      contains: "Rueckfahrt"
    - path: "frontend/src/components/FahrtenListe.js"
      provides: "Nochmal-Buttons mit Andere-Richtung-Option"
      contains: "Andere Richtung"
  key_links:
    - from: "frontend/src/components/Dashboard.js"
      to: "AppContext.executeFavorit"
      via: "showNotification Dialog"
      pattern: "executeFavorit.*mitRueckfahrt"
    - from: "frontend/src/contexts/AppContext.js"
      to: "POST /api/favoriten/:id/execute"
      via: "axios.post mit mitRueckfahrt body"
      pattern: "mitRueckfahrt"
---

<objective>
Favoriten-Ausfuehrung um Rueckfahrt-Dialog erweitern und Nochmal-Buttons in der FahrtenListe mit "Andere Richtung"-Option versehen.

Purpose: Wiederkehrende Hin-und-Rueckfahrten mit einem Klick statt zwei anlegen.
Output: Backend mit mitRueckfahrt-Support, Frontend mit Dialog und erweiterten Nochmal-Buttons.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-dashboard-polish/13-CONTEXT.md
@backend/controllers/favoritController.js
@frontend/src/contexts/AppContext.js
@frontend/src/components/Dashboard.js
@frontend/src/components/FahrtenListe.js
</context>

<interfaces>
<!-- Existing interfaces the executor needs -->

From frontend/src/contexts/AppContext.js:
```javascript
// executeFavorit currently:
const executeFavorit = async (id) => {
  const response = await axios.post(`${API_BASE_URL}/favoriten/${id}/execute`);
  await refreshAllData();
  return response.data;
};

// showNotification signature:
const showNotification = (title, message, onConfirm, showCancel, confirmLabel, onSecondAction, secondLabel, onThirdAction, thirdLabel) => { ... };
```

From backend/controllers/favoritController.js:
```javascript
// executeFavorit currently creates one Fahrt via Fahrt.create(fahrtData, null, req.user.id)
// getDistance imported from utils/distanceCalculator
```

From frontend/src/NotificationModal.js:
```javascript
// Supports multiple action buttons: onConfirm/confirmLabel, onSecondAction/secondLabel, onThirdAction/thirdLabel
// Plus "Abbrechen" button when hasMultipleActions is true
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Backend — Execute-Endpoint mit Rueckfahrt-Support</name>
  <files>backend/controllers/favoritController.js</files>
  <read_first>backend/controllers/favoritController.js, backend/models/FavoritFahrt.js</read_first>
  <action>
Den `executeFavorit` Controller (per D-07, D-08 — FAV-04) erweitern:

1. Aus `req.body` den optionalen Parameter `mitRueckfahrt` (Boolean, default false) lesen
2. Die bestehende Hinfahrt-Logik bleibt unveraendert
3. Nach der Hinfahrt-Erstellung: Wenn `mitRueckfahrt === true`, eine zweite Fahrt anlegen mit vertauschten Von/Nach:
   ```javascript
   if (mitRueckfahrt) {
     // Rueckfahrt: Von und Nach vertauscht
     const rueckKilometer = await getDistance(favorit.nach_ort_id, favorit.von_ort_id, req.user.id);
     const rueckfahrtData = {
       datum,
       anlass: favorit.anlass,
       kilometer: rueckKilometer || kilometer || 0,
       abrechnung: favorit.abrechnungstraeger_id,
       vonOrtId: favorit.nach_ort_id,
       nachOrtId: favorit.von_ort_id,
       einmaligerVonOrt: null,
       einmaligerNachOrt: null,
       userId: req.user.id,
     };
     const rueckfahrtId = await Fahrt.create(rueckfahrtData, null, req.user.id);
   }
   ```
4. Response aendern: Bei mitRueckfahrt die Message anpassen: `'Hin- und Rückfahrt aus Favorit erstellt'`
5. Bei Fehler in der Rueckfahrt: Die Hinfahrt wurde bereits angelegt — das ist OK, Fehlermeldung loggen aber Hinfahrt-ID trotzdem zurueckgeben
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Fahrtenbuch/backend && node -e "const c = require('./controllers/favoritController'); console.log(typeof c.executeFavorit === 'function' ? 'OK' : 'FAIL')"</automated>
  </verify>
  <acceptance_criteria>
    - executeFavorit akzeptiert optionalen mitRueckfahrt-Parameter aus req.body
    - Bei mitRueckfahrt=true werden zwei Fahrten (Hin + Rueck mit vertauschten Orten) erstellt
    - Ohne mitRueckfahrt oder mitRueckfahrt=false wird nur eine Fahrt erstellt (Abwaertskompatibilitaet)
  </acceptance_criteria>
  <done>Execute-Endpoint erstellt bei mitRueckfahrt=true zwei Fahrten</done>
</task>

<task type="auto">
  <name>Task 2: Frontend — Rueckfahrt-Dialog und Nochmal-Erweiterung</name>
  <files>frontend/src/contexts/AppContext.js, frontend/src/components/Dashboard.js, frontend/src/components/FahrtenListe.js</files>
  <read_first>frontend/src/contexts/AppContext.js, frontend/src/components/Dashboard.js, frontend/src/components/FahrtenListe.js (Zeilen 100-120 und 1120-1135 und 1393-1405)</read_first>
  <action>
**A) AppContext.js — executeFavorit erweitern (per D-08):**
- Signature aendern: `const executeFavorit = async (id, mitRueckfahrt = false) => {`
- Den axios.post Body erweitern: `await axios.post(..., { mitRueckfahrt })`

**B) Dashboard.js — Rueckfahrt-Dialog bei Favorit-Klick (per D-07):**
- Die `handleExecuteFavorit` Funktion ueberarbeiten: Statt direkt executeFavorit aufzurufen, zuerst den Dialog zeigen.
- Das bestehende `showNotification` mit Multi-Action nutzen (NotificationModal unterstuetzt bereits confirmLabel, onSecondAction, secondLabel):
  ```javascript
  const handleExecuteFavorit = (favorit) => {
    showNotification(
      'Favorit ausführen',
      `${favorit.von_ort_name} → ${favorit.nach_ort_name}`,
      async () => {
        // "Nur Hinfahrt"
        try {
          await executeFavorit(favorit.id, false);
          showNotification('Fahrt erstellt', 'Hinfahrt wurde für heute eingetragen.');
        } catch (error) {
          console.error('Fehler beim Ausführen des Favoriten:', error);
          showNotification('Fehler', 'Favorit konnte nicht ausgeführt werden.');
        }
      },
      true, // showCancel
      'Nur Hinfahrt',
      async () => {
        // "Mit Rückfahrt"
        try {
          await executeFavorit(favorit.id, true);
          showNotification('Fahrten erstellt', 'Hin- und Rückfahrt wurden für heute eingetragen.');
        } catch (error) {
          console.error('Fehler beim Ausführen des Favoriten:', error);
          showNotification('Fehler', 'Favorit konnte nicht ausgeführt werden.');
        }
      },
      'Mit Rückfahrt'
    );
  };
  ```
- WICHTIG: handleExecuteFavorit ist jetzt NICHT mehr async (die async-Logik ist in den Callbacks)

**C) FahrtenListe.js — Nochmal-Buttons mit "Andere Richtung" (per D-09, D-10):**
- Neue Funktion `handleNochmalAndereRichtung` erstellen, die Von/Nach vertauscht:
  ```javascript
  const handleNochmalAndereRichtung = async (fahrt) => {
    try {
      await axios.post(`${API_BASE_URL}/fahrten`, {
        vonOrtId: fahrt.nach_ort_id || null,
        nachOrtId: fahrt.von_ort_id || null,
        datum: new Date().toISOString().slice(0, 10),
        anlass: fahrt.anlass,
        abrechnung: fahrt.abrechnung,
        einmaligerVonOrt: fahrt.einmaliger_nach_ort || null,
        einmaligerNachOrt: fahrt.einmaliger_von_ort || null,
        kilometer: fahrt.kilometer
      });
      showNotification('Fahrt erstellt', `Rückfahrt ${fahrt.nach_ort_name || fahrt.einmaliger_nach_ort} → ${fahrt.von_ort_name || fahrt.einmaliger_von_ort} wurde für heute eingetragen.`);
      refreshAllData();
    } catch (error) {
      console.error('Fehler beim Duplizieren der Fahrt:', error);
      showNotification('Fehler', 'Fahrt konnte nicht erstellt werden.');
    }
  };
  ```
- Desktop-Nochmal-Buttons (ca. Zeile 1124-1131): Den einzelnen "Nochmal"-Button durch zwei kleine Buttons ersetzen:
  ```jsx
  {letzteDreiIds.has(fahrt.id) && (
    <div className="flex items-center gap-1">
      <button
        onClick={() => handleNochmal(fahrt)}
        className="text-xs bg-blue-50 dark:bg-blue-900/30 text-blue-600 dark:text-blue-400 px-2 py-1 rounded hover:bg-blue-100 dark:hover:bg-blue-900/50 flex items-center gap-1"
        title="Gleiche Fahrt für heute"
      >
        <Copy size={12} />
        <span>Nochmal</span>
      </button>
      <button
        onClick={() => handleNochmalAndereRichtung(fahrt)}
        className="text-xs bg-green-50 dark:bg-green-900/30 text-green-600 dark:text-green-400 px-2 py-1 rounded hover:bg-green-100 dark:hover:bg-green-900/50 flex items-center gap-1"
        title="Rückfahrt für heute"
      >
        <ArrowLeftRight size={12} />
        <span>Rückfahrt</span>
      </button>
    </div>
  )}
  ```
- `ArrowLeftRight` aus lucide-react importieren (zum bestehenden Import hinzufuegen)
- Mobile-Nochmal-Button (ca. Zeile 1396-1403): Analog zwei Icon-Buttons (ohne Text, nur Icons):
  ```jsx
  {letzteDreiIds.has(fahrt.id) && (
    <>
      <button
        onClick={() => handleNochmal(fahrt)}
        className="text-xs bg-blue-50 dark:bg-blue-900/30 text-blue-600 dark:text-blue-400 p-1.5 rounded hover:bg-blue-100 dark:hover:bg-blue-900/50"
        title="Gleiche Fahrt für heute"
      >
        <Copy size={14} />
      </button>
      <button
        onClick={() => handleNochmalAndereRichtung(fahrt)}
        className="text-xs bg-green-50 dark:bg-green-900/30 text-green-600 dark:text-green-400 p-1.5 rounded hover:bg-green-100 dark:hover:bg-green-900/50"
        title="Rückfahrt für heute"
      >
        <ArrowLeftRight size={14} />
      </button>
    </>
  )}
  ```
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Fahrtenbuch/frontend && npx react-scripts build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Klick auf Favorit im Dashboard zeigt Dialog mit "Nur Hinfahrt", "Mit Rückfahrt" und "Abbrechen"
    - "Nur Hinfahrt" erstellt eine Fahrt, "Mit Rückfahrt" erstellt zwei
    - Nochmal-Buttons in FahrtenListe zeigen "Nochmal" + "Rückfahrt" als zwei Buttons
  </acceptance_criteria>
  <done>Favorit-Dialog fragt nach Rueckfahrt, Nochmal-Buttons bieten Richtungswahl</done>
</task>

</tasks>

<verification>
- Backend kompiliert und executeFavorit akzeptiert mitRueckfahrt
- Frontend kompiliert ohne Fehler
- Favorit-Klick zeigt Dialog mit drei Optionen
- Nochmal-Buttons zeigen zwei Varianten
</verification>

<success_criteria>
Favoriten-Ausfuehrung mit Rueckfahrt-Dialog funktioniert, Nochmal-Buttons bieten "Gleiche Richtung" und "Andere Richtung".
</success_criteria>

<output>
After completion, create `.planning/phases/13-dashboard-polish/13-02-SUMMARY.md`
</output>
