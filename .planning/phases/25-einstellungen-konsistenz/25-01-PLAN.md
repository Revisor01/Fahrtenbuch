---
phase: 25-einstellungen-konsistenz
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/Settings.js
autonomous: true
requirements:
  - ES-01
  - ES-02
  - ES-03

must_haves:
  truths:
    - "Jede Form-Section in jedem Sub-Tab hat ein Lucide-Icon und einen klaren Header-Text"
    - "Alle 8 Sub-Tabs haben identische Card-Struktur (gleiche Padding, Spacing, Border-Radius)"
    - "Formular-Inputs (Text, Select, Toggle) sehen in allen Tabs gleich aus"
  artifacts:
    - path: "frontend/src/components/Settings.js"
      provides: "Settings-Komponente mit konsistenten section-headers und Card-Struktur"
      contains: "section-header"
  key_links:
    - from: "frontend/src/components/Settings.js"
      to: "frontend/src/index.css"
      via: "section-header CSS-Klasse"
      pattern: "section-header"
---

<objective>
Alle 8 Einstellungen-Sub-Tabs auf das globale section-header Pattern (Phase 22) umstellen und Card-Struktur vereinheitlichen.

Purpose: Settings-View soll visuell auf dem gleichen Niveau wie Dashboard, FahrtenListe und Abrechnungen sein.
Output: Aktualisierte Settings.js mit Icons in allen Section-Headers und konsistenter Card-Struktur.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/src/components/Settings.js
@frontend/src/index.css

<interfaces>
<!-- section-header Pattern aus Phase 22 (index.css Zeile 299-309) -->
```css
.section-header {
  @apply flex items-center gap-2 mb-3;
}
.section-header h2 {
  @apply text-base font-medium text-value;
}
.section-header .section-count {
  @apply text-sm text-muted;
}
```

<!-- Verwendung in Dashboard.js (Referenz-Pattern): -->
```jsx
<div className="section-header">
  <Star size={18} className="text-yellow-500" />
  <h2>Favoriten</h2>
</div>
```

<!-- Bereits importierte Icons in Settings.js: -->
```jsx
import { User, MapPin, Route, Building2, Star, Coins, Lock, Key } from 'lucide-react';
```

<!-- Vorhandene Tabs-Definition: -->
```jsx
const tabs = [
  { id: 'profile', name: 'Profil', icon: User },
  { id: 'orte', name: 'Orte', icon: MapPin },
  { id: 'distanzen', name: 'Distanzen', icon: Route },
  { id: 'abrechnungen', name: 'Träger', icon: Building2 },
  { id: 'favoriten', name: 'Favoriten', icon: Star },
  { id: 'erstattungssaetze', name: 'Erstattungen', icon: Coins },
  { id: 'security', name: 'Passwort', icon: Lock },
  { id: 'api', name: 'API-Zugriff', icon: Key }
];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Section-Headers mit Icons in allen 8 Sub-Tabs</name>
  <files>frontend/src/components/Settings.js</files>
  <read_first>frontend/src/components/Settings.js, frontend/src/index.css</read_first>
  <action>
Ersetze ALLE h3-Section-Headers in Settings.js durch das globale section-header Pattern. Jede Stelle wo aktuell steht:
```jsx
<h3 className="text-lg font-medium text-value mb-4">Header-Text</h3>
```
wird ersetzt durch:
```jsx
<div className="section-header">
  <Icon size={18} className="text-icon-color" />
  <h2>Header-Text</h2>
</div>
```

Konkrete Ersetzungen (per ES-01):

**profile Tab:**
- "Persoenliche Daten" -> User icon, className="text-primary-500"

**orte Tab:**
- "Neuen Ort anlegen" -> MapPin icon, className="text-primary-500"
- "Gespeicherte Orte" -> MapPin icon, className="text-primary-500"

**distanzen Tab:**
- "Neue Distanz anlegen" -> Route icon, className="text-primary-500"
- "Gespeicherte Distanzen" -> Route icon, className="text-primary-500"

**abrechnungen Tab:**
- "Abrechnungstraeger verwalten" -> Building2 icon, className="text-primary-500"

**favoriten Tab:**
- "Neuen Favoriten anlegen" -> Star icon, className="text-yellow-500"

**erstattungssaetze Tab:**
- "Erstattungssaetze verwalten" -> Coins icon, className="text-primary-500"

**security Tab:**
- "Passwort aendern" -> Lock icon, className="text-primary-500"

**api Tab:**
- "API Key erstellen" -> Key icon, className="text-primary-500"

Wichtig: Die Icons (User, MapPin, Route, Building2, Star, Coins, Lock, Key) sind BEREITS importiert — kein neuer Import noetig. Nur Star bekommt text-yellow-500 (wie im Dashboard), alle anderen text-primary-500.

Die Beschreibungs-Paragraphen (p.text-sm.text-muted) BLEIBEN direkt nach dem section-header div. Der mb-4 vom alten h3 entfaellt — section-header hat eigenes mb-3 aus CSS.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Fahrtenbuch && grep -c "section-header" frontend/src/components/Settings.js | grep -E "^(9|1[0-9])$" && echo "OK: section-headers found" && ! grep 'className="text-lg font-medium text-value mb-4"' frontend/src/components/Settings.js && echo "OK: no old h3 headers"</automated>
  </verify>
  <acceptance_criteria>
- grep "section-header" Settings.js findet mindestens 9 Treffer (9 Sections ueber 8 Tabs)
- grep 'text-lg font-medium text-value mb-4' Settings.js findet 0 Treffer (alte h3 komplett ersetzt)
- grep "size={18}" Settings.js findet mindestens 9 Treffer (ein Icon pro Section)
  </acceptance_criteria>
  <done>Alle Section-Headers in allen 8 Sub-Tabs nutzen das section-header Pattern mit passenden Lucide-Icons</done>
</task>

<task type="auto">
  <name>Task 2: Card-Struktur und Input-Styling vereinheitlichen</name>
  <files>frontend/src/components/Settings.js</files>
  <read_first>frontend/src/components/Settings.js</read_first>
  <action>
Pruefe und vereinheitliche die Card-Struktur ueber alle 8 Tabs (per ES-02 und ES-03):

1. **Card-Klassen:** Jede Form-Section soll card-container-highlight nutzen, Listen-Sections card-container. Aktuell ist das bereits fast konsistent — sicherstellen dass keine Abweichungen vorhanden sind. Die Favoriten-Liste-Cards (grid mit card-container) und API-Keys-Cards (grid mit card-container) sind korrekt.

2. **Spacing:** Jeder Tab-Content-Wrapper soll das gleiche Pattern haben:
   - Aeusserer Wrapper: `<div className="space-y-6">`
   - Jede Card: `card-container-highlight` oder `card-container`
   - Innerhalb der Card: section-header, dann optionaler p.text-sm.text-muted.mb-6 Beschreibungstext, dann form/content

3. **Profile-Tab Sonderfall:** Aktuell hat der Profile-Tab `<div className="space-y-4">` statt `space-y-6` — angleichen auf space-y-6 wie alle anderen Tabs.

4. **Input-Styling (ES-03):** Pruefen dass ALLE Inputs form-input, ALLE Selects form-select, ALLE Labels form-label nutzen. Das ist aktuell bereits der Fall — Verification-Step soll das bestaetigen.

5. **Button-Konsistenz:** Alle Submit-Buttons nutzen btn-primary. Der "Passwort aendern" Button hat `w-full sm:w-auto` statt `mobile-full` — auf `mobile-full` angleichen fuer Konsistenz mit den anderen Tabs.

Keine strukturellen Aenderungen an der Logik. Nur CSS-Klassen und Spacing anpassen.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Fahrtenbuch && grep -c "space-y-6" frontend/src/components/Settings.js | grep -E "^[89]$" && echo "OK: consistent spacing" && grep -c "card-container" frontend/src/components/Settings.js | grep -E "^[0-9]+" && echo "OK: card containers present"</automated>
  </verify>
  <acceptance_criteria>
- grep "space-y-4" Settings.js findet 0 Treffer auf Tab-Wrapper-Ebene (nur innerhalb von Formularen erlaubt)
- Alle Tab-Content-Wrapper nutzen space-y-6
- grep "w-full sm:w-auto" Settings.js findet 0 Treffer (ersetzt durch mobile-full)
- Alle form-Elemente nutzen konsistent form-input, form-select, form-label
  </acceptance_criteria>
  <done>Alle 8 Sub-Tabs haben identische Card-Struktur, gleiches Spacing und konsistentes Input-Styling</done>
</task>

</tasks>

<verification>
1. `grep -c "section-header" frontend/src/components/Settings.js` — mindestens 9
2. `grep 'text-lg font-medium text-value mb-4' frontend/src/components/Settings.js` — 0 Treffer
3. Visuell: Alle 8 Tabs durchklicken — jede Section hat Icon + Header
4. `npm start` im Frontend — keine Build-Fehler
</verification>

<success_criteria>
- Alle h3-Headers ersetzt durch section-header Pattern mit Icons
- Card-Struktur (Padding, Spacing, Border-Radius) identisch in allen 8 Tabs
- Input-Styling konsistent (form-input, form-select, form-label ueberall)
- Kein Funktionalitaets-Regression
</success_criteria>

<output>
After completion, create `.planning/phases/25-einstellungen-konsistenz/25-01-SUMMARY.md`
</output>
