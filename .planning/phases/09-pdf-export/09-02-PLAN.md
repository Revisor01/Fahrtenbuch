---
phase: 09-pdf-export
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - frontend/src/components/FahrtenListe.js
autonomous: false
requirements: [PDF-02]

must_haves:
  truths:
    - "Im Export-Bereich sieht der Nutzer fuer jeden Abrechnungstraeger zwei Buttons: Excel und PDF"
    - "Klick auf PDF-Button laedt ein PDF herunter (identischer Ablauf wie Excel)"
    - "Nach erfolgreichem PDF-Download wird gefragt ob Zeitraum als eingereicht markiert werden soll"
  artifacts:
    - path: "frontend/src/components/FahrtenListe.js"
      provides: "Format-Auswahl (Excel/PDF) im Export-Bereich"
      contains: "handleExportToPdf"
  key_links:
    - from: "frontend/src/components/FahrtenListe.js"
      to: "/api/fahrten/export-pdf"
      via: "axios.get mit responseType blob"
      pattern: "export-pdf"
---

<objective>
Frontend-Integration: Format-Auswahl im Export-Bereich der FahrtenListe. Fuer jeden Abrechnungstraeger werden zwei Buttons (Excel / PDF) angezeigt statt einem einzelnen Export-Button.

Purpose: PDF-02 erfordert Format-Auswahl im UI
Output: FahrtenListe.js mit Excel/PDF-Buttons pro Abrechnungstraeger
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-pdf-export/09-CONTEXT.md
@.planning/phases/09-pdf-export/09-01-SUMMARY.md

@frontend/src/components/FahrtenListe.js

<interfaces>
<!-- Neue Backend-Routen aus Plan 01 -->
Backend PDF-Routen:
  GET /api/fahrten/export-pdf/:type/:year/:month -> application/pdf
  GET /api/fahrten/export-pdf-range/:type/:startYear/:startMonth/:endYear/:endMonth -> application/pdf

<!-- Bestehender Excel-Export-Handler in FahrtenListe.js (Zeilen 127-210) -->
handleExportToExcel(type) macht:
  - Unterscheidung Einzelmonat vs. Zeitraum anhand selectedVonMonth
  - axios.get mit responseType 'blob'
  - Blob-Download via temporaeres Link-Element
  - showNotification fuer "als eingereicht markieren" Dialog
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: handleExportToPdf und Format-Buttons im Export-Bereich</name>
  <files>frontend/src/components/FahrtenListe.js</files>
  <read_first>
    - frontend/src/components/FahrtenListe.js (komplett — Export-Handler und JSX-Bereich)
  </read_first>
  <action>
1. **Neue Funktion `handleExportToPdf(type)`** erstellen (direkt nach handleExportToExcel):
   - Identische Logik wie handleExportToExcel, aber:
   - URL-Pfade: `/api/fahrten/export-pdf/...` statt `/api/fahrten/export/...`
   - URL-Pfade Range: `/api/fahrten/export-pdf-range/...` statt `/api/fahrten/export-range/...`
   - Content-Type-Check: `application/pdf` statt xlsx/zip
   - Dateiname-Extension: `.pdf` statt `.xlsx`
   - defaultFilename-Prefix bleibt `fahrtenabrechnung_...` aber mit `.pdf`
   - Blob-Typ: `application/pdf`
   - showNotification nach Download: identisch (eingereicht-Frage)
   - Gleiche Validierung (Bis >= Von bei Zeitraum)
   - Kein ZIP noetig (PDF hat eingebaute Mehrseiten-Unterstuetzung)

2. **Export-Bereich im JSX aendern** (ca. Zeile 590-600):
   Aktuell:
   ```jsx
   {getKategorienMitErstattung().map(([key, displayName]) => (
     <button key={key} onClick={() => handleExportToExcel(key.toLowerCase())} className="btn-primary">
       Export {displayName}
     </button>
   ))}
   ```

   Aendern zu (per D-10 — Claude entscheidet pragmatisch, zwei Buttons pro Kategorie):
   ```jsx
   {getKategorienMitErstattung().map(([key, displayName]) => (
     <div key={key} className="flex gap-1">
       <button
         onClick={() => handleExportToExcel(key.toLowerCase())}
         className="btn-primary text-sm">
         Excel {displayName}
       </button>
       <button
         onClick={() => handleExportToPdf(key.toLowerCase())}
         className="btn-primary text-sm">
         PDF {displayName}
       </button>
     </div>
   ))}
   ```

   Die Buttons sollen kompakt nebeneinander stehen. Auf Mobile stacken sie automatisch durch das uebergeordnete `flex flex-col sm:flex-row`. Die Beschriftung ist kurz: "Excel {Name}" und "PDF {Name}" damit klar ist welches Format.

3. **handleExportToPdf Fehlerbehandlung:** Identisch zu handleExportToExcel — showNotification bei Fehler mit error.message.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/Fahrtenbuch && grep -c "handleExportToPdf" frontend/src/components/FahrtenListe.js | grep -v "^0$" && grep -c "export-pdf" frontend/src/components/FahrtenListe.js | grep -v "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - handleExportToPdf existiert als Funktion in FahrtenListe.js
    - Fuer jeden Abrechnungstraeger gibt es zwei Buttons: "Excel {Name}" und "PDF {Name}"
    - PDF-Download nutzt die neuen /api/fahrten/export-pdf Routen
    - Nach erfolgreichem Download wird "als eingereicht markieren" gefragt (wie bei Excel)
    - Bestehender Excel-Export funktioniert weiterhin unveraendert
  </acceptance_criteria>
  <done>Export-Bereich zeigt Excel/PDF-Buttons, beide Formate funktionieren</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: PDF-Export End-to-End verifizieren</name>
  <what-built>Kompletter PDF-Export: Backend-Generierung mit pdfkit und Frontend-Integration mit Format-Auswahl (Excel/PDF Buttons)</what-built>
  <how-to-verify>
    1. App oeffnen (kkd-fahrtenbuch.de oder lokal)
    2. Im Fahrten-Bereich einen Monat mit Fahrten auswaehlen
    3. Im Export-Bereich: Es sollten jetzt "Excel {Name}" und "PDF {Name}" Buttons pro Abrechnungstraeger sichtbar sein
    4. Auf "PDF {Name}" klicken — ein PDF sollte heruntergeladen werden
    5. PDF oeffnen und pruefen:
       - A4 Querformat
       - Titel "Abrechnung von genehmigten Dienstfahrten"
       - Header mit Jahr, Monat, Name, Kostentraeger, Anschrift, IBAN
       - Tabelle mit Datum, Reiseantritt, Reiseziel, Reisezweck, km
       - Gesamt-km und Erstattungsberechnung unten
       - Unterschriftsfelder
    6. Von/Bis-Zeitraum auswaehlen und PDF exportieren — Zeitraum sollte im Header stehen
    7. Excel-Button testen — sollte wie vorher funktionieren
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. FahrtenListe.js enthaelt handleExportToPdf Funktion
2. Export-Bereich zeigt zwei Buttons pro Abrechnungstraeger
3. PDF-Download funktioniert (Einzelmonat und Zeitraum)
4. Excel-Download funktioniert weiterhin
</verification>

<success_criteria>
- Nutzer kann zwischen Excel und PDF waehlen (per PDF-02)
- PDF-Download funktioniert fuer Einzelmonat und Zeitraum (per PDF-03)
- Das PDF ist druckfertig mit allen Feldern des Referenz-PDFs (per PDF-01)
- Bestehender Excel-Export ist unveraendert
</success_criteria>

<output>
After completion, create `.planning/phases/09-pdf-export/09-02-SUMMARY.md`
</output>
